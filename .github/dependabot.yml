version: 2

# Keeps every external action SHA pin current through its adjacent version
# comment, and updates the lockfile-backed Node dependency surface.
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
