{
	"name": "project-standards",
	"version": "0.0.0",
	"private": true,
	"description": "Dev tooling only (Prettier). This repo's product is the documentation standards; Node is present solely to pin the formatter.",
	"scripts": {
		"format": "prettier --write .",
		"format:check": "prettier --check ."
	},
	"devDependencies": {
		"prettier": "3.8.3"
	}
}
