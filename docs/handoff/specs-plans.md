# Specs And Plans

**Last updated:** 2026-08-30

> **Housekeeping (2026-07-19):** `docs/superpowers/` was retired — design docs moved to `docs/specs/archive/`, research to `docs/research/`, the active plan to `docs/plans/`. The implemented Python Tooling checker-table plan and its four design-review audits were deleted. `standard-bundle-authoring@2.1` was authored (contract-conformant correction of 2.0's SPEC-BA02 pointer) and release 5.0.2 was prepared (PATCH under the new internal-additive classification rule).
>
> **Plan prune (2026-07-18):** the published v5 release retains its durable specifications, decisions, implementation, and concise deployment record. Release-only replay, retained evidence, parallel-coverage preparation plans, and their review chain were removed.

| Item | Path | Status |
| --- | --- | --- |
| ADR mechanical guardrails — issue #129 | `docs/design/adr-conformance/2026-08-05-adr-mechanical-guardrails-v5.17-feature-proposal.md` | **draft starting brief** — preserves the historical v5.17.0 proposal; a formal specification still requires owner decisions and current-state re-derivation. |
| github-workflow standard package — **SPEC-GHW1** | `docs/specs/2026-08-06-github-workflow-package-spec.md`; design `docs/specs/2026-08-06-github-workflow-package-design.md`; input preserved at `docs/specs/archive/2026-08-06-github-repo-administration-preliminary-design.md` | **Approved spec rev 1.35; implementation not started.** The exact 1.6 implementation baseline was independently verified by a native verifier and headless Opus before the 1.7 amendment. The approved 1.7 target defines uniform T0-or-PR admission, Final/Supporting/Standalone governance, four-section PR contracts, shared derived validation, paired Ready/Merge/Final-disposition operations, zero-secondary-approval operator authority, no new consumer options, and repair-on-touch migration. DEV-004 is closed by the preserved design's dated amendment; known 1.6 deviations remain historical until their stated 1.7 proof exists. The prior 1.0 implementation plan was completed and retired; a 1.7 implementation plan has not been authored. |
| Open-issue resolution program | `docs/research/2026-08-01-open-issue-resolution-closeout-evidence.md` | **completed 2026-08-12** — format-3 revision 6 closed every governed issue disposition across the 5.15.0, 5.16.0, and 5.19.0 releases. The final release is published from `127bd3dd`; all 27 governed reports are terminal. The program master, execution scratch, Project Spec conformance/conversion child plans, and GitHub Workflow 1.2 guidance plan were retired after durable evidence harvest. |
| Project Spec conformance and conversion | `docs/specs/2026-08-01-project-spec-conformance-plan-input.md`; `docs/specs/2026-08-01-project-spec-conversion-plan-input.md`; `docs/research/2026-08-01-project-spec-feature-release-evidence.md` | **implemented and released in 5.19.0** — Project Specification 1.9 ships strict conformance linting and preservation-first import. The two terminal child plans were deleted under completed-plan policy after checkpoint and release evidence harvest; #62 and #55 are closed as completed. |
| Agent Handoff 1.12 documentation successor | `src/project_standards/payloads/agent-handoff/1.12/`; `CHANGELOG.md` (5.19.0); `docs/handoff/deployed.md` row `v5.19.0` | **implemented and released in 5.19.0** — `agent-handoff` 1.12 shipped the paired #140/#165 documentation cut with no runtime change: the adoption guide now states that a clean V5-native reconciliation retires the unchanged superseded managed Python launcher and refuses consumer-modified bytes with `CP-MODIFIED-MANAGED`, the false manual `git rm` step is gone, and legacy-migration step 2 names `.agents/hooks/agent-handoff/session-start` as the current confirmation target while the historical per-harness references stay byte-identical. 1.12 is `retained` in `catalogs/5.toml` behind the 1.15 default. Single-task plan (revision 1); T1 checkpoint `d874356f`. Issues #140 and #165 are closed as completed with the v5.19.0 release reference. Deliberately deferred and still unowned: whole-file retirement when the applied lock carries `created_container=False` (adopted pre-existing containers and residual V4-migration cases) — `_classify_removed` preserves the file, no guidance describes it, and removal/preservation/lock semantics need an owner decision; the Agent Handoff command-provider/Go migration was executed separately under #142. The implementation plan was deleted 2026-08-27 under the completed-plan policy after this harvest. |
| Runner-label reachability advisory | `src/project_standards/payloads/markdown-tooling/1.15/`; `src/project_standards/payloads/markdown-frontmatter/1.11/`; `src/project_standards/payloads/project-spec/1.9/`; `CHANGELOG.md` (5.19.0); `docs/handoff/deployed.md` row `v5.19.0` | **implemented and released in 5.19.0** — Markdown Tooling 1.15, Markdown Frontmatter 1.11, and the shared Project Specification 1.9 candidate emit a non-fatal, package-specific warning naming the affected caller, the unreachable cause, and a state-correct remedy whenever non-empty `runner_labels` cannot reach an enabled caller, covering both consumer-owned callers and direct self-hosted mode; empty labels and managed caller mode stay silent and Project Specification 1.9 admits the request through only the added `verify` provider-input operation. `markdown-tooling` 1.15 and `project-spec` 1.9 are the Catalog 5 defaults; `markdown-frontmatter` 1.11 is `retained` behind 1.14. Four-task plan (revision 3); checkpoints `82da50f3`, `f962c170`, `989f2038`, `a7f01cd4`. Issue #143 is closed as completed. The command-provider-execution master cited this plan's T3 anchor as operational evidence in its Authority and Source Map; that master was itself retired on 2026-08-27, so no surviving document points here. The implementation plan was deleted 2026-08-27 under the completed-plan policy after this harvest. |
| Python Tooling VS Code task prefix and missing-managed diagnostics | `src/project_standards/payloads/python-tooling/1.14/`; `src/project_standards/control_plane/planner.py` and `diagnostics.py` (`governing_options`); `CHANGELOG.md` (5.19.0); `docs/handoff/deployed.md` row `v5.19.0` | **implemented and released in 5.19.0** — Python Tooling 1.14 added the closed `vscode.task_prefix` choice (`""` and `"python: "`, default `""`), expressed as two option-gated five-task contribution sets so an omitted or empty value renders the 1.13 bytes exactly and only the five managed task labels change; the paired generic planner correction makes every missing-unit `CP-MODIFIED-MANAGED` finding carry the selected declaration's unambiguous `governing_options` tuple without changing lock identity or public schemas. 1.14 is `retained` behind the 1.16 default and the option and its `/vscode/task_prefix` governance survive there. Two-task plan (revision 1); checkpoints `ff22531e` (diagnostics) and `d16dc0f3` (payload). Issue #153 is closed as completed. Deferred by owner decision: free-form prefixes and a `vscode.tasks_ownership` option, either of which needs a new issue and approved design. The implementation plan was deleted 2026-08-27 under the completed-plan policy after this harvest. |
| ADR amendment relationship validation | `src/project_standards/payloads/adr/1.6/`; `.standards/config.toml` (`validate_amendments = true`); `CHANGELOG.md` (5.19.0); `docs/handoff/deployed.md` row `v5.19.0` | **implemented, activated, and released in 5.19.0** — `adr` 1.6 added the independent, default-off `validate_amendments` option that checks both directions of every declared `project.amends` / `project.amended_by` relationship over the immutable ADR snapshot, reporting `ADR-AMEND-ONEWAY` for a missing reciprocal member or absent target and `ADR-AMEND-SUPERSEDED` for an amendment of a superseded record; the provider reads no filesystem and infers nothing from prose, and 1.5 results are unchanged at the default. 1.6 is the Catalog 5 default and this repository dogfoods the guardrail with `validate_amendments = true`. Two-task plan (revision 5); checkpoints `cf90c88c` (unadvertised candidate) and `617bd3b7` (activation, dogfood, and final corpus qualification, including green v5.17.0/v5.18.0 tagged corpora). Issue #163 is closed as completed. Deferred by design: amendment prose, note-placement, and semantic-scope validation; metadata field-shape and duplicate-ID findings, which Markdown Frontmatter and reference validation own; and downstream consumer opt-in or corpus repair. The implementation plan was deleted 2026-08-27 under the completed-plan policy after this harvest. |
| Command provider execution | `docs/adr/adr-0030-command-provider-execution-boundary.md`; `src/project_standards/control_plane/provider_subprocess.py`; `cmd/command-provider-fixture/` and `scripts/build-command-provider-fixture.sh`; `CHANGELOG.md` (5.19.0); `docs/handoff/deployed.md` row `v5.19.0` | **implemented and released in 5.19.0** — one control-plane-owned bounded subprocess runner now serves direct CLI/control-plane dispatch and MCP services alike, preserving ADR 0025's 30-second bound, per-stream and result caps, explicit truncation, the third inherited result descriptor, `SIGTERM`-then-`SIGKILL` process-group teardown, reaping, and content-safe diagnostics; a trusted V2 payload may declare a `command` provider whose integrity-verified bytes are materialized at mode `0755` for that invocation only, on `linux/amd64`, never from an installed-wheel resource path, over a canonical UTF-8 JSON stdin envelope carrying validated provider input plus padded-Base64 declared resources with the raw result on the `argv[1]` descriptor. New [ADR 0030](../adr/adr-0030-command-provider-execution-boundary.md) records the trust boundary, wire contract, accepted portability narrowing, and both rejected alternatives, reciprocally linked to ADRs 0025 and 0027; the reproducibly built Go fixture proves the route end to end through real reconcile/validate/drift-check calls. Six-task plan (revision 12); checkpoints `2cf74539`, `78fb946b`, `fdbbd85c`, `e033260a`, plus two permanent corrections — `98565e0d` (T5, plan-bound command input authority) and `78d64d59` (T6, Git-mode classification of the two fixture artifacts in the repository-hygiene inventory). Issue #142 is closed as completed. Deferred and still unowned: migrating the Agent Handoff provider to a Go command provider — `agent-handoff` 1.15 still ships `providers/agent_handoff.py` and every declaration at `kind = "python"`, so that successor needs its own plan, parity proof, package version, catalog row, and release authority; command-provider platforms beyond `linux/amd64`, which need real consumer demand plus build, declaration, fixture, and release authority; and stronger sandboxing or privilege isolation, which the accepted trusted-payload threat model deliberately excludes. Its Authority and Source Map cited the runner-label and schema-payload-reference plans, both already retired. In the other direction, the v5.19 ADR corpus master carried this path as an Authority and Source Map row; bridge 3.5.0 does resolve `repo:` sources for existence, so that master was first moved to revision 5 through the engine's `pause`/`revise`/`replace` transaction — its only citing task T4 was already `done` at checkpoint `4f4a3644` and no task definition changed — and was then itself retired the same day under this policy, so no surviving document points here. The implementation plan was deleted 2026-08-27 under the completed-plan policy after this harvest. |
| Schema payload reference validation | `src/project_standards/package_contract/graph.py` and `repository.py`; `tests/package_contract/test_graph.py`; `standards/standard-bundle-authoring/README.md`; `CHANGELOG.md` (5.19.0); `docs/handoff/deployed.md` row `v5.19.0` | **implemented and released in 5.19.0** — `standards validate-graph` now fails with `PC-SCHEMA-PAYLOAD-REFERENCE` when any declared JSON schema in a successor payload still pins a predecessor identity, migration, endpoint, or selector that the successor manifest does not declare. The check is family-agnostic and manifest-derived rather than version-pattern heuristic, preserves valid predecessor migration edges, and replaced the four per-family opt-in assertions that had left the class blind to all five validators; the authoring landing page states the obligation at the version-cut point. Two-task plan (revision 1); checkpoints `aa5846f6` and `0eb0b965`. Issue #156 is closed as completed. Nothing was deferred beyond the parent 5.19 release work that consumed the guard: no regex or version-shaped-literal detector was added, no published payload bytes changed, and no open question survived authoring. The implementation plan was deleted 2026-08-27 under the completed-plan policy after this harvest. |
| Package version reference sweep | `scripts/release_prep.py`; `tests/test_release_prep.py`; `meta/versioning.md`; `CHANGELOG.md` (5.19.0); `docs/handoff/deployed.md` row `v5.19.0` | **implemented and released in 5.19.0** — release preparation now emits an early deterministic review report for stale package-version references in each selected family's mutable root `README.md`, `adopt.md`, and `agent-summary.md`, deriving the expected version independently per family from the candidate catalog source and naming the expected selection beside each mismatching path and line. It reports only: no document, selector, link, catalog, payload, lock, or changelog byte is rewritten, `release_consistency.py` and `packages check-release` are untouched, and the release gate remains the fail-closed authority for the complete current-reference corpus. Version comparison parses canonical package versions into numeric pairs so `1.10` outranks `1.9`, and the report is fully planned and validated before any existing write. Single-task plan (revision 1); checkpoint `de0e2079`. Issue #164 is closed as completed. Deliberately out of scope and unchanged: `standards/*/agent-summary.md` stays outside `sweep_version_references`' outgoing release-literal target set, immutable `standards/*/versions/**` payloads are never scanned, and family-shape validation stays elsewhere. The implementation plan was deleted 2026-08-27 under the completed-plan policy after this harvest. |
| Create-only stale-content advisory | `src/project_standards/control_plane/planner.py`; `tests/control_plane/test_planner.py`; `docs/adr/adr-0028-create-only-artifact-refresh.md`; `docs/handoff/bugs/006-create-only-artifacts-invisible-to-drift-check.md`; `CHANGELOG.md` (5.19.0); `docs/handoff/deployed.md` row `v5.19.0` | **implemented and released in 5.19.0** — for each selected and materialized create-only unit, the planner compares observed consumer content against the immutable content digests of every permanently advertised version of that package and emits one non-blocking `CP-CREATE-ONLY-STALE` advisory in validation and drift reporting when the bytes match a strictly earlier advertised version. Content matching the selected version, customized or unmatched content, absent content, and content matching a newer version all stay silent, and create-only ownership is untouched: no refresh command, overwrite branch, lock rewrite, or automatic update path exists, and `CP-CREATE-ONLY-ABSENT` remains permanent. ADR 0028 was amended to record the advisory as the sanctioned discovery mechanism and durable bug 006 was closed against it. Two-task plan (revision 1); checkpoints `d8d5c52f` and `37b52fa7`. Issue #157 is closed as completed. Permanent non-goals, not deferrals: `reconcile --refresh-create-only`, an `upgrade` provider operation, a per-artifact refresh option, and any advisory for customized, newer-version, provider-generated, unselected, or non-materialized content. The implementation plan was deleted 2026-08-27 under the completed-plan policy after this harvest. |
| v5.19 ADR corpus corrections | `docs/adr/adr-0025-mcp-service-and-sdk-boundary.md`; `docs/adr/adr-0026-mcp-local-read-only-transport.md`; `docs/adr/adr-0029-agents-root-allocation.md`; `docs/adr/README.md`; `CHANGELOG.md` (5.19.0); `docs/handoff/deployed.md` row `v5.19.0` | **implemented and released in 5.19.0** — the active ADR corpus now states one unambiguous authority for each of four settled concerns. ADR 0026 owns the four-segment Model Context Protocol resource URI grammar while ADR 0010 adopts it by reference and keeps its own exclusion, with the producer divergence recorded as closed by `e400f83f`; ADR 0024 stays one record and self-amends with the joint-determination rationale and a reader map, leaving `meta/versioning.md` untouched; new [ADR 0029](../adr/adr-0029-agents-root-allocation.md) allocates the platform-owned `.agents/` root, requires a new ADR and a `standard-id` key for each future artifact class, grandfathers skills, and relates reciprocally to ADRs 0016, 0021, and 0022 without amending or superseding them; and every active ADR now separates frozen evidence (`source` and More Information) from current authority (`related` and body prose). ADRs 0025 and 0026 were renamed to their short filenames in one atomic commit that updated every tracked operational and reader-facing inbound reference, with `id` values and ADR numbers unchanged and Git rather than filesystem traversal as the corpus authority. Four-task plan; checkpoints `eb5d944f`, `4d5bedaf`, `8dbdf9b1`, and `4f4a3644`, all taken at revision 3. Revisions 4 and 5 changed no task definition: revision 4 repointed the section 7 preflight to the successor `plan-authoring` helper under #178, and revision 5 repointed the retired command-provider master's Authority and Source Map row to this record. Issues #159, #160, #161, and #162 are closed as completed with the v5.19.0 release reference. Deliberately excluded and still unowned: promoting the repository-local evidence-versus-authority convention into the reusable `adr` package, which needs separately authorized package work; any ADR renumbering or `id` change; and relocation, reconciliation, or collision repair for existing `.agents/` content. The implementation plan was deleted 2026-08-27 under the completed-plan policy after this harvest. |
| Repository hygiene remediation | `tests/test_repository_hygiene.py`; `scripts/verify.sh`; `.github/workflows/`; `CHANGELOG.md` (5.14.0); `docs/handoff/deployed.md` row `v5.14.0` | **completed 2026-08-01 and released in 5.14.0** — the plan corrected every finding of the 2026-08-01 hygiene review (three high, five medium, two low) plus both approved behavior-preserving simplifications: maintained lifecycle documents were reconciled and competing stale authority removed; the local gate now selects and reuses one exact candidate wheel with hermetic orchestration tests; recurring npm maintenance and full-SHA pins for every external GitHub Action were added; accidental executable modes were removed from mutable files only, with a mode-policy test preventing recurrence; Agent Handoff optional reads and the adapter newline/offset helpers were consolidated with byte-layout parity and unchanged security semantics; and one coherent 5.14.0 candidate was restored by putting back the exact v5.13.0 catalog lineage before reconciling, then aligning README, managed harness registrations, and the owner-approved release classification. Discovered task T9 authored Markdown Frontmatter 1.8, Markdown Tooling 1.12, and Project Specification 1.6 so the action pins became authoritative managed payload content instead of root-only `CP-MODIFIED-MANAGED` drift. Legacy plan format (`status: complete`, pre-format-3, no per-task checkpoint trailers): phases landed at `ae6d2cca` (T1–T4), `1430082d` (T5–T6), `d588631b` (T9), `cb89ce24` (T7, the close-out's final content commit), and `00ebbca3` (T8); v5.14.0 published from release commit `b4be9d2e`. Harvested deviations: T6 used exhaustive boundary cases rather than adding Hypothesis, and final-gate corrections isolated nested smoke fixtures without changing production temporary-directory ownership. Nothing substantive was deferred — only push, tag, publication, and issue mutation, which are separately authorized — and no open question survived. Its source review `docs/reviews/2026-08-01-repository-hygiene-maintenance-review.md` was already removed in earlier housekeeping, and the master itself predated bridge 3.5.0 and could not be validated by it (243 format findings); it was deleted 2026-08-27 under the completed-plan policy after this harvest. |
| Durable document references optional tooling — **SPEC-GSF3** | `docs/specs/2026-07-31-durable-document-references-optional-tooling-spec.md`; `docs/plans/2026-07-31-durable-document-references-optional-tooling-plan.md` | **spec rev 0.2 (draft); plan restored at format 3 (revision 1) and READY — T1 unimplemented, no task has ever executed** — optional meta-repository main-wheel tooling; owner-approved design and Opus findings remain integrated. #178 is now resolved on both of its masters. Its other target, the v5.19 ADR corpus corrections plan, took the section 7 preflight repoint at revision 4 and has since been retired under the completed-plan policy, so this is the only master #178 still governs. For it, the master was re-authored at plan format 3 at the same path and identity (`Plan-Id: 2026-07-31/durable-document-references-optional-tooling`), preserving the approved 14-task decomposition and scope; `plan-authoring validate --no-scratch` exits 0 and `generate` projects 14 tasks with T1 ready. Spec revision 0.2 is a currency audit only: `project-toolbox` now exists (WH-004 gate restated), the 2026-07-31 corpus counts are marked as a dated measurement, the Standards references are repinned to the resolved 1.9/1.6/1.13 payloads, and IR-005 plus new OQ-001 surface the previously unstated consumer-config `schema_version` obligation for a sibling `[tools]` table. T1 is gated on the owner answering OQ-001 (plan EG-001); nothing else blocks execution. Known engine limitation: bridge 3.5.0 resolves no Appendix C `EV-###` identifier, so the three retained artifacts (dogfood baseline, cold-run benchmark, security review receipt) are enforced through task acceptance and §12 instead of the parsed evidence field. |
| V5 adoption friction correction train design | `docs/specs/archive/2026-07-22-v5-adoption-friction-correction-train-design.md` | **released (5.8.0)** — python-tooling 1.8 `pytest.test_paths`, markdown-tooling 1.8 legacy byte-form acceptance, markdown-frontmatter 1.5 `workflow_ownership`, minimal-escape frontmatter serialization converged on the pinned Prettier oracle, CLI hardening, legacy-note rewording (issues #26–#31); the implementation plan was removed under the completed-plan policy |
| V5 managed-edit fidelity correction train design | `docs/specs/archive/2026-07-22-v5-managed-edit-fidelity-correction-train-design.md` | **released (5.7.0)** — python-tooling 1.7 per-root coverage scoping and anchored TOML comment preservation (issues #24–#25); the implementation plan was removed under the completed-plan policy |
| Consumer Standards Control Plane — **SPEC-CP01** | `docs/specs/2026-07-10-consumer-standards-control-plane-spec.md` | **approved rev 0.15; implemented; decisions accepted as ADRs 0023-0024** — live-root dogfood, FR-037/FR-038, declared-path provider integrity, scale, and complete integrated evidence pass. |
| Root-artifact ownership and semantic composition design | `docs/specs/archive/2026-07-10-root-artifact-ownership-semantic-composition-design.md` | **approved; adopted by ADR 0023** after adversarial review converged in round 2 — consumer-owned shared containers, typed package-owned semantic units, reference-counted shared contributions, syntax-preserving adapters, and no precedence; prerequisite input to `SPEC-BA02` |
| Consumer control-plane architecture decisions | `docs/adr/adr-0023-unified-consumer-standards-control-plane.md`; `docs/adr/adr-0024-catalog-scoped-package-version-channels.md` | **accepted 2026-07-10** — supersede ADRs 0003, 0008, 0017, and 0020; reconcile the retained lifecycle, provenance, packaged-skill, packaged-hook, frontmatter, and versioning decisions before `SPEC-BA02` and implementation |
| Standard Bundle Authoring V2 — **SPEC-BA02** | `docs/specs/2026-07-10-standard-bundle-authoring-v2-spec.md` | **approved rev 0.15; implemented** — the immutable family/payload authoring contract includes the bounded direct package-config transform declaration used by Standard Bundle Authoring 2.6. |
| Python Tooling checker-table materialization design | `docs/specs/archive/2026-07-12-python-tooling-checker-table-materialization-design.md` | **implemented** — canonical checker selection, exact `pyright==1.1.411`, conditional payload rendering, and migration/lifecycle contracts remain part of the released feature. Release-only proof details are historical context, not an active gate. |
| MCP meta-repo readiness prep — **SPEC-MT01** | `docs/specs/2026-07-07-project-standards-meta-repo-mcp-readiness-spec.md` | **approved rev 1.4; complete 2026-07-12; factual sync 2026-07-27** — preserves accepted Step 07 evidence and records the current 5.9.0 V2 package/control-plane successor state. |
| MCP enablement roadmap — **SPEC-RD01** | `docs/specs/2026-07-07-project-standards-mcp-enablement-roadmap-spec.md` | **approved and locked rev 1.8; read-only server delivered in 5.12.0** — Steps 08-through-14, ADRs 0025/0026, and exact `mcp==2.0.0` yielded the local dual-era stdio server; controlled-write and remote-transport phases remain separately approved future work. |
| MCP server implementation — **SPEC-MS01** | `docs/specs/2026-07-07-project-standards-mcp-server-implementation-spec.md`; `docs/mcp-server.md` | **implemented and released in 5.12.0** — the documentation reconciliation relocked the MCP specifications before implementation; the completed delivery ships the read-only dual-era stdio server, three `standards://` resource forms, and six tools. |
| V5 adoption integrity correction train — **SPEC-VAIC** | `docs/specs/2026-07-26-v5-adoption-integrity-correction-train-spec.md` | **implemented and released in 5.9.0; approved rev 0.12 SHA-256 `9fe629b29f2d86078798ab8e9f01feec9cba324dd53f6a7759be10e6ceb4f64d`** — Option 1 delivers the bounded generic direct package-configuration transform; every scoped task, review, exact-candidate qualification, publication, and issue-closure gate passed. The completed plan was removed under the completed-plan policy. |
| V5 validation fidelity correction train — **SPEC-VFCT** | `docs/specs/2026-07-27-v5-validation-fidelity-correction-train-spec.md` | **implemented and released in 5.10.0; approved rev 0.3 SHA-256 `ba44fb8f09393d80e2c362183001fa0f5b69b126351ea0b6ea2b690403c2dcb6`** — shared specification-engine, `standards show`, migration-hint, and create-only planning corrections plus Python Tooling 1.10, Markdown Tooling 1.10, Agent Handoff 1.6, and Markdown Frontmatter 1.6 close issues #50–#54, #56–#61, and #63–#69. The train was implemented in-session directly against the specification; no separate plan document was authored. |
| Usage Documentation Site V2 design and preserved draft set — **SPEC-U000…U007** | `docs/specs/2026-08-02-usage-documentation-site-v2-design.md`; `docs/specs/usage-documentation-site/` | **approved design; v5.16.0 specification authoring queued** — the new design governs new specifications with new identities; preserve the original seven-spec bundle and transcript unchanged as historical input; include a compatible CLI Documentation successor before implementation planning |
| Self-dogfooding version skew prior art | `docs/research/2026-08-04-self-dogfooding-version-skew-producer-mode.md` | **active; confidence medium** — tests the producer-role decision against compiler bootstrapping, self-linting tools, package managers, and schema registries. Records the rejected always-advancing dev-version alternative: `ParsedToolRelease` requires canonical SemVer, so it costs more here than the header schema bump |
| MCP specification reference pack | `docs/research/2026-07-07-project-standards-mcp-specification-reference-pack.md` | **active; rev 0.4, reviewed 2026-07-28** supporting material for the three MCP specs. The final 2026-07-28 protocol and `mcp==2.0.0` selection are recorded; evidence authority lives in the 2026-07-28 protocol/SDK/client matrix. |
| SPEC-MT01 Step 00 — Baseline inventory | `docs/research/2026-07-07-spec-mt01-baseline-inventory.md` | **done 2026-07-07** — factual map of current standards/registry/bundles/CLI/workflows/tests + the grounded readiness-gap list feeding Steps 01–07. First completed v5.0.0 work item |
| SPEC-MT01 Step 01 — ADR foundation | `docs/adr/` (`adr-0001`…`adr-0013` + `README.md` index) | **done + accepted 2026-07-07** — 13 meta-repo readiness ADRs, now `status: active` (MADR accepted); `docs/adr/**` dogfood-wired into frontmatter/id/section validation. SPEC-MS01 server ADRs deferred to the server phase |
| SPEC-MT01 Step 02 — Standard Bundle Authoring (spec) | `docs/specs/archive/2026-07-07-standard-bundle-authoring-standard.md` (**SPEC-BA01**, Light) | **implemented 2026-07-07; superseded by approved SPEC-BA02 on 2026-07-10.** Bundle and completed plan remain implementation history until V2 execution replaces them. |
| SPEC-MT01 Step 03 — `standard.toml` manifest (design) | `docs/specs/archive/2026-07-07-standard-manifest-schema-model-design.md` | **IMPLEMENTED 2026-07-08** (`89464ea`…`44c290e`) — Codex spec-review converged r2. Delivers the `standard.toml` Pydantic model, `load_standard_manifest` loader, generated + drift-tested `standard.schema.json`, and the valid/invalid fixture corpus. CR-004 machine-layer diff empty (no `registry.json`/bundles/`spec.include` touched) |
| SPEC-MT01 Step 04 — Standards graph validator | `src/project_standards/standards_graph/` | **implemented 2026-07-09** (`8d23a0c`) — graph loader, authority/capability/resource/provider/config/relationship validators, hidden-dependency rejection fixtures, current-repo smoke tests, and `project-standards standards validate-graph` CLI. |
| SPEC-MT01 Step 05 — Existing-standard manifest retrofit | `standards/` | **implemented 2026-07-09** (`82d1978`, review fix `ce11a54`) — all nine bundles ship graph-validated family manifests with authorities, resources, providers, and relationships. |
| SPEC-MT01 Step 06 — Dogfood catalog and artifact linkage | `standards/catalog.md` | **implemented 2026-07-09** (`39b9f76`) — explicit manifest links, typed provenance, graph parity, package composition fixtures, and freshness enforcement. Step 07 passed on 2026-07-12. |
| SPEC-MT01 pre-Step-07 readiness remediation design | `docs/specs/archive/2026-07-10-pre-step-07-readiness-remediation-design.md` | **implemented 2026-07-10** (`70b20ee`…`342a802`) — traceability/release evidence reconciled, bug-index shape targeting fixed, repository-only graph/catalog CI added, and the 1,373-test gate passed. Step 07 passed on 2026-07-12. |
| SPEC-MT01 FR-013 agent-summary coverage design | `docs/specs/archive/2026-07-10-fr-013-agent-summary-coverage-design.md` | **implemented 2026-07-10** (`529ec72`…`9463dcb`) — nine compact summaries, 3,000-byte enforcement, generated catalog URIs, additive package-version changes, and FR-013 Passing. |
| Catalog 5 MCP-exposure review | `docs/research/2026-07-12-catalog-5-mcp-exposure-review.md` | **passing 2026-07-12** — all nine packages have validated generic exposure metadata; Python Tooling's missing canonical-summary backlink was fixed pre-release and a catalog-wide regression test now enforces the contract; 494 package-contract tests pass |
| Project Standards 5.1 review remediation | `docs/specs/archive/2026-07-19-project-standards-review-remediation-design.md` | **implemented and published in 5.1.0** — all 100 findings have final dispositions; 96 accepted or adjusted corrections are complete, four findings are closed with no change, and exactly nine payloads were added. Signed tags and byte-verified release assets are live from release commit `b69600d`. |
| Agent Handoff standard package — **SPEC-DPEY** | `docs/specs/2026-07-09-agent-handoff-standard-package.md`; closeout evidence `docs/research/2026-08-01-open-issue-resolution-closeout-evidence.md` (the program plan was deleted 2026-08-09 under the completed-plan policy) | **approved rev 1.2 (2026-08-27 currency revision); Agent Handoff 1.8 released in v5.14.0, current default per `docs/handoff/deployed.md`** — the adaptive Claude Code and Codex SessionStart launcher fixes issue #80. T1 closed the tracker from published evidence and T32 closed the legacy retirement on 2026-08-04; both outcomes are durable in the retirement inventory. The SessionStart launcher implementation plan was deleted 2026-08-09 under the completed-plan policy (superseded by agent-handoff 1.9/1.10; see `CHANGELOG.md`). |
| CLI Documentation standard design | `docs/specs/archive/2026-07-07-cli-documentation-standard-design.md` | approved (codex r3); **RELEASED in `v4.3.0`** (2026-07-07) |
| Markdown-Tooling formatter authority (issue #3 F5 — Spec B) design | `docs/specs/archive/2026-07-06-markdown-tooling-formatter-authority-design.md` | **RELEASED in `v4.2.0`** (opt-in reusable Prettier gate; `markdown_tooling 1.1`; DEC-9→DEC-10) |
| spec-validator external references (issue #3 F1–F4) design | `docs/specs/archive/2026-07-06-spec-validator-external-references-design.md` | **RELEASED in `v4.1.0`** (`spec.reference_prefixes` + token hygiene) |
| project-spec tooling Spec #1 design | `docs/specs/archive/2026-07-04-project-spec-tooling-design.md` | approved (codex r2); plan implemented (`2a6c4c0`) |
| project-spec tooling Spec #2 design (`new` scaffold) | `docs/specs/archive/2026-07-04-project-spec-tooling-spec2-design.md` | approved (codex r2); plan implemented (`8d48c22`) |
| project-spec tooling Spec #3 design (`upgrade` tier promotion) | `docs/specs/archive/2026-07-05-project-spec-tooling-spec3-design.md` | approved (codex r3); plan implemented (`testing`) |
| Frontmatter suite (format/references/fix) design | `docs/specs/archive/2026-06-08-frontmatter-suite-design.md` | approved (codex r3); plan implemented (shipped 3.0.0) |
| `check` drift design | `docs/specs/archive/2026-06-08-check-drift-design.md` | approved historical v2.2 design; superseded for catalog 5 by the CP01/V2 control plane (`reconcile --check`, central-lock convergence, transactional repair, and package drift providers) |
| `adopt` CLI design | `docs/specs/archive/2026-06-08-adopt-cli-design.md` | approved (codex r6); plan implemented (shipped 3.0.0) |
| Markdown Tooling Standard design | `docs/specs/archive/2026-06-06-markdown-tooling-standard-design.md` | approved (audit r3); plan released (`v2.0.0`) |
| Per-standard versioning design | `docs/specs/archive/2026-06-06-per-standard-versioning-design.md` | approved (audit r2); plan released (`v2.0.0`) |
| Standards bundle restructure design | `docs/specs/archive/2026-06-06-standards-bundle-restructure-design.md` | approved; plan released (`v2.0.0`) |
| Python Tooling SSOT adoption design | `docs/specs/archive/2026-06-06-python-tooling-ssot-adoption-design.md` | approved; plan released (`v2.0.0`) |
| Handoff v3 migration design | `docs/specs/archive/2026-06-05-handoff-v3-migration-design.md` | approved; plan implemented |
| Linting/formatting stack (DEC-1…9 trail) | `docs/specs/archive/2026-06-04-linting-formatting-stack.md` | implemented (shipped in v2.0.0; originally targeted an unreleased 1.3.0) |

## Storage

- Maintained Project Specification documents: `docs/specs/`
- Historical design artifacts: `docs/specs/archive/`
- Research / reference support docs: `docs/research/`, indexed by `docs/research/index.md` (consolidated 2026-07-19; the former `docs/superpowers/research/` split is retired and the index is maintained by hand)
- Active implementation plans live under `docs/plans/`; completed and superseded plans are pruned after their outcomes have durable owners.
- `docs/specs/README.md` — maintained specification index
- `docs/specs/archive/README.md` — historical design index
