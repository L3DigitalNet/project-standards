# Sessions — 2026-07

| Date | Headline | Commits | Bugs |
| --- | --- | --- | --- |
| 2026-07-01 | Handoff system resynced to engine v3.4: hooks + `.codex/config.toml` byte-matched, root `STATUS.md` created, `TODO.md` heading renamed, `specs-plans.md` backfilled; msgpack bumped for GHSA-6v7p-g79w-8964 | `7e2cdf2`…`a81c301` | — |
| 2026-07-01 | **Python Tooling standard review + fixes** (user-requested review; all 11 findings fixed). 🔴 §15 `check.yml` scaffold was **invalid YAML** — tab-indented (the shared `.editorconfig`'s Markdown-tab default leaked into the fence; Prettier silently skipped the unparseable block) so the manual copy-adopt path failed at workflow parse. Repaired with spaces, made byte-identical to the bundle artifact behind a bare `<!-- prettier-ignore -->` (verified Prettier's `embeddedLanguageFormatting` otherwise rewrites fence quote style), drift tests added → conventions §9. 🟠 ruff floor `>=0.9.0` couldn't run the shipped config (`target-version = "py314"` rejected until 0.12, non-preview at 0.14 — verified against 0.9/0.12/0.13/0.14) → `>=0.14` in §6/fragment/repo. 🟠 adopt CLI silently omitted §13-mandated `.vscode/settings.json`+`tasks.json` → new bundle artifacts, delivered + tested end-to-end. 🟡 dead `pytest-cov` dropped everywhere (second coverage entry point violated §3); §6 dev group synced with fragment + semantic drift test; §15 policy reworded to match its own SHA-pinned template + `permissions: contents: read` rule + direct-commit `push.branches` caveat; §25 retitled, missing 2026-06-12 entry backfilled; status paragraph → list; §18 "optional" clarified. Stale 3-failing-tests report disproven (all pass). 587 tests, 100% cov, full gate + dogfood green. CHANGELOG items added to `TODO.md`. | `752ad32`…`a2c518b` | — |
| 2026-07-01 | **Markdown standards swept for the python-tooling YAML defect class** (user-requested follow-up). No tab-broken YAML anywhere in `standards/` (all yaml fences parse; `---` "failures" are multi-doc frontmatter examples, verified with `safe_load_all`). Latent drift found + fixed: 🟠 frontmatter adopt.md §2 starter example had **dropped `**/*.template.md`** vs the delivered starter (manual adopters got a config that validates the ADR template) and Prettier had rewritten its quotes; 🟡 §3 caller example drifted from delivered `validate-standards.yml` in quote style only; 🟡 md-tooling §6 prettierrc fence byte-identical but unguarded. All three fences now byte-locked to bundle artifacts behind bare `<!-- prettier-ignore -->` (conventions §9); doc-only extras (`.obsidian/**`, references opt-in) moved to prose. New tests: starter/caller/prettierrc fence↔bundle equality (caller rendered via `major_ref()` so a v4 bump forces doc updates), no-tabs-in-yaml-fences repo-wide over `standards/`, and every partial snippet's `uses:` must name an existing workflow at the current major. md-tooling adopt manifest verified complete vs its documented deliverables (no `.vscode`-gap analog). 591 tests, 100% cov, full gate green. | `4c8543e` | — |
| 2026-07-04 | **`project-spec` (5th standard) README authored** (in-dev draft; §1–§10 written except §6 Adoption, parked pending tooling). Guarantee-driven design: §3 Features → 8 guarantees (G1–G8); §5 Tooling → capability set (`validate`/`lint`/`extract`/`next`/`new`/`upgrade` core + `status` planned + agent review contract), read-only + guarded authoring, each traced to a guarantee. Purpose/Scope (specs use own frontmatter schema, independent of Markdown Frontmatter standard), Templates, Exceptions (spined on "does the tooling refuse it?"), Update-cadence, and Source register written. Source editions verified live (29148:2018, 1016-2009, 42010:2022, MADR 4.0.0 current; OpenAPI now 3.2.0). Found + fixed **3 pre-existing red gates** the project-spec commits (453df7f/d24f276) introduced un-gated: dogfood frontmatter (excluded `standards/project-spec/**` in `.project-standards.yml`), markdownlint (dead anchor + fence in tooling-notes), ruff (check_specs.py E741/C416/RUF001/003). Full gate green after. | `838b8cb`…`1be1b0b` | — |
| 2026-07-01 | **Full cross-standard consistency review** (user-requested: internal + between-standards; adopting one must never force an ADR for another). Empirical proof first: `adopt markdown-frontmatter adr markdown-tooling python-tooling` into one repo → 15 files, zero collisions, shared artifacts delivered once; combined result passes markdownlint + prettier with the delivered configs and `project-standards validate` exit 0 once fragments merged as instructed (an earlier "duplicate `markdown:` key" scare was self-inflicted comment-stripping — the CLI's fragment output includes correct merge guidance; retracted). Fixed: 🟠 the one true adopt-A-forces-ADR-for-B conflict — python-tooling §3 banned `pre-commit` while frontmatter adopt.md ships six pre-commit hooks → scope note (prohibition targets duplicate *Python* gates; frontmatter hooks sanctioned, mirror note in adopt.md); 🟡 frontmatter README Tags section still quoted the pre-F37 pattern `^[a-z0-9][a-z0-9-]*$` vs schema's `^[a-z0-9]+(-[a-z0-9]+)*$`; 🟡 7 frontmatter templates' `id: 'replace-with-stable-id'` taught nothing about the enforced `{doc_type}-{base36}-{slug}` format (ADR templates already teach theirs) → doc_type-correct hints; 🟡 md-tooling adopt.md lacked steps for the §10 VS Code formatter blocks and §12 agent block (combined adopters got a Python-only `AGENTS.md`/`settings.json` with no pointer) → steps added; 🟢 status headers normalized to list form (md-tooling/adr/python-coding); python-coding draft: §4 SHOULD/MUST aligned, §8 `BaseException` cleanup carve-out its own example relied on, 06-12 audit entry backfilled (subagent cross-scan vs python-tooling: zero toolchain contradictions). Clean: all relative links + anchors across standards/meta/README; ADR interop (filenames/MADR sections/status mapping) consistent across all three exceptions processes; meta/versioning.md + registry + standards index agree; adr fence↔fragment semantic drift test added. 592 tests, 100% cov, full gate green. | `11af02d` | — |
| 2026-07-04 | **project-spec tooling Spec #1 — brainstormed → spec → plan, both codex-converged.** Design (`docs/superpowers/specs/2026-07-04-project-spec-tooling-design.md`): a `src/project_standards/specs/` subpackage exposing read-only `project-standards spec validate\|lint\|extract\|next` over a shared registry core parsed from the bundled templates; decomposed v1-core along the read/write seam (`new`/`upgrade` → Spec #2); `check_specs.py` retired into the pytest gate; `spec:` config block for discovery; frozen `--json` contract; reusable `validate-specs.yml`. Codex spec-review converged r2 (8 findings: consumer-validate vs template-maintainer sentinel split, non-vacuous discovery, integrity-vs-advisory rule placement, real-`spec_id` pattern, Light traceability, frozen JSON, guarantee reconciliation). 12-task TDD plan (`plans/2026-07-04-project-spec-tooling-spec1.md`); codex plan-review converged r3 (16 findings across 3 rounds: def-vs-ref uniqueness, PyYAML frontmatter parsing, discovery corpus-fallback, workflow self-repo split, SpecParseError boundary, no-verb exit 2, non-UTF-8 exit 1, DEV- uniqueness, Must-only traceability via Priority column, explicit-path exclude bypass, strict-typed primitives, full `--json` tests, built-wheel test). Docs-only session; no code changed. **Implementation delegated to Codex (parallel session).** | `403ea7c`, `9119039`, `47f5e04` | — |
| 2026-07-05 | **project-spec tooling Spec #1 implemented:** `src/project_standards/specs/` registry/document/config core + read-only `project-standards spec validate\|lint\|extract\|next`, `spec:` discovery, JSON/exit-code tests, reusable `validate-specs.yml`, bundled-template wheel smoke test, and maintainer pytest replacement for retired `check_specs.py`; test fixtures excluded from markdownlint/Prettier gates as generated parser inputs. Verified: ruff format/check, basedpyright, 651 pytest tests, coverage 98%, pip-audit, markdownlint, Prettier, dogfood frontmatter. | `2a6c4c0` | — |
| 2026-07-05 | **project-spec Spec #3 `upgrade` — brainstormed → spec → plan, both codex-converged; + validator GFM fixes + CI fix.** Design (`specs/2026-07-05-…-spec3-design.md`) + 10-task TDD plan (`plans/…-spec3.md`) for additive tier promotion Light→Standard→Full: preview-first `-i`/`-o` writes; template-faithful **source-as-spine** splice (author bytes verbatim; missing units + filler from the target-tier template); **upgradeability precheck** `check_upgradeable` (reshape-to-own-tier identity) that refuses `validate`-clean-but-non-canonical sources — the design's original "no gap prose" invariant was **empirically disproven by codex** (validate only gap-checks top-level sections), so it became an enforced precondition; Appendix A/B/D template-owned; U3 byte-exact fixture round-trip oracle; three fail-closed gates (validate + precheck + output self-validate); no `--config`. Codex spec-review converged r3 (SA-001..006 + SA-NEW-001..003: gap-prose/subsection/Appendix A-B-D ownership, config removal, package README); plan-review converged r3 "minor" 0-blocking (CR-001 strict-H1 precheck, CR-002 CI gap, CR-003 CommonMark fence-aware segmentation). **Mid-review, shipped two code fixes:** (1) `/code-review high spec` surfaced 9 defects in the shipped `validate/lint/extract/next` machinery — fixed the 3 🔴 GFM false-rejections TDD (SV-TABLE inline/escaped pipes, SV-ANCHOR GitHub repeated-heading `-1` anchors, `_OMIT` em-dash ranges; 3 tests, 723 green), 6 🟡/🟢 queued to `TODO.md`; (2) pre-existing **CI red** — `validate-specs.yml` ran `spec validate` with no `spec:` block (exit 2) → gated this-repo steps on the block's presence (auto-enables at registration). `upgrade` **not yet implemented** — next: execute subagent-driven. | `d9eb54c`…`300e4e3` | — |
| 2026-07-05 | **project-spec Spec #3 `upgrade` IMPLEMENTED** (subagent-driven execution of the 10-task TDD plan; final whole-branch review = merge-ready). Additive tier promotion Light→Standard→Full: pure source-as-spine splice core (`upgrade.py`) + CLI shell (`_run_upgrade`) — `SRC --to {standard\|full}`, `-i`/`-o`/preview, three fail-closed gates (source-invalid → upgradeability precheck → output self-validate), additive-only tier direction, full `--json` contract; shared `_safe_atomic_write` extracted from `new`. **U3 byte-exact round-trip oracle green** (light→std, light→full, std→full all byte-identical + output-validates). Notable judgment calls: 🔴 **normalized 3 tier-variant Appendix-C cross-refs** out of the standard/full templates' shared sections (§9/§12.2/§19 MS-2) so the shared-section-tier-invariant property holds (user-approved) — bundled `src/` template copies re-synced; 🟡 **§17.1 (Definition of Done) design refinement** — it is legitimately tier-variant boilerplate (light's DoD can't reference §17.3/§18.7/§13.6), so the merge reconciles it *from the target* like Appendix A/B/D (`_TEMPLATE_OWNED_SUBS`); 🟡 fixed two latent bugs the oracle surfaced — `_block_key` collapsed subsection keys (§7.1→`"7"`), and stale whole-section omission notes rode on source-kept last subsections (`_swap_tail` now reconciles kept-subsection trailing filler); 🟡 one review Critical (`_is_filler` over-trim) adjudicated down — guarded system-wide by the reshape-identity precheck (`check_upgradeable` refuses non-canonical scaffolding rather than silently losing author bytes) + proving test. Rebased onto `origin/testing` (trivial `.vscode` commit `ac87e26`) and pushed. Full gate: 781 passed, 98% cov, basedpyright 0/0/0, ruff + pip-audit + dogfood 16/16 clean. | `904d209`…`b84103a` | — |
| 2026-07-05 | **`project-spec` (5th standard) REGISTERED; deferred bugs/gaps closed; CHANGELOG resolved to MAJOR.** Fixed 3 🟡 code-review bugs (`next_id` width-exhaustion + tier-default mismatch, `lint` case-fold) + 2 🟢 cleanups (O(n²) line scan → `bisect`, duplicate appendix/prefix regexes consolidated into `registry.py`), all TDD'd. Fixed 3 residual `format-frontmatter`/`cli.py` gaps (typo'd `--config` silent-default, non-UTF-8 traceback, misleading `--glob` help text). Wrote `standards/project-spec/adopt.md` in full (discovered the reusable `validate-specs.yml` CI workflow already existed, undocumented); registered the standard — `spec:` config block live, real frontmatter added to README/adopt.md/resources, a dogfood example (`examples/spec.example.md`) added and locked in by a new test, top-level `README.md`/`standards/README.md`/`meta/versioning.md` updated. Caught a real gap along the way: a maintainer test assumed every standard's `examples/` used canonical frontmatter — scoped it and added an equivalent `spec validate`/`lint` dogfood test. CHANGELOG `[Unreleased]` fully itemized: `project-spec` registration classified MINOR; six validator/config strictness bumps + the Python Tooling ruff-floor raise verified (via a dispatched fork) and classified MAJOR each on their own (previously-passing rule, no judgment call); `pytest-cov` removal's classification resolved by reading its actual removal commit — confirmed never load-bearing, PATCH. **Next release off `testing` is MAJOR (v4.0.0).** Full gate: 790 tests, 98% branch cov, ruff/basedpyright/pip-audit/dogfood clean throughout. | `8616fc8`…`d1e0847` | — |
| 2026-07-05 | **Docs pruned for v4.0.0 release prep** (user-requested). Deleted 53 docs: `docs/codex-reviews/` (31 completed spec/plan/code review rounds), `docs/fable-findings.md` (55/58 implemented, outcome in this log), `docs/python-backend.md` (resolved scratch/advisory distribution notes), and every implemented plan under `docs/superpowers/plans/` + the `v1.1.0/` planning archive (11 files) — implementation verified per-plan against CHANGELOG/state/code by a parallel audit. **Kept:** all specs (durable design record) and `2026-06-08-check-drift.md` (pending 2.2.0 feature, never built — no `check.py`/`lock.py`/`check` subcommand). Distribution folders audited by a second parallel agent: `src/.../bundles/` has zero orphans (every file named by an `adopt.toml`); `standards/` templates/examples all realize documented `doc_type`s (incl. the `index` `repo-pages/` scaffold) — nothing removed. `specs-plans.md` pointer table pruned (deleted-plan rows dropped, implementation status folded into design rows). Dogfood `validate-frontmatter` green (19 files). | `9bf50be` | — |
| 2026-07-05 | **Release-readiness audit + fixes for v4.0.0** (user-requested, two-workflow multi-agent). Audit workflow (13 auditors × per-finding adversarial verify, 33 agents) → **18 verified findings, 0 false positives**; four products audited clean (frontmatter, ADR, python-tooling, adopt-manifests). Fix workflow (10 agents, Sonnet doc-workers + Opus for the code/changelog/versioning work). 🔴 **`spec upgrade` release blocker**: failed SV-ANCHOR self-validation on *every* tier-increasing upgrade of an unmodified `spec new` scaffold — Appendix D is `_TEMPLATE_OWNED` so its heading/slug is swapped to the target tier, but the kept source preamble's `[Appendix D](#appendix-d-…)` cross-ref went dead. Fixed in `upgrade.py` (`upgrade_text` re-points every Appendix D anchor to the target slug; identity no-op same-tier so `check_upgradeable`'s precheck is untouched) + real-scaffold regression tests for all three pairs **plus a precondition guard** — the old hand-maintained `upgrade_light.md` fixture omitted the preamble link, which is exactly why the bug shipped uncaught. Drift fixes: AGENTS/CLAUDE "four"→**five** standards; `src` README `spec extract`/`next` signatures + `--strict`; top README project-spec consumption clause; CHANGELOG `### Fixed` (validate-references semantics, format-frontmatter, doc-consistency — release stays MAJOR); `meta/versioning.md` checklist hardened (on-`main` precondition, `@vN` bump step, `UPGRADING.md` rewrite step, project-spec version row); md-tooling MD060/Prettier rationale; python-coding "contract version"→"version"; validate-specs.yml stale comment. Caught + reverted a stray `.vscode/settings.json` edit an agent made outside its assignment. **Deferred to the release commit** (now mandated by the checklist): all `@v3`→`@v4` pin bumps + `UPGRADING.md` v3→v4 rewrite. Left `TODO.md`'s MD060 item for the user (personal list). Gate: 795 tests, 98% cov, basedpyright 0/0/0, ruff + dogfood 19/19 + pip-audit clean. | `38b7980` | project-spec `spec upgrade` dead-anchor |
| 2026-07-05 | **Release-readiness pass 2 + fixes** (user asked for one more workflow pass before any release cut). Second differently-angled audit (13 agents: adopter-simulation, whole-repo link/anchor integrity, template/example self-validation, CLI edge-case exerciser, handoff-doc accuracy, regression-check on today's commits, completeness-critic) → **6 verified findings, 0 false positives** — genuinely new ground the doc-focused pass 1 couldn't reach. 🟡 **`spec extract "Appendix "` IndexError crash** — `startswith("appendix ")` accepts a trailing-space-only selector but `split()[1]` has no element; degraded to a clean no-match + regression test (a second broken-happy-path bug in the same class as pass 1's upgrade anchor). 🟡 **`validate-specs.yml` consuming-repo path ran `spec validate`/`lint` unconditionally** — a v4 consumer wiring the reusable workflow without a `spec:` block would hard-fail on the exit-2 vacuous-run guard; mirrored the this-repo `has_spec` detection onto the consumer branch (the reframed comment now matches the code). 🟢 added the ADR bundle↔`standards/adr/templates/adr.md` byte-identity guard (only bundled template lacking one). 🟢 refreshed `docs/handoff/architecture.md` component graph (project-spec bundle, `specs/` engine + full spec CLI, corrected `cli.py` subcommands, `validate-specs.yml`). Also earlier deleted the resolved MD060 `TODO.md` line (user-approved). Gate: **796 tests**, 98% cov, basedpyright 0/0/0, ruff + dogfood 19/19 + pip-audit clean. **Repo is audit-clean and staged for v4.0.0; release not yet cut.** | `206e97e`, `e2471b9` | project-spec `spec extract` letterless-appendix crash |
| 2026-07-05 | **v4.0.0 RELEASED on `main`.** Final release-readiness confirmation, then the full `meta/versioning.md` checklist executed: `testing` fast-forwarded to `main`; release commit `c7c2fd8` carried the `standards-ref` defaults `v3`→`v4` (both reusable workflows), every `@v3`→`@v4` doc pin (README, all five `adopt.md`s, markdown-tooling README, build-backend, versioning.md examples — project-spec `adopt.md` gained its "available only from v4.0.0" banner; versioning.md's one-time special case rewritten as a durable rule), `UPGRADING.md` rewritten as the v3→v4 runbook (new id `runbook-p5m7nf-…`), pyproject `4.0.0` + `uv.lock`, and CHANGELOG `[Unreleased]`→`[4.0.0]` with consumer migration notes. Signed tags `v4.0.0` (immutable) + `v4` (moving, new) pushed; `v3` frozen at `e69ab6b`; GitHub release published. **Gate caught real pre-release drift:** two spec-design docs failed Prettier + 60× markdownlint MD049 — `testing` never runs format/lint CI (push triggers `main`-only) — fixed `a2fe444` before the release commit; also reverted a stray hook-artifact `- [ ]` in `TODO.md`. Post-push: all 5 CI workflows green on `main`. Handoff closeout: deployed.md v4 rows + v3 freeze, state/STATUS/TODO updated, `testing` re-synced. Full gate: 796 tests, 98% branch cov, basedpyright 0/0/0, ruff, pip-audit, dogfood 19/19 + spec validate, markdownlint 0 errors, Prettier clean (tracked files). | `a2fe444`, `c7c2fd8` | — |
| 2026-07-05 | **hw-radar branch protection research, documented in `TODO.md` (docs-only, no code changed).** Queried hw-radar's live GitHub config (`gh api .../branches/main/protection` + `.../rulesets`): classic protection on `main` only (`dev` unprotected) — required status checks `check`+`dependency-review` (strict), PR required (0 approvals, no CODEOWNERS), required commit signatures, `enforce_admins`, required conversation resolution, no force-push/delete. Compared against project-standards' `main` (currently unprotected; 5 CI job names: `check`/`prettier`/`lint`/`validate`/`validate-specs`; no dependency-review equivalent — no locked runtime deps to license-gate). Wrote a 7-step adoption plan into `TODO.md`'s Agent Tracked Tasks, gated on **user go-ahead** and timed for **the next major release**, coordinated with that release's first PR merge to `main`. Also tightened the `TODO.md` template's usage-and-maintenance example (8 bullets → 5). | `db64ea4` | — |
| 2026-07-06 | **Spec B (F5 markdown-tooling formatter authority) — brainstormed → spec → plan, both codex-converged; docs-only, no code.** Resolves issue #3 F5 (Prettier shipped+recommended but unenforced for consumers; no documented authority). Brainstorm surfaced the pivotal design turn: reading `standards/markdown-tooling/README.md` §4–§7 exposed **DEC-9** — Prettier is *deliberately* repo-local/not-shipped — which the first spec (bolt Prettier onto `lint-markdown.yml`, on-by-default, MAJOR v5) would have silently reversed **and** under-enforced (the Prettier config is repo-wide md/json/jsonc/yaml, not md-only). Codex's mechanics review had already "converged" that spec (r1–r3) and never flagged the DEC-9 conflict — a repo-philosophy issue, not a syntax bug. Redesigned per user: ship an **OPT-IN reusable repo-wide Prettier gate** (generalize `format.yml` to dual-role `workflow_call` + new bundle `format.caller.yml`), formally **supersede DEC-9 via a new DEC-10**. Because a consumer only gets the gate by adopting the new caller (`adopt` skips existing files; `@v4` + `lint-markdown.yml` untouched), it's **additive ⇒ MINOR** (`markdown_tooling 1.0→1.1`, tool `v4.x`) — not the MAJOR the bolt-on required. Folded in a repo-local **coherence tool** (`tests/coherence/`: split-ownership declaration extending the existing `CUSTOMIZATIONS` dict + hermetic conformance/pin checks + Node-subprocess behavioral corpus, skip-guarded like the packaging tests). Codex spec-review **5 passes** (r1–r3 bolt-on, r4–r5 redesign; final "no significant findings"): notable catches = coercion-unsafe boolean `if` (`false != 'false'` → `0 != NaN` → true), job-level opt-out so `prettier:false` skips checkout/setup-node too, `package-lock` regen + `npm ci` install model, stale "no Prettier workflow" claims in root README/CLAUDE/AGENTS. 10-task TDD plan; codex plan-review **4 passes** (final "no significant findings"): every finding was a defect in the plan's *own test code* — behavioral test loaded the wrong Prettier config (temp file outside repo → config-discovery miss), bare `dict` annotations failing basedpyright strict, a `"npm ci" not in text` assertion a comment breaks, and a vacuous CUSTOMIZATIONS drift-guard (string filter always empty). Implementation NOT started; release DEFERRED with F1–F4. Full 9-pass audit trail in `docs/codex-reviews/`. | `eab41b1`…`da01e97` | — |
| 2026-07-06 | **Spec B (F5) IMPLEMENTED via executing-plans → REVIEWED → v4.1.0 + v4.2.0 RELEASED (full arc in one session).** Executed the 10-task TDD plan: opt-in reusable repo-wide Prettier gate — `format.yml` made dual-role (`workflow_call` + coercion-safe `${{ format('{0}', inputs.prettier) != 'false' }}` job-level opt-out) + adoptable `format.caller.yml`; `markdownlint-cli2@0.22.1` pinned dev dep; `markdown_tooling 1.0→1.1`; DEC-9→DEC-10; `tests/coherence/` (split-ownership declaration + hermetic conformance/pins + Node behavioral co-satisfaction corpus + `coherence.yml`); docs across markdown-tooling README/adopt, root README/CLAUDE/AGENTS, CHANGELOG/UPGRADING. **Fixed mid-flight, not deferred:** 2 plan-code defects (bool-keyed `dict[str,Any]` failing basedpyright strict; mid-file `import pytest` → E402), a pre-existing local-only Prettier miss (`.superpowers/` SDD scratch flagged because Prettier reads only the root ignore, not the nested `.gitignore` → added to root `.gitignore`), and a stale registry test asserting the old `1.0` default. Scoped opus post-impl review of `84c0054..HEAD` = **0 Critical/0 Important**; applied 2 accepted items — behavioral markdownlint made truly hermetic (`cwd=tmp_path`; cli2 was merging the repo's `.markdownlint-cli2.jsonc` globs and linting all 68 repo files) + `test_version_consistency` guard (pyproject↔top-CHANGELOG). **Released** sequential MINORs: signed tags `v4.1.0@84c0054` (F1–F4) + `v4.2.0@6614612` (F5), `v4`→`6614612`, GitHub releases (v4.2.0 Latest). **`main`'s new `release-pipeline` ruleset (`required_signatures`) rejected the push** — 6 prior-session Spec B design/plan commits were unsigned → rebased `1a85009..ba78dba` with `--gpg-sign` (byte-identical trees, new tip `6614612`); tag ruleset `release-pipeline-tags` briefly toggled `disabled`→`active` (backed up first, verified rules survived) to re-create the immutable `v4.2.0` tag. Recorded both rulesets in `state.md` Ops notes; corrected the `TODO.md` branch-protection item (main is now partially protected out-of-band). Full gate green throughout: 830 tests, cov 98%, basedpyright 0/0/0, ruff, coherence 8 (behavioral ran under `npm ci`), prettier + markdownlint + frontmatter + pip-audit clean. | impl `2eb1c1e`…`d61a74c`; re-signed → `6614612`; closeout `d1a3ae8` | — |
| 2026-07-07 | **CLI Documentation Standard (sixth) built end-to-end on `testing`** (spec + 13-task SDD plan, codex-converged r3; commits `a09b431..17b3099`): `standards/cli-documentation/` bundle (README/adopt/templates/examples/resources), adopt artifacts (`docs/usage.md` scaffold, `cli-docs-check.yml` CI template, config fragment), `cli_documentation` contract `1.0` registered end-to-end (registry.json + adopt-engine manifest/parity gate + CLI path), `--version` on all seven console scripts via shared `_version.py`, `--help` fixed on the two sync commands (previously parsed as a positional filename), dogfood `docs/usage.md` guarded by inventory-parity + installed-wrapper smoke tests, `src/project_standards/README.md` repositioned as internals reference. Final-review findings fixed (`17b3099`). Release deferred per handoff policy. | `a09b431`…`17b3099` | — |
| 2026-07-07 | **MCP enablement spec set ingested** (docs-only, on `testing`; no code). Migrated four prepared docs into the formal system: **SPEC-MT01** meta-repo readiness prep, **SPEC-RD01** enablement roadmap, **SPEC-MS01** MCP server implementation → `docs/superpowers/specs/` (date-prefixed, spec_id preserved), + the reference pack → `docs/superpowers/research/` (chose it over qdev-generated `docs/research/`). Dependency order MT01→RD01→MS01 preserved; MCP impl blocked until MT01 readiness gate. Ingestion fixes (conformance, not weakening): stripped template-merge doubled headings (`## 5. Stakeholders## …`, `## 18. Deployment## …` ×3 files), de-numbered MT01 §9.1–§9.5 to clear `SV-SECTION` (Full template §9 is flat), normalized `**Environment matrix**`→`:` for MD036, rewrote every ADR path `docs/decisions/`→**`docs/adr/`** (owner directive), date-prefixed the reference pack's spec cross-refs. Wired 3 specs into `.project-standards.yml` `spec.include` by explicit path (not a glob — legacy design docs aren't project specs). Updated specs index, `specs-plans.md`, `architecture.md` backlog, `state.md`, `STATUS.md`, `TODO.md` (ADR backlog, 13+ ADRs → `docs/adr/`). Gate: `spec validate`/`lint` 4/4 OK, markdownlint 0, prettier clean, plus full Python gate. | `76b09da` | — |
| 2026-07-07 | **ADR standard: canonical directory corrected `docs/decisions/` → `docs/adr/`** (owner directive, on `testing`; follow-up to the MCP ingestion). Full-consistency sweep of forward-looking references only (history untouched): ADR bundle `README.md` (directory/index convention) + `adopt.md`; adopt manifest `bundles/adr/adopt.toml` `dest` (so `adopt adr` writes `docs/adr/adr.template.md`) + its 3 `test_adopt_cli.py` assertions; the "exception ADR" path in Markdown Tooling / Project Specification / Python Tooling READMEs; the Markdown Frontmatter `concept`/`runbook` example `related:` links; root `README.md` pointer. Neutralized `src/project_standards/README.md`'s illustrative `exclude` (was `docs/decisions/**`, would have wrongly implied excluding the ADR dir). Left historical refs (CHANGELOG past entries, `2026-06-*` design docs, the `check-drift` plan) and the `validate_frontmatter` glob-mechanics regression test/comment (tied to CHANGELOG #245). Recorded in CHANGELOG `[Unreleased]` with a proposed **MINOR** (`adr 1.0→1.1`) classification for owner sign-off; registry not bumped (release-time). Gate green: 868 tests, spec validate/lint, markdownlint 0, prettier, frontmatter 24/24, ruff/basedpyright/pip-audit. | `e31a570` | — |
| 2026-07-07 | **v4.3.0 RELEASED on `main`** — additive MINOR shipping the CLI Documentation Standard. Full gate re-verified on the release tree (868 tests, 98% cov, ruff/basedpyright/pip-audit/coherence 8/frontmatter 24/prettier/markdownlint clean; the session-context "4 failing tests" flag was stale). `release: prepare` commit `74db623` (pyproject 4.2.0→4.3.0, CHANGELOG `[Unreleased]`→`[4.3.0]`, uv.lock); `main` fast-forwarded `f1994bb`→`74db623`; signed tags `v4.3.0@74db623` + `v4`→`74db623` (forced, tracker); GitHub release published (Latest). User interjected an "adopt the ADR standard" request then retracted it ("disregard last") — not actioned. Handoff closeout: state/STATUS/deployed/specs-plans/sessions updated; completed CLI-Doc section pruned from `TODO.md`. Post-release: all 6 `main` CI workflows green; the repo's 2 Dependabot alerts root-caused (js-yaml/markdown-it quadratic-DoS, transitive via dev-only pinned `markdownlint-cli2@0.22.1`; fix = coordinated bump to `0.23.0` + `lint-markdown.yml` action `v23`→`v24`, upstream `v24.0.0` available) — recorded in `TODO.md`, needs user go-ahead. | `74db623`, `0d56139`, `1137170` | — |
| 2026-07-07 | **Release policy set: freeze until v5.0.0** (owner directive). The next release is **v5.0.0**, carrying the Meta-repo/MCP-readiness implementation (`SPEC-MT01`) + every standards change accrued before it; **no interim PATCH/MINOR releases** until then, so standards work proceeds without per-change release friction. Version-affecting changes accumulate under CHANGELOG `[Unreleased]` and promote together at the v5.0.0 cut; keep classifying each change but don't tag. Recorded authoritatively in `meta/versioning.md` (callout above Purpose), with pointers in `state.md` (Ops notes), `STATUS.md`, `TODO.md`, and the CHANGELOG `[Unreleased]` banner. The pending `adr` `1.0`→`1.1` bump now targets v5.0.0. | `cdb1a6b` | — |
| 2026-07-07 | **v5.0.0 work begun — SPEC-MT01 Step 00 (baseline inventory) done** (the first planless, no-design-decision step; the "easy" groundwork). Wrote `docs/superpowers/research/2026-07-07-spec-mt01-baseline-inventory.md` — a factual map of current state (7 standards / 5 with machine bundles+registry entries; `project-spec` adoptable-via-CLI but absent from `registry.json`; heterogeneous registry with dir-name/key-name mismatch; `adopt.toml` is artifact-only; config namespaces; 4 reusable + 2 internal workflows; 868 tests/47 files) plus a grounded 10-item readiness-gap list feeding Steps 01–07. Checked Step 00 in the v5.0.0 tracker (in place, per its retention rule); pointer added to `specs-plans.md`. Triage: Steps 02–06 (meta-standard, `standard.toml` schema, graph validator, retrofit, fixtures) each need their own TDD plan; Step 01 (ADR authoring) is planless but a decision-per-ADR effort; the Dependabot markdownlint-cli2 bump + branch-protection remain owner-gated. Gate: markdownlint 0, prettier clean, 868 tests (unchanged — docs only). | `194637e` | — |
| 2026-07-07 | **Dependabot security bump done** (planless v5.0.0 work, owner-approved). `markdownlint-cli2` `0.22.1`→`0.23.0` (markdownlint `0.40.0`→`0.41.0`) in `package.json` + regenerated `package-lock.json`; reusable `lint-markdown.yml` action `markdownlint-cli2-action` `v23`→`v24`; `tests/coherence/test_pins.py` pin + comment updated for local↔CI co-satisfaction. Clears both moderate npm advisories — js-yaml (GHSA-h67p-54hq-rp68) `→5.2.0`, markdown-it (GHSA-6v5v-wf23-fmfq) `→14.2.0`; `npm audit` 0 vulns. Verified the 0.40→0.41 delta via web (serper): **no new rules** (refines MD022/28/35/42/51/60, drops inline-directive handling), so low consumer-visibility risk; the 85-file corpus lints clean under 0.41. Ships in v5.0.0 (freeze), recorded in CHANGELOG `[Unreleased]` (Security) + v5.0.0 tracker (Completed); retired the standalone Dependabot TODO item. Gate: coherence 8/8 (npm ci), 868 tests, ruff/basedpyright clean. | `abc44bf` | — |
| 2026-07-07 | **Owner granted standing agent-team permission for the v5 build-out.** Subagents, agent teams, and headless Codex may be used without asking for the whole v5.0.0 effort — **Sonnet or Opus by task complexity, never Fable/Haiku** — until v5.0.0 is released (then revert to ask-first). Recorded in `AGENTS.md` (Working Rules, first bullet), `state.md` (Ops notes), the v5.0.0 tracker (`TODO.md`), and agent memory (`project-standards-v5-agent-team-permission` + companion `-release-freeze`). | `7d3046b` | — |
| 2026-07-07 | **SPEC-MT01 Step 01 — ADR foundation authored** (first use of the v5 agent-team permission). Stood up `docs/adr/` and authored the **13 meta-repo readiness ADRs** (`adr-0001`…`0013`) as **proposed** (`status: review`) + an index `README.md`, transcribing SPEC-MT01 §8.3 D-001..D-013 into MADR form. `adr-0001` written + validated by me as the exemplar (proving the dogfood pipeline); `adr-0002`…`0013` drafted by **3 parallel Sonnet subagents** (4 each), then centrally normalized + validated. Dogfood-wired `docs/adr/**` into `.project-standards.yml` frontmatter validation so the repo validates its own ADRs (the opt-in `markdown.adr.require_sections` check enforces the 3 MADR sections; `validate-id` accepts the `adr-NNNN-repo-slug` form). **Owner action pending:** review + accept (flip `review`→`active`). SPEC-MS01 _server_ ADRs deferred to the server phase. Gate: validate-frontmatter 38, validate-id 38, markdownlint 0, prettier, 868 tests, spec validate. | `ee98d0f` | — |
| 2026-07-07 | **13 SPEC-MT01 ADRs ACCEPTED** (owner batch-accept after a quick contradiction review — none conflict with current operation; 3 forward-commitments flagged: provider-plugin refactor away from inline dispatch (adr-0006), stricter config-namespace rejection (adr-0008), and the project-spec/python-coding retrofit obligation (adr-0001/0002)). Flipped all 13 `status: review`→`active` + MADR `proposed`→`accepted` + `reviewed: 2026-07-07`; index intro/table updated. Direction is now locked — changing any needs a superseding ADR. | `ae787ce` | — |
| 2026-07-07 | **v5 Step 02 design — SPEC-BA01 (Standard Bundle Authoring meta-standard) brainstormed + written as a Light project spec.** Owner corrected the artifact format: use the repo's adopted **Project Specification Standard**, not the superpowers `-design.md` convention (dogfood our own standard; the MCP specs set precedent). Brainstorm locked three decisions: **internal/reference** audience (no adopt.md / registry contract version), **doc-only** Step 02 scope (the `standard.toml` schema/model/validator deferred to Steps 03–04), and an **`adoption`-mode enum** (`validator`/`copy-adopt`/`cli`/`reference-only`/`none`) that makes the `project-spec` (cli) + `python-coding` (reference-only draft) outliers first-class instead of exceptions. Scaffolded with `project-standards spec new` (dogfooding the CLI), 11 FRs, wired into `.project-standards.yml` `spec.include`. Fixed a bare `D-001` token (SV-ID-UNDECLARED FAIL) + `<id>` path pattern (SL-PLACEHOLDER WARN). Gate: `spec validate`/`lint` clean, markdownlint 0, prettier. **Awaiting owner spec-review before writing-plans.** | `3b10ae4` | — |
| 2026-07-07 | **SPEC-BA01 Codex spec-review — converged in 2 passes** (user-invoked). R1 (verdict: needs _major_ correction; 2 High + 3 Medium) → fixed the config-namespace model (top-level → **dotted/nested** with parent delegation + reserved meta keys, so it can represent `markdown.frontmatter`/`markdown.adr`/`spec`/`standards_version` — the original model literally couldn't describe the repo it governs), added manifest-safety (FR-012), `adopt.toml`-linkage (FR-013), and manual-conformance-checklist (FR-014) requirements + a standards-index anatomy DoD item, and resolved OQ-001 (`cli`) + OQ-002 (full annotated example). R2 (verdict: needs _minor_ correction; **0 blocking**) → made FR-012 paths **bundle-relative/contained** (adr-0010) + clarified provider entrypoints vs filesystem paths — converged. Twice stripped external ID tokens (`SA-`/`C-`/`EC-NNN`) that tripped `SV-ID-UNDECLARED` in our own spec validator. Audits: `docs/codex-reviews/2026-07-07-2058…round1.md` + `…2106…round2.md`. Gate: spec validate/lint, markdownlint, prettier all clean. Rev 0.3. Awaiting owner spec-review → then writing-plans. | `66cfb83` | — |
| 2026-07-07 | **SPEC-BA01 implementation plan written (writing-plans) + Codex plan-review converged in 3 passes.** 5-task doc-authoring plan. R1 (major; 2 High + 2 Med): CR-001 dropped `[capabilities]` manifest section → re-added; CR-002 plan contradicted resolved OQ-002 → clarified SPEC-BA01 rev 0.4 (README holds the full annotated example; own manifest minimal-but-conformant, no fake authority); CR-003 full-gate cmd masked TOML failures → `tomllib` + no `;`-masking; CR-004 stale repo-facing docs → scope expanded. R2 (major; 1 High) CR-NEW-001: mislabeled CLI-enforced standards as non-adoptable → **`adopt.md` presence is independent of adoption mode**; `project-spec` (`cli`) is adoptable + keeps `adopt.md`; only `none`/unreleased-draft use a non-adoptable marker (SPEC-BA01 rev 0.5, FR-001 + DoD). R3 (minor; 0 blocking): CR-004 residual (add `AGENTS.md` + `docs/handoff/architecture.md`) + CR-NEW-002 (stale rev refs) applied → converged. Audits: `docs/codex-reviews/…plan-review-round{1,2,3}.md`. **Both spec + plan Codex-converged; ready for execution.** | `fd7d192` | — |
| 2026-07-07 | **Session close-out — v5.0.0 Steps 00–02 advanced; `testing` pushed.** This session: set the release-freeze policy + durable v5.0.0 tracker + owner agent-team permission; Step 00 baseline inventory; Dependabot security bump (2 npm DoS alerts cleared); Step 01 — 13 ADRs authored (3 parallel Sonnet agents) + owner-accepted; Step 02 — meta-standard `SPEC-BA01` (Light) spec + 5-task implementation plan, **both Codex-converged** (spec r2 / plan r3). All docs-only under the freeze (accrues to v5.0.0). Final gate green: frontmatter/id 38, spec validate/lint, markdownlint 0, prettier, ruff/basedpyright, 868 tests. Pushed `testing` to origin (ahead of `main`, unreleased). **Next: execute the SPEC-BA01 plan** (Step 02) or advance to Step 03. | `4298159` | — |
| 2026-07-07 | **SPEC-BA01 implemented (executing-plans, inline) — v5.0.0 Step 02 complete.** Authored the `standards/standard-bundle-authoring/` bundle: README contract (FR-001…FR-014 — bundle anatomy, annotated `standard.toml`, five-mode adoption enum, authority tuple + conflict rule, relationship taxonomy, **dotted** config-namespace ownership representing `markdown.frontmatter`/`markdown.adr`/`spec`/`standards_version` without duplicate-owner ambiguity, providers, resources, manifest safety, `adopt.toml` linkage, lifecycle, FR-014 conformance checklist) + its own worked `standard.toml` (`adoption = "none"`, minimal-but-conformant) + a blank template. Reconciled repo-facing maps: `standards/README.md` index + `adopt.md` anatomy, root `README.md`, `AGENTS.md` (also fixed a stale **five→six-standards** drift + restored `format.yml`, caught by the DEC-10 `test_no_stale_prettier_claims` coherence guard), budget-neutral `CLAUDE.md` (2090→2041 B), `meta/versioning.md`, `docs/handoff/architecture.md`. CR-004 machine-layer diff empty (no `registry.json` / bundles / `spec.include`). SPEC-BA01 §17.1 DoD ticked (rev 0.6; owner acceptance of the empty Deviations Log pending). Full gate green: frontmatter/id 39, spec validate/lint, markdownlint 0, prettier, ruff/basedpyright, 868 tests. | `fbaf49d`…`83fe5fd` | — |
| 2026-07-08 | **SPEC-MT01 Step 03 implemented subagent-driven — `standard.toml` Pydantic model + loader + generated schema + fixtures.** 11-task TDD plan (spec r2 / plan r2 both Codex-converged) executed via subagents: `StandardManifest` model tree ([standard]/[versions]/[config]/[capabilities]/[relations]/[resources]/[[authority]]/[[providers]], adoption=none forbids adopt resource), `load_standard_manifest` loader (tomllib, error-wrapped, id/dir agreement, symlink-safe resource resolution), generated + byte-drift-tested `standard.schema.json` shipped in the wheel, and a ~22-fixture valid/invalid corpus plus schema-vs-model semantic tests. Added `pydantic` as a runtime dependency. **Two `ValueError`-class loader-boundary escapes caught in review** (the invariant is "only `StandardManifestError` escapes"): per-task review found a null-byte resource path reaching `.resolve()` as a raw `ValueError`; the final Opus whole-branch review found its twin — a `UnicodeDecodeError` (subclass of `ValueError`, not `OSError`) on a non-UTF-8 read that the earlier post-check fix hadn't covered. Both wrapped (reject `\x00` at the model + `except (OSError, UnicodeDecodeError/ValueError)` on read/post-check), plus a drive-letter entrypoint false-reject fixed — each with a RED→GREEN regression test. CR-004 machine-layer diff empty (no `registry.json`/bundles/`spec.include` touched). Full gate green: ruff format+check, basedpyright 0/0/0, 961 tests, 98% cov, pip-audit clean, spec validate/lint, prettier, markdownlint, coherence 8/8; wheel confirmed to ship `project_standards/schemas/standard.schema.json`. | `89464ea`…`44c290e` | null-byte + UnicodeDecodeError loader-boundary escapes (both fixed) |
| 2026-07-09 | **SPEC-MT01 Step 04 implementation plan written and review-addressed.** Added `docs/superpowers/plans/2026-07-09-standards-graph-validator.md` for the standards graph loader, authority/capability/resource/provider/config/relationship validators, hidden-dependency rejection fixtures, and `project-standards standards validate-graph` CLI. Registered the plan in `TODO.md` + `specs-plans.md`; preserved the Claude Code adversarial review trail in `docs/reviews/2026-07-09-cc-review.md`. Review rounds drove fixes for loader-contract mismatches, shared finding sorting, task-boundary imports, and Ruff E402 import-hoisting guidance. Next: execute Step 04; Step 05 retrofit remains pending. | `5f83d79` | — |
| 2026-07-09 | **SPEC-MT01 Step 04 implemented.** Added `project_standards.standards_graph` model/discovery/validators/CLI, wired `project-standards standards validate-graph`, and covered graph findings, hidden-dependency rejection, current-repo pre-retrofit smoke, and installed wrapper routing. Verification: ruff, basedpyright, 1006 tests, 98% coverage, pip-audit, coherence, frontmatter, spec validate/lint, and graph CLI checks green; full Prettier/markdownlint still blocked only by pre-existing `docs/future-standards/**` backlog. Step 05 retrofit remains next. | `8d23a0c` | — |
| 2026-07-09 | **SPEC-MT01 Step 05 implemented + subagent-reviewed.** Added `standard.toml` manifests for all eight standard bundles (`validator`, `copy-adopt`, `cli`, `reference-only`, `none` modes) with authorities/resources/providers/relationships, flipped the current-repo `--require-all-manifests` graph gate to pass, and fixed graph validation so `reference-only` standards do not require an `adopt` resource. Subagent review found no spec-compliance gaps and caught generic-provider-operation drift + two missing template resources; fixed by constraining provider operations to the generic enum and adding the missing resources. Deferred `standard.toml`↔packaged-`adopt.toml` artifact-linkage reconciliation to Step 06 because Step 05 keeps resources bundle-local while artifact manifests remain under `src/project_standards/bundles/`. Verification: graph, 1015 tests, 98% coverage, ruff/basedpyright, pip-audit, coherence, frontmatter, spec validate/lint. Step 06 dogfood fixtures + generated index/catalog remains next. | `82d1978`, `ce11a54` | — |
| 2026-07-09 | **Markdown Frontmatter value guidance + skill ownership.** Split the standard into README entry point, `structure.md` hard rules, and `field-values.md` semantic guidance; added `templates/repository-frontmatter-adr.md`; defined baseline IT/network/infrastructure tags; ADR 0014 dogfoods field-value policy, ADR 0015 excludes `standards/**` from this repo's local frontmatter corpus, and ADR 0016 moves the `markdown-frontmatter` skill into the standard package/adopt bundle. Fresh adoption now installs the repo-local skill under `.agents/skills/markdown-frontmatter` with executable `new-doc-id`; old `agent-configs` skill source removed. Iterated subagent drift reviews until a final no-findings convergence pass; full gate green. No `schema_version`/validator behavior change. | this closeout | — |
| 2026-07-09 | Handoff v3.5 migration applied; repo-local skill installed and fatal layout/shape drift cleared. | this closeout | — |
| 2026-07-09 | **Session closeout — package methodology and handoff v3.5.** ADRs 0017–0021 active; `project-spec` copy-adopt artifacts packaged; frontmatter references reconciled; scoped gate green. Broad markdown backlog remains under `docs/future-standards/**`. | this closeout | — |
| 2026-07-09 | SPEC-MT01 Step 06 linked artifact manifests, proved composition, and generated the standards catalog. | this closeout | — |
| 2026-07-09 | Agent Handoff v1 Full spec drafted from approved package, runtime, migration, and retirement design. | this closeout | — |
| 2026-07-09 | Agent Handoff v1 spec adversarial review addressed: generic-provider mapping, explicit CLI/artifact-lifecycle platform scope, strict owned config namespace, numbered workflow/error contracts, and proposed ADR 0022 for project-local packaged hooks. | this closeout | — |
| 2026-07-09 | Agent Handoff spec and ADR approved; 18-task implementation plan written after round-2 convergence. | this closeout | — |
| 2026-07-09 | Agent Handoff plan review remediated; rev 0.5 records installed-path authority and repository-license inheritance. | this closeout | — |
| 2026-07-09 | Agent Handoff v1 dogfooded; status/tasks relocated, shared hook/skill installed, and legacy artifacts retired. | this commit | — |
| 2026-07-09 | Agent Handoff acceptance passed; release docs and the blocked 26-repo retirement inventory were recorded. | this commit | — |
| 2026-07-09 | Migrated `cc-usage-monitor` to Agent Handoff v1; dual hook probes and its full 150-test gate pass on `main`. | `81d464d` | — |
| 2026-07-09 | Migrated `control-center` to Agent Handoff v1; dual probes and 283 tests pass after two lock security updates. | `1be92ec` | — |
| 2026-07-09 | Migrated `website-aboutme` on protected `testing`; dual probes and the Astro production build pass. | `ab6bc3d` | — |
| 2026-07-09 | Migrated `Markdown-Keeper`; dual probes and 174 dependency-light tests pass on `main`. | `d373df1` | — |
| 2026-07-09 | Migrated `HomeBase`; dual probes and the full 466-test suite pass on `main`. | `ec3df46` | — |
| 2026-07-09 | Fixed Agent Handoff reference checks to decode local URLs and ignore Markdown code examples; 1,368 tests pass. | this commit | false missing-link findings |
| 2026-07-09 | Migrated `Russ-Estate-Paperwork`; dual probes pass and sensitive task identifiers were replaced by record pointers. | `ab71b83` | — |
| 2026-07-09 | Migrated `star-trek-retro-remake`; dual probes and its full 11,212-test CI-equivalent gate pass. | `9d4e19e` | — |
| 2026-07-09 | Migrated `agent-pseudocode`; dual probes, 48 tests, Ruff, BasedPyright, and all pseudocode checks pass. | `21ade51` | — |
| 2026-07-09 | Migrated `Claude-Code-Plugins`; dual probes, marketplace validation, npm audit, and targeted Markdown checks pass. | `0fdbd98` | — |
| 2026-07-09 | Migrated `dotfiles`; dual probes and its complete 170-test `make check` contract pass. | `baf8705` | — |
| 2026-07-09 | Migrated `finances`; dual probes, Ruff, and all 361 PostgreSQL-backed tests pass; the historical brief was reclassified. | `f3e1d01` | — |
| 2026-07-09 | Migrated `homelab`; dual probes, changed-doc standards checks, npm audit, and llm-wiki citation validation pass. | `ceba125` | — |
| 2026-07-09 | Migrated `website-l3digital.net` on protected `testing`; dual probes and the Astro production build pass. | `87dabc2` | — |
| 2026-07-09 | Classified `ClaudeCodeStatusLine` as a non-consumer: its handoff-like notes are ignored and untracked by deliberate public-distribution policy. | — | — |
| 2026-07-09 | Migrated `docmend` on protected `dev`; dual probes and its full 619-test, 97%-coverage local gate pass. | `1657e2e` | — |
| 2026-07-09 | Migrated `hw-radar` on protected `dev`; retired the monolith; dual probes and its 357-test, 93%-coverage gate pass. | `cbe77be` | — |
| 2026-07-09 | Migrated the `projects` workspace meta-repo; dual probes and its complete 298-test local gate pass on `main`. | `3da7641` | — |
| 2026-07-10 | Integrated Agent Handoff v1 into `testing`; preserved owner tasks and scratch exclusions; 1,368 tests and static gates pass. | this closeout | — |
| 2026-07-10 | Traceability/release docs, bug-index targeting, and graph/catalog CI reconciled; Step 07 remains next, with owner-choice debt open. | `70b20ee`…`342a802`; 1,373 tests | — |
| 2026-07-10 | Repaired all 13 provisional future-standard drafts for broad Prettier/markdownlint; none were promoted or registered. | this commit | — |
| 2026-07-10 | FR-013 closed: nine compact summaries, catalog URIs, strict 3,000-byte policy, and package versions verified. | `529ec72`…`9463dcb`; 1,382 tests | — |
| 2026-07-10 | Accepted ADRs 0023-0024 for the unified `.standards/` control plane and catalog-scoped package channels; reconciled legacy decisions and versioning. | this commit | — |
| 2026-07-10 | Drafted Full SPEC-BA02 for family indexes, immutable payloads, catalog roles, bounded providers, migrations, and V5 package compatibility. | this commit | written-spec review pending |
| 2026-07-10 | Remediated SPEC-BA02 review round 1: phase/traceability fixes, sourced V5 count, and exact legacy managed-block migrations. | this commit | owner approval pending |
| 2026-07-10 | Owner approved SPEC-BA02 rev 0.3; SPEC-BA01 became superseded implementation history and planning was authorized. | this commit | implementation plan next |
| 2026-07-10 | Wrote the 15-task BA02 foundation plan: strict package models/schemas, inventory and release integrity, graph/catalog validation, authoring CLI, and source-to-wheel payload projection. Self-review and targeted Markdown gates pass; adversarial plan review is next. | this commit | — |
| 2026-07-10 | Remediated BA02 foundation review; SPEC-BA02 rev 0.4 now pins digest bytes and a golden vector. Convergence review remains. | this commit | digest-plan contradiction fixed |
| 2026-07-10 | Converged BA02 foundation plan review round 2: all six dispositions verified in place, the spec's golden digest vector independently reproduced (wrong-order and bare-hex variants proven distinguishable), rev 0.4 confirmed clarification-only, and the plan approved for execution with one non-blocking gate note (F7). | this commit | — |
| 2026-07-10 | BA02 foundation implemented; authoring, integrity, graph, CLI, projection, offline wheel, and scale gates pass. Runtime and migration remain. | closeout | mirror drift fixed |
| 2026-07-10 | Drafted the 18-task CP01 core plan; aligned rev 0.5 examples with approved BA02 config/payload contracts. | this commit | review pending |
| 2026-07-10 | Remediated the CP01 core-plan review; replaced shim-sensitive wheel installation and cleared all plan findings. | this commit | approved for inline execution |
| 2026-07-10 | CP01 core Tasks 1-4 implemented: state/codecs, nine schemas, offline installed content, and directory locking; 1,744 tests pass. | `90debb5`…`d23a10b` | Task 5 next |
| 2026-07-11 | CP01 Task 5 implemented exact neutral initialization and completed installed family projection; 1,757 tests pass. | `2d8d831` | Task 6 next |
| 2026-07-11 | CP01 Tasks 6-8 added safe config edits, version/track resolution, referenced inputs, and bounded providers; 1,821 tests pass. | `2b6a359`, `a51420a`, `6bd74b3` | Task 9 next |
| 2026-07-11 | CP01 Tasks 9-10 added safe snapshots, whole-file lifecycle, and round-trip TOML composition; 1,856 tests pass. | `0ca5fd8`, `078b53b` | Task 11 next |
| 2026-07-11 | CP01 Tasks 11-13 added bounded JSON/JSONC, YAML, EditorConfig, and formatter-stable Markdown composition; 1,919 tests pass. | `6f7f0a4`, `8d78fb8`, `03e80f5` | Task 14 next |
| 2026-07-11 | CP01 Tasks 14-15 added deterministic virtual-tree planning, central shared ownership, and package-local lifecycle safety; 1,949 tests pass. | `75fba70`, `954a493` | Task 16 next |
| 2026-07-11 | CP01 Tasks 16-18 completed transactional apply/recovery, public reconciliation and compatibility CLI paths, offline installed-wheel repair, all-adapter fixed point, determinism, no-hardcode, and scale acceptance. | `000ba43`, `6876ba4`, this closeout | follow-on package migration/activation plan next |
| 2026-07-11 | Closed the CP01 core session, reconciled its remaining work into the durable v5 tracker, expanded the managed housekeeping workflow, and published the 25-commit `testing` baseline. Hosted graph/catalog run `29145160155` passed at `5d21517`. | `5d21517`, this closeout | two moderate default-branch Dependabot alerts remain visible |
| 2026-07-11 | Drafted the 18-task CP01 package-migration/release-readiness follow-on plan; review and approval are next. | this commit | v4/v5 dogfood sequencing pinned |
| 2026-07-11 | Remediated CP01 follow-on plan audit CR-001–CR-003: scratch catalog gate, explicit migrate remap, and matrix marker policy. | this commit | convergence re-audit next |
| 2026-07-11 | Remediated CR-NEW-001 by moving catalog scratch rendering to a unique ignored in-root `build/` directory. | this commit | scratch-path convergence re-audit next |
| 2026-07-11 | CP01 package migration was approved. Task 1 added typed reports, schema, fixtures, and validation; 635 focused tests pass. | this commit | Task 2 planning next |
| 2026-07-11 | CP01 Task 2 added read-once discovery, exact ambiguity checks, complete read-only plans, and current legacy fixtures. | this commit | Task 3 apply/CLI next |
| 2026-07-11 | CP01 Tasks 3-5 completed migration apply, catalog refresh/recovery, and isolated Authoring 2.0 self-hosting. | this commit | Task 6 next; root activation stays in Task 14 |
| 2026-07-11 | CP01 Task 6 reconstructed Python Coding 0.5 as a reference-only V2 payload with source/wheel parity and no consumer outputs. | this commit | Task 7 next; root activation stays in Task 14 |
| 2026-07-11 | CP01 Task 7 reconstructed Markdown Frontmatter 1.2 with locked unified config, immutable validate/inspect/fix/migration providers, typed executor-only authoring, exact V4 migration, and source/wheel parity. The full gate passes 2,156 tests at 90% coverage. | this commit | Task 8 next; root activation stays in Task 14 |
| 2026-07-11 | CP01 Task 8 reconstructed ADR 1.1 as a Frontmatter-independent companion with immutable section validation, create-only scaffold lifecycle, exact V4 migration, and source/wheel parity. | this commit | Task 9 next; root activation stays in Task 14 |
| 2026-07-11 | CP01 Task 9 reconstructed CLI Documentation 1.1 with neutral options, create-only usage docs, a generic stdout-only render command, package-isolated referenced workflow verification, exact V4 migration, and safe no-clobber bootstrap guidance. | this commit | Task 10 next; root activation stays in Task 14 |
| 2026-07-11 | CP01 Task 10 reconstructed Markdown Tooling 1.2 with managed lint/format configs and callers, typed safe scope/exclusion inputs, semantic EditorConfig and per-setting VS Code composition, exact V4 migration, and stable disabled/manual behavior. | this commit | Task 11 next; root activation stays in Task 14 |
| 2026-07-11 | CP01 Task 11 reconstructed Project Specification 1.1 with version-selected inspection and authoring providers, descriptor-bound snapshot routing, executor-only scaffold/upgrade writes, semantic V4 migration, and offline source/wheel parity. | this commit | Task 12 next; root activation stays in Task 14 |
| 2026-07-11 | CP01 Task 12 reconstructed Python Tooling 1.1 with closed tuning options, mandatory Ruff/type/pytest/coverage/audit gates, bounded TOML/EditorConfig/VS Code/instruction composition, independent 1.0/1.1 consumer-contract selection, exact V4 migration, and offline source/wheel parity. | this commit | Task 13 next; root activation stays in Task 14 |
| 2026-07-11 | CP01 Task 13 reconstructed Agent Handoff 1.1 with profile-conditional hook and instruction ownership, entry-level Claude/Codex composition, immutable policy validation, typed bounded authoring, exact legacy lock import/retirement, lock-last live-byte verification, and offline source/wheel parity. | this commit | Task 14 activates the real catalog and root V2 families next |
| 2026-07-12 | CP01 Task 14 activated catalog 5 and all nine root V2 family indexes, moved graph/catalog generation to validated package facts, generated relative installed projections, required the complete seven-default adoption surface, and proved exact source/wheel bytes. Two independent review rounds converged after fail-closed Agent Handoff routing and V2 missing-family fixes; the full gate passes 2,407 tests at 90% coverage. | this commit | Task 15 unifies public command configuration and provider routing next |
| 2026-07-12 | CP01 Task 15 routed public validation, inspection, rendering, and authoring through exact applied package payloads/config; retained read/write locks for complete command lifetimes; preserved legacy output/exit contracts; and added typed mutation diagnostics with executor-enforced refusals. Independent spec and quality reviews converged READY; the full source/wheel gate passes 2,461 tests at 89% coverage. | this commit | Task 16 real package compatibility matrix next |
| 2026-07-12 | CP01 Task 16 added the catalog-derived real package compatibility matrix across all seven defaults, 21 unordered pairs, fresh and all-namespace migration, every declared validator, shared ownership/removal, consumer-state preservation, offline source/wheel parity, deterministic order permutations, repeated wheel builds, and real-plan scale budgets. Matrix-specific gates pass 56 correctness and 3 performance rows; independent spec and quality reviews converged READY after adapter comment/provenance regressions were fixed. | this commit | Task 17 disposable release-cut dogfood and refresh proof next |
| 2026-07-12 | CP01 Task 17 proved the disposable 5.0.0 release cut: installed migration, exact replay, same-major refresh preservation, fixed-point convergence, and source/wheel command validation pass. | `a891973` | Task 18 documentation and release-ready closeout next |
| 2026-07-12 | CP01 Task 18 reconciled adoption, migration, upgrade, versioning, release, and spec evidence; independent reviews converged and the local gate passes 2,535 tests at 89% coverage. | this commit | hosted post-push confirmation and atomic source-root release migration remain |
| 2026-07-12 | Integrated CP01 package migration into `testing`; the complete local gate and hosted graph/catalog run `29195839078` pass. | `b6a60fc` | atomic source-root release migration remains |
| 2026-07-12 | Prioritized the canonical queue for a clean v5 release; deferred two new packages to dedicated post-v5 cycles. | this commit | scope and debt before Step 07 |
| 2026-07-12 | Reconciled the v5 launch scope to the nine catalog 5 families in SPEC-BA02 rev 0.8 and its indexes. | this commit | debt inventory next |
| 2026-07-12 | Inventoried release debt across plans, reviews, future drafts, scratch files, link integrity, shape, and security gates. | this commit | classify and repair before Step 07 |
| 2026-07-12 | Closed resolved issue #3 and stale PRs #1, #2, #4, #5, and #6; consolidated incomplete action bumps into one v5-wide task. | remote state | action parity work remains |
| 2026-07-12 | Closed the three deferred maintenance decisions: existing `spec new` symlink-parent enforcement is sufficient for v5; Project Specification keeps its OpenAPI 3.2.0 contract with intentionally unpinned template citations; catalog 5 drift tooling supersedes the unexecuted v2.2 `check` plan. | this commit | superseded plan awaits owner-approved artifact pruning |
| 2026-07-12 | Cleared current handoff shape debt: instruction files meet byte targets, architecture reflects catalog 5, and bug lessons meet advisory limits. Historical session rows remain immutable. | this commit | append-only session warnings accepted |
| 2026-07-12 | Indexed all 12 future-standard drafts and two indexes: retained post-v5 package inputs, routed consolidation candidates, and labeled two superseded transcripts for owner-approved deletion. | this commit | no future draft gates v5 |
| 2026-07-12 | Established `docs/specs/` as the durable specification home: moved six current specs, archived superseded BA01, and moved the eight-document future Usage Documentation set. Updated configuration and references; all 15 specs pass validate/lint. | this commit | historical designs remain under `docs/superpowers/specs/` |
| 2026-07-12 | Repaired retained-document links after the spec move and older reorganizations; a parser-backed audit resolves 1,924 local links across 305 source Markdown files with zero failures. | this commit | rerun only for review artifacts retained after owner disposition |
| 2026-07-12 | Cleared the catalog 5 MCP-exposure review across all nine packages. Fixed Python Tooling's missing canonical-summary backlink before first release, refreshed integrity/projection metadata, and added a catalog-wide regression test; 494 package-contract tests pass. | this commit | final housekeeping remains before Step 07 |
| 2026-07-12 | Fixed the disposable release checkout to model non-ignored additions and tracked deletions from the live working tree. Added a hermetic regression, refreshed Python Tooling legacy signatures and release-cut evidence, and reproduced the complete migration proof in isolation. | this commit | final full gate rerun pending |
| 2026-07-12 | Final cleanup gate passed: 2,542 tests, 89% coverage, dependency audits, release proof, and package/spec/docs/handoff checks are green. | this commit | owner approvals remain |
| 2026-07-12 | Hygiened `docs/TODO.md` to an open-only, dependency-ordered queue; completed history remains in STATUS and session records. | this commit | P0 owner decisions next |
| 2026-07-12 | Pruned 55 approved completed/superseded review and plan artifacts plus two ignored scratch files; retained the release-cut evidence and active Agent Handoff retirement plan. | this commit | approved Action upgrades next |
| 2026-07-12 | Completed Action parity, optimized verification, coverage cleanup, and SPEC-MT01 Step 07 with no blockers. | this commit | contract convergence, implementation, evidence, migration |
| 2026-07-12 | Reconciled ownership audit: static target binding, known-claim compatibility, and unknown-observation fail-closed retention. | this commit | convergence audit, then TDD |
| 2026-07-12 | Converged the ownership contract and Python Tooling plan; plan audit resolved execution defects without a spec change. | this commit | prerequisite commit, then isolated TDD |
| 2026-07-12 | Independently reviewed the checker-table blocker (empirically reproduced with locked basedpyright 1.39.6), authored the conditional nested-option-pointer design, reconciled five contract-audit rounds to convergence, and committed the six-task checker implementation plan. | `c1c13e4`, `50c0776` | execute the checker plan, then amend and re-audit coverage Tasks 9 and 11 before Task 9 |
| 2026-07-12 | Reconciled four adversarial checker-plan audit rounds to convergence: two-phase pinned Pyright provisioning with npm strict offline and a `$GITHUB_ENV`-published shared cache, scratch import seam with probe and coverage assertions, real-payload lifecycle/lock proofs from both selections, the complete repository gate, exact-pin release handoff, and handoff conformance gates. | `ab23e61`…`eb6402b` | implement the checker plan, then amend and re-audit coverage Tasks 9 and 11 |
| 2026-07-12 | Implemented all checker-table tasks with `pyright==1.1.411`; checker-owned gates pass. | `4e209a9`…`1222abb` | amend/audit coverage Tasks 9 and 11, then refresh release evidence |
| 2026-07-12 | Integrated coverage Tasks 1–6 and checker materialization into `testing`; post-merge contract gates pass. | this commit | amend/audit coverage Tasks 9 and 11 |
| 2026-07-13 | Re-audited coverage Tasks 9/11; CTM-NEW-008 later superseded the initial signature-only overlay. | `2bc3bf0` | Tasks 7–8 next |
| 2026-07-13 | Completed coverage Tasks 7–8: consumer-owned workflow preservation and target-bound authoring guidance. | `582568f`, `d5c8031` | Task 9 next |
| 2026-07-13 | CTM-NEW-008 closed Task 9 root-authority reconstruction; superseding audits are Ready without spec backtrack. | this commit | resume Task 9 |
| 2026-07-13 | Coverage Task 9 reconstructed the exact predecessor twice, added both installed-provider guards, refreshed four package recognition/integrity chains, proved locked sync plus migration replay, and hardened Git/input/evidence binding under CTM-NEW-009–016. CP01 rev 0.11 and BA02 rev 0.12 record passing ownership-relinquishment evidence; the real migrated root gate remains correctly sequenced after Task 11's atomic root expectation changes. | this commit | Task 10 complete gate next; Task 11 atomic migration follows |
| 2026-07-13 | Audited live release state; queued two workflow blockers and all remaining work in dependency order. | `7d4d5fa`; run `29249551448` | fix inputs before Task 9 refresh |
| 2026-07-17 | Task 11 atomically migrated the source root to catalog 5 through the extracted 5.0.0 provider, preserved reviewed consumer bytes, passed the migrated-root gate and immutable predecessor replay, and bound the complete parent-to-release evidence. | this commit | land exact commit on `main`, then hosted gates and publication |
| 2026-07-18 | Audited the release-preparation layer against the original v5 contracts; removed replay, retained self-referential evidence, custom orchestration, the frozen release tree, and their process artifacts. Kept direct installed-wheel ordinary, compatibility, performance, package, and quality gates green. | this commit | await explicit post-v5 task selection; do not recreate release-proof infrastructure |
| 2026-07-18 | Completed the comprehensive implementation/specification/documentation drift audit, corrected only confirmed drift, and published patch release 5.0.1 after independent no-findings scope review and the complete extracted-wheel gate. | signed `v5.0.1` tag | no audit findings remain; future work queue unchanged |
| 2026-07-18 | Preserved and committed the pre-existing user maintenance-queue refresh separately from the 5.0.1 release tag, then synchronized `main` and `testing`. | this closeout | await explicit selection from the refreshed queue |
| 2026-07-19 | Housekeeping pass: retired `docs/superpowers/` (designs → `docs/specs/archive/` with a complete index, research → `docs/research/` + index rows, active Agent Handoff plan → `docs/plans/`), deleted the implemented checker-table plan, four design-review audits, and empty `docs/codex-reviews/`; fixed every live reference. Authored internal payload `standard-bundle-authoring@2.1` to correct 2.0's SPEC-BA02 pointer without mutating released bytes; added the internal-additive PATCH classification rule to `packages check-release` with tests and policy text; prepared release 5.0.2 (pyproject, changelog, lock, control-plane refresh, golden fixture). | this commit | owner cuts `v5.0.2` on `main` |
| 2026-07-19 | Published Project Standards 5.0.2 from `c731955`: complete extracted-wheel gate and all hosted workflows passed; signed `v5.0.2` and moving `v5` tags and byte-verified wheel/sdist assets are live. Owner selected 5.1.0 for the accepted review corrections and set the consumer Python floor to 3.14. | signed `v5.0.2` tag | verify and correct every review finding without unrelated changes |
| 2026-07-20 | Closed all 100 implementation-review findings with 96 accepted or adjusted corrections and four final no-change dispositions. From the exact 5.1.0 candidate wheel, ordinary, compatibility, performance, coverage, package, audit, dogfood, immutable-payload, Markdown style, and handoff gates pass with MINOR classification, Python 3.14+, and exactly nine approved payloads. The completed source and design reviews were retired at owner direction; 5.1.0 is prepared and verified, unpublished. | this commit | await explicit owner authorization before tagging or publishing 5.1.0 |
| 2026-07-20 | Audited consumer documentation against the prepared 5.1.0 implementation, corrected all 14 drift findings, fixed the two Agent Handoff CLI defects the audit exposed, and added immutable successor payloads for released documentation that could not be edited in place. Catalog 5, current authority pointers, adoption guidance, release records, generated projections, and direct installed behavior now agree; the complete extracted-wheel and documentation gates pass. | this commit | push `testing`; publication still requires explicit owner authorization on `main` |
| 2026-07-20 | Published Project Standards 5.1.0 from `b69600d` after all eight `main` workflows passed, including `Check` run `29751307357`. Signed `v5.1.0` and moving `v5` tags are live; downloaded wheel (`619f2b1…600f`) and sdist (`932279f…50c2`) assets match the uploaded bytes exactly. Reconciled deployment, status, release-policy, plan, and session truth after publication. | this closeout | await explicit selection from the post-release queue |
| 2026-07-20 | Drafted the testing-only agent adoption prompt and queued next-release promotion with mandatory upstream irregularity reporting. | this commit | next release |
| 2026-07-20 | Corrected issue #8 so Catalog 4 repositories migrate only physically adopted package namespaces, including empty-default adoption and unknown-setting fail-closed coverage. Prepared 5.1.1 with an exact candidate-wheel gate, PATCH classification, unchanged immutable payload/catalog bytes, and promoted agent adoption guidance. | this commit | publish on `main`, verify hosted workflows and assets, then close issue #8 |
| 2026-07-20 | Published Project Standards 5.1.1 from `784d7bb` after all eight `main` workflows passed, including `Check` run `29762664859`. Signed `v5.1.1` and moving `v5` tags are live; downloaded wheel (`54340f9…fea7f`) and sdist (`68ffca6…f4de`) assets match the uploaded bytes exactly; issue #8 is closed. | this closeout | await explicit selection from the post-release queue |
| 2026-07-20 | Revised the consumer agent adoption/update prompt to resolve GitHub's latest official release once, use its immutable tag and derived package version throughout the task, and retain exact-release installation and verification without per-release prompt edits. Discovery, Markdown, frontmatter, managed-document, and reconciliation checks pass. | this closeout | await explicit selection from the post-release queue |
| 2026-07-20 | Corrected issues #9, #10, #11 and an unfiled check-workflow digest gap so released v4 consumers migrate: the platform gate accepts the "v3" wire tag every released v3/v4 CLI wrote, Python Tooling 1.3 and Markdown Tooling 1.4 carry the released digest lineage, and the new bounded-takeover contract (`unknown_content_disposition = "preserve"`) preserves consumer-modified instruction and shared-configuration files with a warning while reconciliation manages only the bounded units. Standard Bundle Authoring 2.3 documents the contract; migration tests pin released artifact bytes as fixtures; the zipapp build drops the removed `uv pip install` interface. Prepared 5.2.0 with MINOR classification and a full extracted-wheel gate. | this commit | publish `v5.2.0`, verify assets, close issues #9/#10/#11 |
| 2026-07-20 | Published Project Standards 5.2.0 from `4d2cc41` after all nine `main` workflow runs passed. Signed `v5.2.0` and moving `v5` tags are live; downloaded wheel (`20a48ea…ca0e`) and sdist (`ee865b2…ce61`) assets match the uploaded bytes exactly; issues #9, #10, and #11 are closed with resolution comments. | this closeout | await explicit selection from the post-release queue |
| 2026-07-20 | Corrected issues #12/#13 with three immutable payloads (Python Tooling 1.4 `script_ownership`, Markdown Tooling 1.5 per-caller `lint`/`format_workflow_ownership`, CLI Documentation 1.3 `workflow_ownership`) extending consumer-owned relinquishment to every package CI target; prepared the 5.3.0 candidate with a full candidate-wheel gate. Ran a 17-scenario adoption-mechanics audit over copies of every real v4 consumer with two-model adversarial verification; recorded 3 confirmed blockers, 8 confirmed majors, verifier corrections, and the full unverified inventory in `docs/reviews/2026-07-20-adoption-mechanics-audit.md`. Real repositories verified untouched. Owner held the release for a Codex fix session. | `e69831f`, `df761f5` | Codex implements audit fixes, then roll into 5.3.0 |
| 2026-07-20 | Remediated every adoption-mechanics audit finding in the held 5.3.0 candidate: control-plane migration/diagnostic/formatting fixes, conservative known-unit retirement, ownership escapes, three compatible successor payloads, complete runbook/CLI guidance, refreshed dogfood state, and explicit singleton dispositions. Independent engine/package reviews converged; an exact extracted wheel passes 3,030 ordinary tests, 80 compatibility rows, 5 performance tests, 90% coverage, package/static/coherence/dependency gates. No commit, push, tag, or publication was performed. | working tree | release remains held pending owner direction |
| 2026-07-21 | Audited consumer-facing documentation against pre-remediation commit `88a6f88`; the verified report records 17 documentation findings and 3 implementation bugs. Committed the complete adoption-mechanics remediation working tree and the report after the extracted-wheel, package, compatibility, performance, dependency, Markdown, dogfood, and handoff gates passed. No push, tag, or publication was performed. | this commit | resolve the drift report before 5.3.0 release review; release remains held |
| 2026-07-21 | Verified and closed the drift report's three implementation bugs: B-002 (`standards list` text view) and B-003 (CLI Documentation/Frontmatter default composition) were confirmed already fixed by `778c29a` with regression tests, re-probed against a rebuilt candidate wheel; B-001 fixed by implementing the documented conditional-omission contract in `spec validate` — a blockquote reason naming the deleted section with an omission marker now covers the gap, unannotated gaps still fail, and the `SV-GAP` message states the accepted shape. Full extracted-wheel gate passes: 3,041 ordinary tests, 80 compatibility rows, 5 performance tests, 90% coverage, static, package, Markdown, dependency, dogfood, and handoff checks. The 17 documentation findings remain queued. | this commit | resolve D-001–D-017 before 5.3.0 release review; release remains held |
| 2026-07-21 | Remediated all 17 documentation findings in the held 5.3.0 drift report: published examples now name live release 5.2.0, mutable authority pointers and adoption guides match Catalog 5, ownership/migration/fix guidance matches implementation, schema and errata references are current, and spec templates lead with guarded upgrades. Refreshed the unpublished Markdown Tooling 1.5 digest chain, dogfood catalog/lock, packaged spec templates, golden upgrade fixture, and candidate-aware version guard. Exact rebuilt-wheel verification passes 3,036 ordinary tests, 80 compatibility rows, 5 performance tests, 90% coverage, static, package, Markdown, dependency, dogfood, and handoff gates. No commit, push, tag, publication, or `main` update was performed. | working tree | owner reviews the held 5.3.0 candidate; publication remains explicitly unauthorized |
| 2026-07-21 | Cleared the remaining js-yaml advisory by aligning local Markdown tooling with `markdownlint-cli2-action@v24`, advanced the consumer package table and immutable install pins to 5.3.0, and prepared the authorized MINOR release on `main` after the complete extracted-wheel gate. | this commit | publish signed `v5.3.0` and moving `v5`, verify assets and hosted workflows, then close issues #12/#13 |
| 2026-07-21 | Published Project Standards 5.3.0 from `9dcec77` after all eight `main` workflows passed, including `Check` run `29835903439`. Signed `v5.3.0` and moving `v5` tags are live; downloaded wheel (`ffa5083c…fe01`) and sdist (`00432736…e817`) assets match the uploaded bytes exactly. Issues #12/#13 are closed with resolution notes, and the default branch has no open Dependabot alerts. | this closeout | await explicit selection from the post-release queue |
| 2026-07-21 | Applied all 24 findings from the post-release code-simplification review: consolidated shared package/schema, control-plane, CLI, sync, Agent Handoff, adapter, and spec logic; corrected the sync helper to return its repository root and both tool paths; retained every reviewed divergent implementation. Added direct contracts for shared helpers, UTF-8 diagnostics, report rendering, mutation lock modes, distribution projections, filesystem cleanup, and selected `spec new --json` output. An exact rebuilt 5.3.0 wheel passes 3,063 ordinary tests, 80 compatibility rows, 5 performance tests, 90% coverage, and all static, package, Markdown, dependency, dogfood, and handoff gates. | this commit | prepare and publish the authorized 5.3.1 PATCH release |
| 2026-07-21 | Prepared Project Standards 5.3.1 as an authorized PATCH release for the 24 behavior-preserving code-simplification consolidations. Updated package metadata, exact install pins, changelog, generated consumer catalogs, dogfood lock, and handoff state without changing package, schema, workflow, or immutable payload bytes. The exact candidate wheel and sdist pass 3,063 ordinary tests, 80 compatibility rows, 5 performance tests, 90% coverage, PATCH classification, and every static, package, Markdown, dependency, dogfood, and handoff gate. | this commit | publish signed `v5.3.1` and moving `v5`, verify hosted workflows and asset bytes, then record publication |
| 2026-07-21 | Published Project Standards 5.3.1 from `50d748c` after all eight `main` workflows passed, including `Check` run `29872162466`. Signed `v5.3.1` and moving `v5` tags are live; the release is Latest, and downloaded wheel (`36457e19…d8b1c`) and sdist (`765b4572…2931`) assets match the uploaded bytes exactly. | this closeout | await explicit selection from the post-release queue |
| 2026-07-21 | Prepared Project Standards 5.4.0 for issues #14/#15. Python Tooling 1.5 moves checker/pytest ownership from whole tables to canonical keys, preserves consumer `extraPaths`/`pythonpath`, and declares a safe 1.4→1.5 lock transition; Markdown Tooling 1.6 code-wraps configured globs. Catalog 5 retains every predecessor and selects both successors. The exact candidate passes 3,066 ordinary tests, 80 compatibility rows, 5 performance tests, 90% coverage, MINOR classification, and every static, package, Markdown, dependency, dogfood, and handoff gate. | this commit | publish signed `v5.4.0` and moving `v5`, verify hosted workflows and asset bytes, close issues #14/#15, then record publication |
| 2026-07-21 | Published Project Standards 5.4.0 from `74cb54b` after all eight release-commit workflows passed, including `Check` run `29883365570`. Signed `v5.4.0` and moving `v5` tags are live; the release is Latest, and downloaded wheel (`2915f4ce…a2e39`) and sdist (`d9bcfe3e…bac87`) assets match the uploaded bytes exactly. Issues #14/#15 are closed with release evidence. | this closeout | await explicit selection from the post-release queue |
| 2026-07-22 | Prepared Project Standards 5.5.0 source for issues #16–#19 after Claude-reviewed RED/GREEN implementation. The train adds bounded semantic migration signatures and compatible Markdown Tooling 1.7, Project Specification 1.4, Agent Handoff 1.4, and Standard Bundle Authoring 2.4 successors; it also corrects Project Specification finding coordinates across legacy and exact selected versions without changing validation outcomes. Catalog 5 retains every predecessor, task-level source/wheel and static/package/Markdown gates pass, and the hypothetical release classifies MINOR with no findings. | this commit | build one candidate pair from the clean release commit, qualify it without rebuild, then publish and close #16–#19 |
| 2026-07-22 | Published Project Standards 5.5.0 from `8cbb306` after all eight release-commit workflows passed, including `Check` run `29930356123`. Signed `v5.5.0` and moving `v5` tags are live; the release is Latest, and downloaded wheel (`bebc096f…c6e5a`) and sdist (`7e1f2792…96e9`) assets match the qualified bytes exactly. | this publication record | synchronize `main` and `testing`, close issues #16–#19 with release evidence, then close the active plan |
| 2026-07-22 | Closed issues #16–#19 after local and remote `main`/`testing` parity reached publication record `27f6f3c`. Each issue carries its package-specific resolution and links to the signed 5.5.0 release and hosted proof; the repository has no remaining open GitHub issues. The Claude-reviewed correction plan is complete and removed under the completed-plan policy. | this closeout | await explicit selection from the remaining user or agent queue |
- 2026-07-22: Prepared Project Standards 5.6.0 (issues #20-#23): python-tooling 1.6 additional source roots, governing-option metadata, enriched conflict diagnostics at schema 1.1, preview readiness exit codes, upgrade-doc corrections, SBA 2.5; Codex review gate passed with two accepted findings fixed (T12).
