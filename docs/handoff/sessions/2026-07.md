# Sessions — 2026-07

| Date | Headline | Commits | Bugs |
| --- | --- | --- | --- |
| 2026-07-01 | Handoff system resynced to engine v3.4: hooks + `.codex/config.toml` byte-matched, root `STATUS.md` created, `TODO.md` heading renamed, `specs-plans.md` backfilled; msgpack bumped for GHSA-6v7p-g79w-8964 | `7e2cdf2`…`a81c301` | — |
| 2026-07-01 | **Python Tooling standard review + fixes** (user-requested review; all 11 findings fixed). 🔴 §15 `check.yml` scaffold was **invalid YAML** — tab-indented (the shared `.editorconfig`'s Markdown-tab default leaked into the fence; Prettier silently skipped the unparseable block) so the manual copy-adopt path failed at workflow parse. Repaired with spaces, made byte-identical to the bundle artifact behind a bare `<!-- prettier-ignore -->` (verified Prettier's `embeddedLanguageFormatting` otherwise rewrites fence quote style), drift tests added → conventions §9. 🟠 ruff floor `>=0.9.0` couldn't run the shipped config (`target-version = "py314"` rejected until 0.12, non-preview at 0.14 — verified against 0.9/0.12/0.13/0.14) → `>=0.14` in §6/fragment/repo. 🟠 adopt CLI silently omitted §13-mandated `.vscode/settings.json`+`tasks.json` → new bundle artifacts, delivered + tested end-to-end. 🟡 dead `pytest-cov` dropped everywhere (second coverage entry point violated §3); §6 dev group synced with fragment + semantic drift test; §15 policy reworded to match its own SHA-pinned template + `permissions: contents: read` rule + direct-commit `push.branches` caveat; §25 retitled, missing 2026-06-12 entry backfilled; status paragraph → list; §18 "optional" clarified. Stale 3-failing-tests report disproven (all pass). 587 tests, 100% cov, full gate + dogfood green. CHANGELOG items added to `TODO.md`. | `752ad32`…`a2c518b` | — |
| 2026-07-01 | **Markdown standards swept for the python-tooling YAML defect class** (user-requested follow-up). No tab-broken YAML anywhere in `standards/` (all yaml fences parse; `---` "failures" are multi-doc frontmatter examples, verified with `safe_load_all`). Latent drift found + fixed: 🟠 frontmatter adopt.md §2 starter example had **dropped `**/*.template.md`** vs the delivered starter (manual adopters got a config that validates the ADR template) and Prettier had rewritten its quotes; 🟡 §3 caller example drifted from delivered `validate-standards.yml` in quote style only; 🟡 md-tooling §6 prettierrc fence byte-identical but unguarded. All three fences now byte-locked to bundle artifacts behind bare `<!-- prettier-ignore -->` (conventions §9); doc-only extras (`.obsidian/**`, references opt-in) moved to prose. New tests: starter/caller/prettierrc fence↔bundle equality (caller rendered via `major_ref()` so a v4 bump forces doc updates), no-tabs-in-yaml-fences repo-wide over `standards/`, and every partial snippet's `uses:` must name an existing workflow at the current major. md-tooling adopt manifest verified complete vs its documented deliverables (no `.vscode`-gap analog). 591 tests, 100% cov, full gate green. | `4c8543e` | — |
| 2026-07-04 | **`project-spec` (5th standard) README authored** (in-dev draft; §1–§10 written except §6 Adoption, parked pending tooling). Guarantee-driven design: §3 Features → 8 guarantees (G1–G8); §5 Tooling → capability set (`validate`/`lint`/`extract`/`next`/`new`/`upgrade` core + `status` planned + agent review contract), read-only + guarded authoring, each traced to a guarantee. Purpose/Scope (specs use own frontmatter schema, independent of Markdown Frontmatter standard), Templates, Exceptions (spined on "does the tooling refuse it?"), Update-cadence, and Source register written. Source editions verified live (29148:2018, 1016-2009, 42010:2022, MADR 4.0.0 current; OpenAPI now 3.2.0). Found + fixed **3 pre-existing red gates** the project-spec commits (453df7f/d24f276) introduced un-gated: dogfood frontmatter (excluded `standards/project-spec/**` in `.project-standards.yml`), markdownlint (dead anchor + fence in tooling-notes), ruff (check_specs.py E741/C416/RUF001/003). Full gate green after. | `838b8cb`…`1be1b0b` | — |
| 2026-07-01 | **Full cross-standard consistency review** (user-requested: internal + between-standards; adopting one must never force an ADR for another). Empirical proof first: `adopt markdown-frontmatter adr markdown-tooling python-tooling` into one repo → 15 files, zero collisions, shared artifacts delivered once; combined result passes markdownlint + prettier with the delivered configs and `project-standards validate` exit 0 once fragments merged as instructed (an earlier "duplicate `markdown:` key" scare was self-inflicted comment-stripping — the CLI's fragment output includes correct merge guidance; retracted). Fixed: 🟠 the one true adopt-A-forces-ADR-for-B conflict — python-tooling §3 banned `pre-commit` while frontmatter adopt.md ships six pre-commit hooks → scope note (prohibition targets duplicate *Python* gates; frontmatter hooks sanctioned, mirror note in adopt.md); 🟡 frontmatter README Tags section still quoted the pre-F37 pattern `^[a-z0-9][a-z0-9-]*$` vs schema's `^[a-z0-9]+(-[a-z0-9]+)*$`; 🟡 7 frontmatter templates' `id: 'replace-with-stable-id'` taught nothing about the enforced `{doc_type}-{base36}-{slug}` format (ADR templates already teach theirs) → doc_type-correct hints; 🟡 md-tooling adopt.md lacked steps for the §10 VS Code formatter blocks and §12 agent block (combined adopters got a Python-only `AGENTS.md`/`settings.json` with no pointer) → steps added; 🟢 status headers normalized to list form (md-tooling/adr/python-coding); python-coding draft: §4 SHOULD/MUST aligned, §8 `BaseException` cleanup carve-out its own example relied on, 06-12 audit entry backfilled (subagent cross-scan vs python-tooling: zero toolchain contradictions). Clean: all relative links + anchors across standards/meta/README; ADR interop (filenames/MADR sections/status mapping) consistent across all three exceptions processes; meta/versioning.md + registry + standards index agree; adr fence↔fragment semantic drift test added. 592 tests, 100% cov, full gate green. | `11af02d` | — |
| 2026-07-04 | **project-spec tooling Spec #1 — brainstormed → spec → plan, both codex-converged.** Design (`docs/superpowers/specs/2026-07-04-project-spec-tooling-design.md`): a `src/project_standards/specs/` subpackage exposing read-only `project-standards spec validate\|lint\|extract\|next` over a shared registry core parsed from the bundled templates; decomposed v1-core along the read/write seam (`new`/`upgrade` → Spec #2); `check_specs.py` retired into the pytest gate; `spec:` config block for discovery; frozen `--json` contract; reusable `validate-specs.yml`. Codex spec-review converged r2 (8 findings: consumer-validate vs template-maintainer sentinel split, non-vacuous discovery, integrity-vs-advisory rule placement, real-`spec_id` pattern, Light traceability, frozen JSON, guarantee reconciliation). 12-task TDD plan (`plans/2026-07-04-project-spec-tooling-spec1.md`); codex plan-review converged r3 (16 findings across 3 rounds: def-vs-ref uniqueness, PyYAML frontmatter parsing, discovery corpus-fallback, workflow self-repo split, SpecParseError boundary, no-verb exit 2, non-UTF-8 exit 1, DEV- uniqueness, Must-only traceability via Priority column, explicit-path exclude bypass, strict-typed primitives, full `--json` tests, built-wheel test). Docs-only session; no code changed. **Implementation delegated to Codex (parallel session).** | `403ea7c`, `9119039`, `47f5e04` | — |
| 2026-07-05 | **project-spec tooling Spec #1 implemented:** `src/project_standards/specs/` registry/document/config core + read-only `project-standards spec validate\|lint\|extract\|next`, `spec:` discovery, JSON/exit-code tests, reusable `validate-specs.yml`, bundled-template wheel smoke test, and maintainer pytest replacement for retired `check_specs.py`; test fixtures excluded from markdownlint/Prettier gates as generated parser inputs. Verified: ruff format/check, basedpyright, 651 pytest tests, coverage 98%, pip-audit, markdownlint, Prettier, dogfood frontmatter. | `2a6c4c0` | — |
| 2026-07-05 | **project-spec Spec #3 `upgrade` — brainstormed → spec → plan, both codex-converged; + validator GFM fixes + CI fix.** Design (`specs/2026-07-05-…-spec3-design.md`) + 10-task TDD plan (`plans/…-spec3.md`) for additive tier promotion Light→Standard→Full: preview-first `-i`/`-o` writes; template-faithful **source-as-spine** splice (author bytes verbatim; missing units + filler from the target-tier template); **upgradeability precheck** `check_upgradeable` (reshape-to-own-tier identity) that refuses `validate`-clean-but-non-canonical sources — the design's original "no gap prose" invariant was **empirically disproven by codex** (validate only gap-checks top-level sections), so it became an enforced precondition; Appendix A/B/D template-owned; U3 byte-exact fixture round-trip oracle; three fail-closed gates (validate + precheck + output self-validate); no `--config`. Codex spec-review converged r3 (SA-001..006 + SA-NEW-001..003: gap-prose/subsection/Appendix A-B-D ownership, config removal, package README); plan-review converged r3 "minor" 0-blocking (CR-001 strict-H1 precheck, CR-002 CI gap, CR-003 CommonMark fence-aware segmentation). **Mid-review, shipped two code fixes:** (1) `/code-review high spec` surfaced 9 defects in the shipped `validate/lint/extract/next` machinery — fixed the 3 🔴 GFM false-rejections TDD (SV-TABLE inline/escaped pipes, SV-ANCHOR GitHub repeated-heading `-1` anchors, `_OMIT` em-dash ranges; 3 tests, 723 green), 6 🟡/🟢 queued to `TODO.md`; (2) pre-existing **CI red** — `validate-specs.yml` ran `spec validate` with no `spec:` block (exit 2) → gated this-repo steps on the block's presence (auto-enables at registration). `upgrade` **not yet implemented** — next: execute subagent-driven. | `d9eb54c`…`300e4e3` | — |
| 2026-07-05 | **project-spec Spec #3 `upgrade` IMPLEMENTED** (subagent-driven execution of the 10-task TDD plan; final whole-branch review = merge-ready). Additive tier promotion Light→Standard→Full: pure source-as-spine splice core (`upgrade.py`) + CLI shell (`_run_upgrade`) — `SRC --to {standard\|full}`, `-i`/`-o`/preview, three fail-closed gates (source-invalid → upgradeability precheck → output self-validate), additive-only tier direction, full `--json` contract; shared `_safe_atomic_write` extracted from `new`. **U3 byte-exact round-trip oracle green** (light→std, light→full, std→full all byte-identical + output-validates). Notable judgment calls: 🔴 **normalized 3 tier-variant Appendix-C cross-refs** out of the standard/full templates' shared sections (§9/§12.2/§19 MS-2) so the shared-section-tier-invariant property holds (user-approved) — bundled `src/` template copies re-synced; 🟡 **§17.1 (Definition of Done) design refinement** — it is legitimately tier-variant boilerplate (light's DoD can't reference §17.3/§18.7/§13.6), so the merge reconciles it *from the target* like Appendix A/B/D (`_TEMPLATE_OWNED_SUBS`); 🟡 fixed two latent bugs the oracle surfaced — `_block_key` collapsed subsection keys (§7.1→`"7"`), and stale whole-section omission notes rode on source-kept last subsections (`_swap_tail` now reconciles kept-subsection trailing filler); 🟡 one review Critical (`_is_filler` over-trim) adjudicated down — guarded system-wide by the reshape-identity precheck (`check_upgradeable` refuses non-canonical scaffolding rather than silently losing author bytes) + proving test. Rebased onto `origin/testing` (trivial `.vscode` commit `ac87e26`) and pushed. Full gate: 781 passed, 98% cov, basedpyright 0/0/0, ruff + pip-audit + dogfood 16/16 clean. | `904d209`…`b84103a` | — |
| 2026-07-05 | **`project-spec` (5th standard) REGISTERED; deferred bugs/gaps closed; CHANGELOG resolved to MAJOR.** Fixed 3 🟡 code-review bugs (`next_id` width-exhaustion + tier-default mismatch, `lint` case-fold) + 2 🟢 cleanups (O(n²) line scan → `bisect`, duplicate appendix/prefix regexes consolidated into `registry.py`), all TDD'd. Fixed 3 residual `format-frontmatter`/`cli.py` gaps (typo'd `--config` silent-default, non-UTF-8 traceback, misleading `--glob` help text). Wrote `standards/project-spec/adopt.md` in full (discovered the reusable `validate-specs.yml` CI workflow already existed, undocumented); registered the standard — `spec:` config block live, real frontmatter added to README/adopt.md/resources, a dogfood example (`examples/spec.example.md`) added and locked in by a new test, top-level `README.md`/`standards/README.md`/`meta/versioning.md` updated. Caught a real gap along the way: a maintainer test assumed every standard's `examples/` used canonical frontmatter — scoped it and added an equivalent `spec validate`/`lint` dogfood test. CHANGELOG `[Unreleased]` fully itemized: `project-spec` registration classified MINOR; six validator/config strictness bumps + the Python Tooling ruff-floor raise verified (via a dispatched fork) and classified MAJOR each on their own (previously-passing rule, no judgment call); `pytest-cov` removal's classification resolved by reading its actual removal commit — confirmed never load-bearing, PATCH. **Next release off `testing` is MAJOR (v4.0.0).** Full gate: 790 tests, 98% branch cov, ruff/basedpyright/pip-audit/dogfood clean throughout. | `8616fc8`…`d1e0847` | — |
| 2026-07-05 | **Docs pruned for v4.0.0 release prep** (user-requested). Deleted 53 docs: `docs/codex-reviews/` (31 completed spec/plan/code review rounds), `docs/fable-findings.md` (55/58 implemented, outcome in this log), `docs/python-backend.md` (resolved scratch/advisory distribution notes), and every implemented plan under `docs/superpowers/plans/` + the `v1.1.0/` planning archive (11 files) — implementation verified per-plan against CHANGELOG/state/code by a parallel audit. **Kept:** all specs (durable design record) and `2026-06-08-check-drift.md` (pending 2.2.0 feature, never built — no `check.py`/`lock.py`/`check` subcommand). Distribution folders audited by a second parallel agent: `src/.../bundles/` has zero orphans (every file named by an `adopt.toml`); `standards/` templates/examples all realize documented `doc_type`s (incl. the `index` `repo-pages/` scaffold) — nothing removed. `specs-plans.md` pointer table pruned (deleted-plan rows dropped, implementation status folded into design rows). Dogfood `validate-frontmatter` green (19 files). | _pending_ | — |
