# Deployed

**Last updated:** 2026-08-12

This repo is consumed as a versioned standard: downstream repos pin a `standards-ref` to a git tag and call the reusable workflow under `.github/workflows/`. "Deployed" here means published git refs on `main`.

| Ref | What it is | Status |
| --- | --- | --- |
| `v1.0.0`–`v1.0.2` | Initial standards + validator + reusable workflow | published on `main` |
| `v1.1.0` | optional `consumer` field; `schema_version` accepts `1.1` | published on `main` |
| `v1.2.0` | adoption runbook (shipped then as `standards/adoption.md`, since relocated to `standards/markdown-frontmatter/adopt.md`); pinning hardened; validator crash-safety | published on `main` |
| `v1` (frozen) | last `v1.x` release — `v1.2.0` (commit `7450170`); does **not** advance to 2.0.0 (breaking), so `@v1` trackers stay on 1.2.0 | published on `main` |
| `v2.0.0` | lint/format + MADR-4, Python Tooling SSOT migration (uv_build, src/ layout, basedpyright, coverage, pip-audit), per-standard bundle restructure (`standards/<name>/` + `meta/`), Markdown Tooling Standard (`standards/markdown-tooling/` + `lint-markdown.yml@v2`), per-standard contract versions, Python baseline 3.13→3.14, `setup-uv` SHA-pin. **BREAKING** — `requires-python` `>=3.11`→`>=3.14`; see `CHANGELOG.md` migration notes. | published on `main` |
| `v2` (frozen) | last `2.x` release — `v2.0.0` (commit `3ece2c9`); does **not** advance to 3.0.0 (breaking), so `@v2` trackers stay on 2.0.0 | published on `main` |
| `v3.0.0` | `format-frontmatter` + opt-in `validate-references` + `project-standards fix`; `validate` runs all three validators; `.pre-commit-hooks.yaml`. **BREAKING** — `validate-id` now runs in the reusable CI workflow (old-style kebab ids fail) and `parse_frontmatter` rejects duplicate top-level keys; see `CHANGELOG.md` "Migration from v2" + [`UPGRADING.md`](../../UPGRADING.md). | published on `main` |
| `v3` (frozen) | last `3.x` release — `v3.0.0` (commit `e69ab6b`); does **not** advance to 4.0.0 (breaking), so `@v3` trackers stay on 3.0.0 | published on `main` |
| `v4.0.0` | Project Specification Standard (5th standard: tiered templates, `spec` CLI, `validate-specs.yml` reusable workflow). **BREAKING** — six validator/config strictness bumps (datetime dates, tag pattern, non-string keys, duplicate config keys, unquoted numeric `version`, nonexistent path → exit 2) + `validate-references` semantic corrections (opt-in only) + Python Tooling ruff floor `>=0.14` on re-sync; see `CHANGELOG.md` "Migration from v3" + [`UPGRADING.md`](../../UPGRADING.md). Release commit `c7c2fd8`; GitHub release live. | published on `main` |
| `v4.1.0` | Project Specification: `spec.reference_prefixes` config key + token hygiene (built-in `ADR` prefix, SPDX/license-token skip), opt-in `spec upgrade --config`. Backward-compatible loosening — **MINOR**; `@v4` inherits. Release commit `84c0054`; GitHub release live. | published on `main` |
| `v4.2.0` | Markdown Tooling: opt-in reusable Prettier gate (`format.yml` dual-role + adoptable `format.caller.yml`, pinned Prettier `3.8.3`, `prettier: false` opt-out), `markdown_tooling 1.0→1.1`, DEC-9→DEC-10, repo-local coherence tool. Additive/opt-in — **MINOR**. Release commit `6614612`; GitHub release live. | published on `main` |
| `v4.3.0` | CLI Documentation Standard (6th standard: `standards/cli-documentation/` bundle, adopt artifacts `docs/usage.md` scaffold + `cli-docs-check.yml` template + config fragment, `cli_documentation` contract `1.0` in `registry.json`), `--version` on all seven console scripts, `--help` fixed on the two sync commands, dogfood `docs/usage.md` + inventory-parity/installed-wrapper tests. Additive — **MINOR**; `@v4` inherits. Release commit `74db623`; GitHub release live. | published on `main` |
| `v4` (moving) | tracks the newest 4.x release (`v4.3.0`) | published on `main` |
| `v5.0.0` | Unified `.standards/` control plane, catalog 5 packages, migration and composition engine, source/wheel compatibility matrix, and updated reusable workflows. **BREAKING**; see [`UPGRADING.md`](../../UPGRADING.md). Release commit `8869a08`; signed tag and GitHub release assets live. | published on `main` |
| `v5.0.1` | Drift-audit corrections for bounded provider integrity, Agent Handoff error classification, package-contract enforcement, source/wheel parity, and current Catalog 5 documentation. **PATCH** — no immutable payload, catalog selection, public command, accepted input, or conforming consumer outcome changed. Release commit `0390b9e`; signed tag and GitHub release assets live. | published on `main` |
| `v5.0.2` | Additive internal `standard-bundle-authoring@2.1` payload correcting the 2.0 SPEC-BA02 pointer, plus the internal-additive PATCH classification rule. Release commit `c731955`; signed tag and GitHub release assets live. | published on `main` |
| `v5.1.0` | Backward-compatible Catalog 5 default advances across all seven consumer packages, Python 3.14 floor alignment, consumer lock schema 1.1 writes, synchronized CLI/consumer documentation, and the complete accepted implementation-review correction train. **MINOR**; every superseded payload remains advertised. Release commit `b69600d`; signed tag and byte-verified wheel/sdist assets live. | published on `main` |
| `v5.1.1` | Partial Catalog 4 migration correction: only physically present package namespaces migrate; empty namespaces adopt defaults, unknown settings remain blocking, and malformed values fail closed. Content-safe provider coordinates and the promoted agent adoption prompt are included. **PATCH**; immutable payload and catalog bytes are unchanged. Release commit `784d7bb`; signed tag and byte-verified wheel/sdist assets live; issue #8 is closed. | published on `main` |
| `v5.2.0` | V4→V5 migration unblock for released v4 consumers: the platform gate accepts the `"v3"` wire tag, Python Tooling 1.3 / Markdown Tooling 1.4 carry the released digest lineage, and the bounded-takeover contract (`unknown_content_disposition = "preserve"`, warning `CP-MIGRATION-BOUNDED-TAKEOVER`) preserves consumer-modified instruction and shared-configuration files. Internal Standard Bundle Authoring 2.3 documents the contract. **MINOR**; every superseded payload remains advertised. Release commit `4d2cc41`; signed tag and byte-verified wheel/sdist assets live; issues #9, #10, #11 are closed. | published on `main` |
| `v5.3.0` | Consumer-owned relinquishment for customized package scripts, workflows, and documentation; complete adoption-mechanics and consumer-documentation audit remediation; corrected migration diagnostics, rendering, recovery, and lock semantics; conditional-section tailoring; and the patched Markdown development toolchain. **MINOR**; every superseded payload remains advertised. Release commit `9dcec77`; signed tag and byte-verified wheel/sdist assets live; issues #12 and #13 are closed. | published on `main` |
| `v5.3.1` | Twenty-four verified internal consolidations replace duplicate package/schema, control-plane, CLI, sync, Agent Handoff, adapter, and specification logic with shared helpers and direct contract tests. **PATCH**; accepted inputs, consumer outcomes, defaults, package/catalog selections, schemas, workflows, and immutable payloads are unchanged. Release commit `50d748c`; signed tag and byte-verified wheel/sdist assets live. | published on `main` |
| `v5.4.0` | Python Tooling 1.5 preserves consumer checker/pytest extensions through key-level ownership and a declared lock-scope transition; Markdown Tooling 1.6 code-wraps managed instruction globs for strict MD037/MD049 policies. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `74cb54b`; signed tag and byte-verified wheel/sdist assets live; issues #14/#15 are closed. | published on `main` |
| `v5.5.0` | Bounded semantic migration signatures; Markdown Tooling 1.7 disabled-formatter migration; Project Specification 1.4 empty-corpus success; Agent Handoff 1.4 managed-envelope size accounting; and absolute Project Specification finding coordinates. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `8cbb306`; signed tag, eight passing release-commit workflows, and byte-verified wheel/sdist assets are live; issues #16–#19 are closed. | published on `main` |
| `v5.6.0` | Python Tooling 1.6 `additional_source_roots` merged into checker include, coverage source, and Ruff src with V4 migration recognition; governing-option contribution metadata; enriched `CP-CONSUMER-CONFLICT` diagnostics (expected/actual values, digests, governing options, explicit JSON nulls) at reconciliation-plan schema 1.1; readiness-signal migration preview exit codes; corrected upgrade docs; internal Standard Bundle Authoring 2.5. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `55ac756`; signed tag, eight passing release-commit workflows (Check run `29942273464`), and byte-verified wheel/sdist assets are live; issues #20–#23 are closed. | published on `main` |
| `v5.7.0` | Python Tooling 1.7 per-root coverage scoping (`additional_source_roots` string or `{ path, coverage }` entries; coverage-false roots stay in checker `include` and Ruff `src` but out of `coverage.run.source`) with 1.6 → 1.7 and V4 migration carriage; anchored TOML managed-region comment preservation (full-span consumption, no blank-line residue, contract documented in `UPGRADING.md`). **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `06162ec`; signed tag, eight passing release-commit workflows (Check run `29951568179`), and byte-verified wheel/sdist assets are live; issues #24–#25 are closed. | published on `main` |
| `v5.8.0` | Adoption-friction correction train (issues #26–#31): Python Tooling 1.8 `pytest.test_paths` collection roots with governing-option declarations; Markdown Tooling 1.8 `known_content_digests` accepting the observed literal `.markdownlint.json` byte form; Markdown Frontmatter 1.5 `workflow_ownership` consumer-owned relinquishment; minimal-escape frontmatter serialization converged with the pinned Prettier oracle (acceptance-preserving); whole-file `CP-CONSUMER-CONFLICT` first-difference pointers (expected-side only); `format-frontmatter` explicit mode resolution and named-skip diagnostics; factual legacy-authority note. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `d007ba0`; signed tag, nine passing release-commit workflow runs (`Check` run `30002838425`), and byte-verified wheel/sdist assets are live; issues #26–#31 are closed. | published on `main` |
| `v5.9.0` | Adoption-integrity correction train (issues #32 and #35–#49): Python Tooling 1.9 bounded configuration and performance-gate-preserving transforms; Markdown Tooling 1.9 caller, exclusion, permission, and formatting safety; Agent Handoff 1.5 reconstructable exclusions and diagnostics; CLI Documentation 1.4 deterministic multi-CLI indexing; internal Standard Bundle Authoring 2.6 direct-transform contract; structured diagnostics and exact release-consistency checks. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `7e630554cf0f1ab014ba902e46834f0c1485cc5a`; signed tag and byte-verified wheel (`3c38ba9822fca15c3b1fce68596aed3f6ac3c289ea82a56bb916af9f3177f729`) and sdist (`9174877245af2a797c5ec7681e55543361fd696ba3467e51f33e80a8b37b59a1`) assets are live. The release-commit `Check` exposed an ANSI-sensitive test oracle; test-only correction `cb5ba6d` preserved the wheel bytes and passed hosted `Check` run `30261345119`. Issues #32 and #35–#49 are closed. | published on `main` |
| `v5.10.0` | Validation-fidelity correction train (issues #50–#54, #56–#61, #63–#69): shared specification-engine acceptance of numbered subsections under present canonical parents, depth-one compound external identifiers, inline-code-masked angle groups, and GFM autolinks; `standards show` schema-resolved effective-configuration digests with an unverifiable-installation fallback; an absent-`standards_version` hint that names the `"v4"` literal and its `UPGRADING.md` section; create-only/`PRESERVE` whole-file targets never planned for removal on a zero-byte render; Python Tooling 1.10 `check.py` argument contract, numeric `python_version`-gated future import, unconditional `.mypy_cache` removal, companion errata, and required-check rename warning; Markdown Tooling 1.10 generated-directory lint exclusions with a `lint_generated_exclusions` opt-out; Agent Handoff 1.6 table-scoped session-log caps and locatable numbered-rules findings; Markdown Frontmatter 1.6 release-tag-pinned installed documentation. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `4caeb1046342f14078c5aad61254d5751900511d`; signed tag, nine passing release-commit workflow runs (`Check` run `30292732694`), and byte-verified wheel (`c51d1e7829d121dd7eb9aa0c00b393e421835e04e02337c06a15fc7da435c936`) and sdist (`7b44fdb69673d8e150b9690fe7ce67167b08d3bafc90475252aaa048c9f8bf80`) assets are live. Issues #50–#54, #56–#61, and #63–#69 are closed — #64 was already fixed in Markdown Tooling 1.9 and is now regression-pinned, and #67 closed as consumer-owned content with no code change; #55, #62, and #69's configurable-threshold request remain deferred. | published on `main` |
| `v5.11.0` | Correction train closing tracked issues #70–#74: create-only deletion drift explained and let apply converge (#70) with the verification skip re-licensed from the lock rather than the action; Agent Handoff engine `shape.defaults.max_paragraph_chars` fallback honored (#71); Agent Handoff engine reads `max_rule_summary_chars` and `require_tables_or_bullets` from the masked view for provider parity (#73); `standards show` no longer silently degrades to the authored-config digest when payload facts are missing or mismatched, and discloses its fallback basis (#74); adr 1.3, cli-documentation 1.5, and project-spec 1.5 drop mutable release links carried over from the #53 fix (#72). Verified by two rounds of internal adversarial review plus an external Codex review (4 findings: 1 already-fixed, 3 accepted and fixed). **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `ab75635`; signed `v5.11.0` tag (GPG `9375AFEFA6F841B0`, verified GOODSIG), force-moved `v5` tag, and byte-verified wheel (`53e93aa6…`) and sdist (`4c3b36b2…`) assets are live; the release is marked Latest. All nine release-commit workflows passed (`Check` ~41 min). Issues #70–#74 are closed with release references; #55 and #62 remain open-deferred; new trackers #75 (enrichment FIFO mispairing), #76 (planner policy-transition asymmetry), and #77 (CREATE-side truthiness for empty managed artifacts) are open. Post-release, `96b21bd` overrides `js-yaml` to `5.2.2` for a Dependabot high-severity DoS advisory while keeping `markdownlint-cli2` at `0.23.1` for action alignment; drop the override once `markdownlint-cli2-action` ships `>=0.23.2`. | published on `main` |
| `v5.12.0` | Ships the MCP server (`project-standards mcp`: dual-era stdio, three `standards://` resource forms, six read-only tools) plus the owner-approved 13-issue correction train: Markdown Frontmatter 1.7 (#97), Markdown Tooling 1.11 (#100), and Agent Handoff 1.7 (#94) join Catalog 5 with every predecessor retained; engine fixes #78, #79, #81, #82 and docs fixes #85, #92, #93, #96, #103, #104 round out the train. Verified by the full battery (4,329/4,329 tests, 90% coverage), `check-release --baseline v5.11.0` classifying MINOR with no findings, two rounds of external Codex review (round 1: 9 findings, 7 fixed + 2 risk-accepted; round 2: 6/7 RESOLVED + 1 risk-accepted documented residual), and an MCP §13 regression gate reporting NO-REGRESSIONS on the release wheel (client matrix revision 0.6, commit `932219c`). **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `932219c`; signed `v5.12.0` tag (GPG `9375AFEFA6F841B0`), force-moved `v5` tag, and byte-verified wheel (`766a1554…`) and sdist (`fd9f6c23…`) assets are live; the release is marked Latest. The release-commit `Check` (run `30631582570`) exposed an unfaithful uv-strict test sandbox on hosted runners — the test's PATH+HOME-only child environment stripped `setup-uv`'s managed-interpreter directory, so `uv run --no-project python3` had no managed interpreter to resolve and fell through to the rejecting shim; six other release-commit workflows passed. Real uv-strict setups always provision a managed interpreter, so the shipped Markdown Frontmatter 1.7 payload was correct and the release unaffected. Test-only corrections `0ff5438` and `9db6a83` preserved the wheel bytes (sha256 unchanged, `766a1554…`) and passed hosted `Check` run `30633727426`. Issues #78, #79, #81, #82, #85, #92, #93, #94, #96, #97, #100, #103, and #104 are closed with release references; #102 was reproduced live on v5.11.0 and stays open-deferred; new trackers #105 (TOML batched-CREATE collision), #106 (JSONC residue layouts), and #107 (engine/provider uppercase-identifier divergence, owner decision needed) are open; #55, #62, and #84 remain unchanged. | published on `main` (Latest) |
| `v5.13.0` | Efficiency train: the release-gate wall-clock program — pytest-xdist parallel ordinary lane, `sys.monitoring` coverage core bridged to workers by `tests/conftest.py`, persistent 4M-inode tmpfs, concurrent non-uv statics lane, `scripts/verify.sh` as the canonical gate with a `--full` legacy cross-check — plus `scripts/release_prep.py` mechanical release preparation, the trimmed local verification policy, and local `prettier --cache`. One engine fix ships in the wheel: `format-frontmatter` validates arguments before taking the repository lock (usage errors no longer acquire an exclusive lock or surface `CP-BUSY`). The planned self-hosted-runner migration was deferred after an adversarial security review (`docs/research/2026-07-31-self-hosted-runner-security-review.md`). Verified by the fast gate AND a legacy-configuration serial battery on the same candidate (4,192 ordinary + 133 compatibility + 5 performance; identical 90% coverage reports, 20898/1811/7728/943), `check-release --baseline v5.12.0` classifying patch with zero findings (MINOR is the owner's train designation), and an adversarial verifier pass over the train diff. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `52d23ca`; signed `v5.13.0` tag (GPG `9375AFEFA6F841B0`, `tag -v` verified), force-moved `v5` tag, and byte-verified wheel (`84812edb…`) and sdist (`685e8d5a…`) assets are live; the release is marked Latest. The release-commit `Check` (run `30662978655`) exposed a fresh-checkout-only race — first-import `__pycache__` creation mutates parent-directory mtimes mid-run under xdist, racing the read-only real-root digest proof — and the first correction's `src/` compileall littered the symlink-only payload projection (run `30663987887`). A tests/-only warmup left the race open — an instrumented cold-clone repro then proved clean-env subprocess tests import the editable install from `src/` mid-run (run `30664963271`). Test-only corrections `836ea78`, `898f37a`, and `988ffe5` (warm tests/ and the src package, excluding the payloads/bundles resource mirrors) preserved the wheel bytes; hosted Check `30666389705` passed the post-release gate/test corrections, not the immutable tagged artifact. | published on `main` (Latest) |
| `v5.14.0` | Repository-hygiene correction train: Agent Handoff 1.8 fixes SessionStart under uv-strict shims; Markdown Frontmatter 1.8, Markdown Tooling 1.12, and Project Specification 1.6 pin managed workflow actions; release classification follows the owner-approved MAJOR/MINOR/PATCH catalog rule; and the exact-wheel release gate and preparation workflow fail closed. **MINOR**; every predecessor remains byte-immutable and selectable. Release commit `b4be9d2e`; signed `v5.14.0` and `v5` tags, successful release-commit Check run `30725509522`, and byte-verified wheel (`02d989a5…`) and sdist (`139808e7…`) assets are live; the release is marked Latest. | published on `main` (Latest) |
| `v5.15.0` | Open-issue resolution program release (widened Agent Handoff surface): Agent Handoff 1.9 fixes the Claude SessionStart spawn form — the 1.8 `"args": []` key selected exec form so the wrapper never reached a shell (#122, #124 duplicate); the producer role at consumer-config schema 1.1 permits installed≠committed catalog at an equal tool release for producing repositories with consumer behavior byte-identical (REQ-908); release lineage is asserted only by advancing commands so `validate`, reconcile previews, and the agent-handoff group answer mid-cycle (#123); read-only commands resolve from the fully-authenticated applied lock and the pre-apply size/shape report checkpoint is restored (#91, #101); the legacy inventory authenticates startup evidence against the lock while duplicate SessionStart injection is detected (#90, #102); shape enrichment pairs by rule identity (#75); the engine matches the published secret-reference rule (#107); adr 1.4 and python-tooling 1.11 are promoted as defaults with predecessors retained; cryptography 50.0.0 clears PYSEC-2026-3552. **MINOR**; all predecessors remain byte-immutable and selectable. Tagged commit `5e9bb6cc` (cold-runner test/CI corrections atop the fully-green qualification commit `f455feba`; wheel byte-identical); signed `v5.15.0` tag (object `92ea1b64`) and re-pushed `v5` tag (object `5655d17c`); hosted `Check` run `30977834400` green on the tagged commit; byte-verified wheel (`7c423d7f…`) and sdist (`06848c64…`) assets are live; `uvx` from the immutable tag reports 5.15.0. Issues #75–#77, #83, #86, #87, #89–#91, #95, #98, #101, #102, #105–#107, #109, #122, #123 are closed with release references; #84 closed as an accepted external-cause disposition; #124 closed as a duplicate of #122. Qualification and closure evidence: EV-007 and EV-011 under `docs/research/`. | published on `main` (Latest) |
| `v5.16.0` | Defect-cycle release closing every open bug issue and superseding all six open dependency PRs: markdown-tooling 1.13 bounds the documented Prettier/markdownlint commands to the declared corpus via Git-tracked selection with `:(glob,exclude)` typed exclusions (#88, #114, #119); python-tooling 1.12 decomposes `table:/tool/ruff` into eleven leaf units so plugin sub-tables are consumer-owned (#99), renders the gate script as a Ruff-format fixed point at any root count and line length (#115), documents the consumer-owned BasedPyright `exclude` contract (#117), and emits `PT-DECLARED-ROOT-MISSING` for declared roots nothing created (#118, with the config-aware consumer-state engine seam); project-spec 1.7 and markdown-frontmatter 1.9 carry the new `runner-labels` reusable-workflow input; engine fixes make `spec lint --strict` gate on structural recognizability (#121) and the default reconcile preview list computed package advances (#126); #108 closed as already-remedied. Reviewed pins: setup-uv 9.0.0 (`prune-cache: true`), setup-node 7.0.0, setup-python 7.0.0, prettier 3.9.6 (engine width tables realigned with bundle-derived guards), markdownlint-cli2 0.23.2 — superseding PRs #33/#110/#111/#112/#113/#125; the `js-yaml` override stays until markdownlint-cli2-action ships >=0.23.2. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `8a2d1b9a`; signed `v5.16.0` tag (object `8f93871e`) and re-created `v5` tag (object `d83c0817`); full local battery and complete hosted fleet green on the tagged commit; byte-verified wheel (`02f51b12…`) and sdist (`bf3875f9…`) assets are live; `uvx` from the immutable tag reports 5.16.0. Issues #88, #99, #108, #114, #115, #117, #118, #119, #121, #126 closed with release references; #55, #62, #116 annotated open-deferred to the feature phase. Evidence: EV-008 under `docs/research/`. Verification included a 13/13-confirmed fresh-context adversarial pass over the full commit range. | published on `main` (Latest) |
| `v5.17.0` | GitHub Workflow release: `github-workflow@1.0` is advertised as Catalog 5's eighth consumer package — the repo-local skill plus the committed, reproducibly built linux/amd64 `gh-workflow` binary with its frozen nine-subcommand surface, including `set --type` assigning organization Issue Types with offline schema validation and response read-back (GitHub answers a type PATCH from a token without push access with 200 and the unchanged previous type); markdown-tooling 1.14, project-spec 1.8, and markdown-frontmatter 1.9→1.10 add the top-level `runner_labels` option rendered through managed caller workflows to the reusable workflows' `runner-labels` input (#132), with unset labels proven byte-identical to the predecessors; agent-handoff 1.10 (Go SessionStart hook, #138/#140) is promoted default; the release checklist gains the doc-audit, reconcile-ordering, and publish-and-close-out steps. Verified by three independent fresh-context verifiers — two refuted live-plan prose staleness (eleven sites, fixed in the release tip) and the adversarial pass confirmed all seven claims including digest recomputation and `build-gh-workflow.sh --verify` — plus the full pre-tag chain on `main` including `verify.sh --full`, with `check-release --baseline v5.16.0` classifying MINOR with no findings. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `29c875ab`; signed `v5.17.0` tag (object `20a3d175`) and re-created `v5` tag (object `ca954c70`); byte-verified wheel (`36bc8e29…`) and sdist (`2cfe6fe2…`) assets are live; the release is marked Latest; `uvx` from the immutable tag reports 5.17.0; the complete hosted fleet is green on the tagged commit (`Check` run `31302517361`) with no post-tag corrections. Issue #132 is closed with a release reference; #129 retitled to the v5.18.0 train; follow-ups #143–#147 are filed in Inbox. Consumer-pin rollout: deliberately deferred to owner scheduling — `@v5` trackers inherit automatically and no exact-pin consumer bumps were performed this session. | published on `main` (Latest) |
| `v5.18.0` | ADR train release closing 14 issues through four package cuts: `adr@1.5` gives partial amendment a first-class vocabulary — reciprocal `project.amends`/`project.amended_by`, a sanctioned blockquote note, an optional `### Amendments` subsection, and the amendment-vs-supersession distinction rule — strictly additive at `schema_version` 1.1 with validator bytes unchanged (#127); `python-tooling@1.13` adds the typed `ruff.extend_per_file_ignores` table that composes into the package-owned per-file-ignores rather than replacing it, deliberately leaving Ruff's own `extend-per-file-ignores` consumer-owned so the documented pre-1.13 workaround does not become `CP-MODIFIED-MANAGED` drift (#116); `agent-handoff@1.11` cuts the adoption guidance three consumer reports asked for — version-correct heading (#148), Markdown Tooling typed `exclusions` first (#139), and the narrow `check-added-large-files` path exemption for the compiled launcher (#151) — with every option, artifact, and provider byte identical to 1.10; `github-workflow@1.1` is a tool/documentation cut over 1.0's unchanged option surface and schemas, removing the ledger read timestamp so `docs/GH-WORKFLOWS.md` is a function of work state alone (#154, spec revision 1.8 amending FR-019), deriving `new`'s Issue-Type guidance from the loaded schema (#144), adding the family-root `adopt.md`/`agent-summary.md` (#145), and correcting the ledger-mutation and pre-commit-exemption guidance (#149, #151). Engine fixes: `agent-handoff legacy-report` answers before the package is selected, resolving the installed catalog's default payload and disclosing that basis, with the exemption scoped to that inventory alone (#130); `validate`/`drift-check` probe the interpreter a pre-1.8 launcher would select and emit `AH-LAUNCHER-INTERPRETER` instead of passing a repository that injects no context (#141). Corpus: all 23 active ADRs now answer the four-part reader test from Decision Outcome alone, the five requirements stranded in `### Consequences` moved into the bounded outcome, five contested authorities closed reciprocally, and eleven banners converted to dated reciprocal frontmatter — no accepted outcome sentence rewritten in place (#128). New [ADR 0028](../adr/adr-0028-create-only-artifact-refresh.md) was accepted and **amended in the same release**: the delete-and-reconcile refresh was refuted by the engine because `CP-CREATE-ONLY-ABSENT` is permanent, so the sanctioned refresh of a create-only artifact is a manual copy; `docs/adr/adr.template.md` was refreshed to the 1.5 bytes that way and the lock retains the creation digest by design. Test fixes make the MCP timing and determinism proofs load-independent without relaxing a proof (#147), and collapse the duplicated retained-version digest assertions (#146). Three same-class payload corrections shipped then were caught in-train — stale embedded predecessor version references in successor schemas (`github-workflow` provider-input `const`, `agent-handoff` migration-report `const`, `python-tooling` config-transform migration `enum`) — now guarded by `assert_schema_payload_references` in `tests/package_contract/helpers.py`; the class is blind to the five validators and tracked as #156. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `68203eca`; signed `v5.18.0` tag (object `ae2dd54e`) and re-created `v5` tag (object `29b2cb68`); byte-verified wheel (`914faaa04cb3244831ae62038f165b43dae02a4091b80a260685ca78878ad303`) and sdist (`75c7dca36efeebd27d7592bf5eddd300a52936cc8ecf96bb866baa49713522f1`) assets are live; the release is marked Latest; the `uvx` probe from the immutable tag is green. Issues #116, #127, #128, #130, #139, #141, #144, #145, #146, #147, #148, #149, #151, #154 are closed with release references; #129 stays open — the `adr-conformance` foundation was deliberately excluded, and no `adr-conformance` family or provider-input capture shipped. Follow-ups #156–#167 are filed in Inbox; #155 carries the `rexec` first-use evidence. First release train to use `rexec`: the full serial gate ran remotely in 17.9 min against 38–48 min locally, while builds stayed local because ~12–15 s of sync dominates ~2.1 s of compute; release verification could not offload because the release worktree's `.git` pointer is not synced (#167, L3DigitalNet/remote-execution#2). | published on `main` (Latest) |
| `v5.19.0` | Consolidation release completing the open-issue resolution program: Project Specification 1.9 strict conformance and preservation-first import; the shared bounded command-provider runner; GitHub Workflow 1.2 REST-only guidance and platform declaration; four promoted package successors; create-only advisory, ADR, catalog, release-reference, and migration corrections. **MINOR**; predecessors remain byte-immutable and selectable. Release commit `127bd3dd`; signed `v5.19.0` tag object `e8460a29` and moving `v5` tag object `ae60b9e7`; final hosted Check `31566383442` and companion workflows green; byte-verified wheel `134d5abc…` and sdist `c1605cde…` assets are live. All 19 cited issues are closed as completed: #55, #62, #140, #142, #143, #153, #156–#167, and #169. The governing 27-report program is also terminal. Consumer-pin rollout: `@v5` trackers inherit automatically; no exact-pin consumer repositories were changed. | published on `main` (Latest) |
| `v5.20.0` | Claude Code skill-discovery repair train (#170–#172): verified 2026-08-15 that Claude Code discovers project skills only from `.claude/skills/` and never read `.agents/skills/` (the Codex convention), so `agent-handoff@1.13`, `github-workflow@1.3`, and `markdown-frontmatter@1.12` install every skill file to both trees as byte-identical, digest-locked managed copies (copies over symlinks: Git checks symlinks out as plain text on Windows clones without Developer Mode); the package contract accepts one payload source at several targets when every declaration pins the same digest, keeping one inventory entry per path so prior aggregate digests are unchanged, with conflicting digests still refused; provider drift coverage treats both trees symmetrically and `provider_inputs` derives read sets from payload-declared artifacts (a recorded step toward its retirement); the agent-handoff legacy report canonicalizes the `.claude` skill tree (caught in-train); the two umask-fragile control-plane tests are mode-pinned (#172). ADRs 0016 and 0021 amended 2026-08-15; ROADMAP renumbered — `project-toolbox` moved to 5.21.0 (#168 retargeted). Acceptance proven live: this repository's Claude Code session lists all three skills from the installed `.claude/skills/` copies. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `a7d2d688`; signed `v5.20.0` tag (object `1ed1e1f9`) and re-created `v5` tag (object `94165cfd`); full serial battery green on the release commit via `rexec` (statics 1:06, ordinary 39:17, compatibility 48:56, performance 0:54 — first release proven end-to-end remotely under `[sync] git_context`); byte-verified wheel (`5bd89c61…`) and sdist (`82815d27…`) assets are live; the release is marked Latest. Issues #170–#172 closed with Done/completed pairing; follow-ups #173–#175 and remote-execution#9 are filed. Consumer-pin rollout: deliberately deferred to owner scheduling — `@v5` trackers inherit automatically; no exact-pin consumer repositories were changed. | published on `main` (Latest) |
| `v5.21.0` | New Catalog 5 consumer family `project-toolbox@1.0` (ninth consumer package): two managed workflow checklists at `.standards/packages/project-toolbox/workflows/` (repo-housekeeping, drift-detection) plus a routing skill installed dual-tree with `openai.yaml`; no providers/contributions/options/migrations; soft-dependency posture; governed by design brief `docs/specs/2026-08-16-project-toolbox-package-design.md` (#168). Plus #176 fix: `check-release` baseline loader accepts same-path-same-digest declarations, with regression tests. Self-hosted: enabled in `.standards/config.toml`, reconciled clean (`drift:false`). **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `a625091c87e1d3cd568edbbb6d2531709d7cbc70`; signed `v5.21.0` tag (object `ce4c9cea`, GPG-verified GOODSIG) and re-created `v5` tag (object `a4a5ca9f`); byte-verified wheel (`155d56f1…`) and sdist (`c8812f20…`) assets are live (downloaded assets match local digests; sdist-derived wheel matches direct wheel entry-for-entry); the release is marked Latest; `uvx` probe from the immutable tag reports 5.21.0. Full serial battery green remotely via `rexec` on the release commit (statics 2:03, ordinary 11:40, compatibility 29:42, performance 1:03, coverage 90%, total ~31 min, exit 0); `check-release --baseline v5.20.0` reported `ok:true`/minor/no-findings; `family_preflight project-toolbox` exit 0; dogfood `validate` green from the release wheel. Verified by one fresh-context verifier pass (all claims confirmed). Issues #168 (Initiative, target date 2026-08-16) and #176 (Bug) closed Done/completed with release references; follow-up filed: `L3DigitalNet/remote-execution#10` (rexec misreports a wedged mirror deletion as tree instability; triage fields open). #173–#175 remain Inbox — #174 gained in-train evidence: `SG-ARTIFACT-SKILL-DEST` cannot reach catalog-native families, only legacy V1 bundles, determined empirically. Consumer-pin rollout: deliberately deferred to owner scheduling — `@v5` trackers inherit automatically; no exact-pin consumer repositories were changed. | published on `main` (Latest) |
| `v5.22.0` | Agent Handoff `1.14` default (session-scoped closeout: `validate --since`, `delta`, caps table, delegation); GitHub Workflow `1.4` (Prettier-stable ledger escaping + emoji width; Go toolchain 1.26.6 rebuild); Python Tooling `1.15` (`runner_labels`, `ruff.enforce_line_length`); Standard Bundle Authoring `2.7` internal (same-digest declaration prose). Control-plane hardening: symlink containment (#179), one-apply alias convergence (#186, `CP-ALIAS-CONFLICT`), in-root deny-list hardening under `.git`/`.standards` (#187, `CP-CONTAINMENT-DESTINATION`), release-consistency marker accepted above tables. Lock advance: pip `26.2.1`. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `ef6a7b0edd5667c94dd518ac410a0ed010b7b0cc`; signed `v5.22.0` tag (object `570439c6`, verify-tag OK) and re-created `v5` tag (object `96104675`, repointed from `a4a5ca9f`); byte-verified wheel (`394a0c76…`) and sdist (`87500f0c…`) assets are live (downloaded and sha256-compared, identical); the release is marked Latest. `check-release --baseline v5.21.0` reported `ok:true`/minor/0 findings. Gates: `verify.sh --full` on the pre-final tree (statics/compat/perf/coverage 90% ok; 6 ordinary reds fixed — 2 stale test goldens + `versioning.md` refs); narrow rerun on `ef6a7b0e` (19 targeted tests, 5 validators, `validate`, `agent-handoff validate --since` with 246 warnings suppressed and 0 errors, `drift-check`) all green. Issues shipped: #173 #177 #179 #180 #181 #184 #185 #186 #187 (closed with v5.22.0, closure fields performed by a parallel triager). Deferred (Inbox): #174 #175 #178 #182 #183 #188 #189 #190; #129 remains owner-deferred; #178 needs a human plan-disposition decision. Consumer-pin rollout: `@v5` trackers inherit 5.22.0 automatically on next reconcile (moving tag repointed); no exact-pin consumer repositories were changed this session. | published on `main` |
| `v5.23.0` | GitHub Workflow `1.5` (ledger subcommand + `docs/GH-WORKFLOWS.md` feature removed; `SKILL.md` 157→69 lines; field-vocabulary 240→30; routing table moved into the consumer `AGENTS.md` block; agents self-define acceptance criteria/Ready/Execution mode except `Unattended agent`; `check` no longer requires Target date; `openai.yaml` moved under `.agents/` only, codex-gated; spec SPEC-GHW1 rev 1.9→1.12). Also cut for #175: Agent Handoff `1.15`, Markdown Frontmatter `1.13` (new `harnesses` option), Python Tooling `1.16` (`uv_build` `<1.0` pin), Project Toolbox `1.1`. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `740caf4cdc96b14adce0932e845364aca995b5fe`; signed `v5.23.0` tag (object `857269cc`) and re-created `v5` tag (object `4d94d565`, repointed to `740caf4c`); byte-verified wheel (`371eb89b…`) and sdist (`cc32f377…`) assets are live; the release is marked Latest; `uvx --from git+…@v5.23.0 project-standards --version` reports 5.23.0. `check-release --baseline v5.22.0` reported minor, 0 findings. Gates: pre-tag battery on `main` — statics/compatibility/performance/coverage (90%) green remotely; 4 ordinary failures were release-pin staleness in tests, fixed in the release commit and reproven per-file; go-check green; `validate` 40 checks green; handoff validators green; fresh-context verifiers: cut-vs-spec 8/8 confirmed after fixing C8 and guard-blocking C10; release-tree 9/10 confirmed (C10 stale-notes-draft fixed). Issues closed Done/completed with release references: #174 #175 #182 #183 #188 #189 #190 #192 #193. Filed this session: #191 (Inbox, re-measure corpus after 1.5 deploys), #194 (Ready, generic provider-registry==payload contract check), #195 (Ready, `CP-VERIFY` should name its target). Still owner-held: #178 (plan disposition), #129. Deviation for owner: spec keeps `Unattended agent` owner-only, narrower than "all three by judgment"; no mechanism honors the wider rule yet. Consumer-pin rollout: deferred to owner scheduling — `@v5` trackers inherit automatically; consumers of `github-workflow` must delete `docs/GH-WORKFLOWS.md` manually per the `UPGRADING.md` 5.23.0 subsection. | published on `main` |
| `v5.24.0` | Catalog 5: `markdown-frontmatter` `1.14` default (`1.13` retained), landing review findings F1 (AGENTS.md/CLAUDE.md blocks), F2 (provenance), F4 (dual-install sentence), F5 (`XXXXXX` placeholder), F7 (`--fix` bulk route), F11 (`--scaffold` first), and the manifest-vs-profile line. Also #194 (generic provider-registry test) and #195 (`CP-VERIFY` path+kind). #178 resolved: v519 plan repointed then retired; GSF3 plan re-authored as format 3 + spec 0.2. Repo-wide docs drift audit landed across partitions A–F (see `CHANGELOG.md` 5.24.0); ten completed plans retired — `docs/plans/` now holds only the GSF3 master. `.standards/config.toml` include fixes for `docs/mcp-server.md` and the project-spec pattern `docs/specs/usage-documentation-site/*.md`. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `3a387543`; signed `v5.24.0` tag and re-created `v5` tag; byte-verified wheel `d78af85c…` and sdist `5902cd91…` assets are live; the release is marked Latest. Post-tag fixes landed directly on `main` at `168ac794` (pushed; `testing` fast-forwarded): spec-lint HTML-comment false positive in `SL-PLACEHOLDER`; payload-tree test enumeration ignores `__pycache__` (`tests/payload_tree.py`) plus `verify.sh` `PYTHONDONTWRITEBYTECODE`; usage-doc-site bundle conformance restored (90→0 findings) and decision D-003 recorded. Also on `main` (pushed; `testing` fast-forwarded): `482de306`/`afffa83e` amended the issue-regression ledger for the payload_tree sweep (GH-8 and GH-37 ×2, per-issue amendment records) after the hosted Check on `168ac794` failed only on those digest pins — the ordinary suite was otherwise fully green and the `__pycache__` class stayed fixed; `2e62d576`/`c7757b49` recalibrated the 100-order compatibility-sweep performance ceiling 40s→60s (self-hosted runner measured 44.7s; workstation 43.6s at release `3a387543` and 43.9s at `c7757b49`, so not a regression; precedent `127bd3dd` 2026-08-12). Hosted CI on `c7757b49`: Validate Specs, Lint Markdown, Format, Validate standards graph, Coherence, and Validate project standards all success; Check was still in_progress at session close — its ordinary lane had been green on the previous commit and only the recalibrated perf lane remained (see `sessions/2026-08.md`). Issues closed Done: #194 #195 #178. Deferred: #129 (feature-scale), #191 (window ≥2026-09-06). Filed: #196 (frozen provider comments, Ready), #197 (v4 bundle + family-root skill `xxxxxx`, Needs definition — owner), #198 (`created_container=False` preserve semantics, Needs definition — owner), #199 (project-spec 1.9 template drift → 1.10 cut, Ready), #200 (stale characterization digests, Ready); `agent-configs#69` (revise `--reason` regex crash), `agent-configs#70` (EV-### ids unresolved). Owner inputs pending: SPEC-GSF3 OQ-001 (blocks that plan's T1); #197; #198. Battery: `verify.sh --full` remote ~100 min, 7 reds all classified (3 worker `__pycache__`, 3 test pins, 1 family-root skill 1.3 mirror), file reruns green; `go-check` clean; `check-release` minor/0. The `rexec` worker went unreachable twice late in the session (ssh timeout); both recovered on retry. Consumer-pin rollout: deferred to owner scheduling — `@v5` trackers inherit automatically. | published on `main` (Latest) |
| `v5.25.0` | GitHub Workflow `1.6` default (finding-disposition rule replaces invariant 15: fix in place, file against the owning repo, or escalate to the operator; ships in `SKILL.md`, the managed `AGENTS.md`/`CLAUDE.md` block, and `references/pr-standard.md`/`summary-format.md`; spec revision 1.13 records D-016/DEV-004), Agent Handoff `1.16` default (provider comment cites `test_provider_registry.py`), Project Specification `1.10` default (templates byte-identical to canonical source). Also: tracked `main`-branch commit guard (`scripts/githooks/main-branch-guard`, installed by `bootstrap-worktree.sh`/`make githooks`, override `PROJECT_STANDARDS_MAIN_COMMIT_OVERRIDE=1`); shared payload-projection test fixture (`tests/installed_package.py`) fixing worker tmpfs ENOSPC. **MINOR**; all predecessors remain byte-immutable and selectable. Release commit `b2f73d9dee080a7579a7900168093dabf4c52b5b`; signed `v5.25.0` tag and re-created `v5` tag; byte-verified wheel (`1dbc0b4ecf3d…`) and sdist (`b67baaa1d9a7…`) assets are live; the release is marked Latest. Issues #196, #197, #198, #199, #200 closed Done/completed with release references. Deferred: #191 (re-measure window opens 2026-09-06), #129 (feature-scale); DEV-004 open in the github-workflow spec. Consumer-pin rollout: deferred to owner scheduling — `@v5` trackers inherit automatically. | published on `main` (Latest) |
| `v5` (moving) | tracks the newest 5.x release (`v5.25.0`) | published on `main` |
