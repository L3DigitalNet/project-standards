---
plan_format: 3
title: 'Command Provider Execution Implementation Plan'
slug: 'command-provider-execution'
status: active
revision: 12
revises_revision: 11
revision_reason: 'append a bounded post-checkpoint Git-mode inventory correction for the T4 fixture artifacts'
pause_reason: ''
source: 'issue L3DigitalNet/project-standards#142 and owner decisions of 2026-08-10'
spec_ref: ''
created: 2026-08-10
updated: 2026-08-11
owners:
  - 'Project Standards maintainers'
---

# Command Provider Execution Implementation Plan

> **Definition, not state.** This plan stops at verified local checkpoints. Plan authoring did not generate execution state; during execution, the orchestrator alone generates and mutates the ephemeral state under `.project-pipeline/2026-08-10-command-provider-execution/execution/`.

## 1. Objective

Allow a trusted V2 payload to declare a Go executable as a `command` provider and receive the same validated provider input, declared resource bytes, typed output handling, and repository-integrity checks as a Python provider. Every executable provider runs through one control-plane-owned bounded subprocess implementation, whether the caller is the CLI or MCP service. The first release supports `linux/amd64`, executes only integrity-verified bytes materialized at mode `0755` for that invocation, and never executes an installed-wheel resource path.

The implementation ends with a committed Go payload fixture that proves `command` dispatch through reconcile, validate, and drift-check, including platform refusal and reproducible-build coverage. Migrating the Agent Handoff provider is a separate successor-payload plan after this platform checkpoint.

## 2. Authority and Source Map

| Source | Source Role | Authority / Use | Version / Date | Affected Plan Surface |
| --- | --- | --- | --- | --- |
| `request` | normative | Continue autonomously through v5.19.0; retain the approved 7.5-second/30-second budgets; correct only the gate-discovered T2 exception-cause and inherited-file-descriptor audit parity gaps; close T3's discovered MCP command-routing/input-authority omission; append the bounded T5 correction discovered by T4; and append T6's post-checkpoint Git-mode inventory correction without changing executable modes/bytes, fixture digests, production behavior, caps, architecture, or accepted product outcomes. | 2026-08-11 | §§3–7, 9–12; T2–T6 |
| `issue:L3DigitalNet/project-standards#142` | normative | Approved `command` provider outcome, trust-boundary properties, Go direction, accepted `linux/amd64` narrowing, rejected alternatives, materialization, and acceptance criteria. | live through 2026-08-10T04:38:11Z | §§1, 3, 5–13; T1–T5 |
| `repo:docs/adr/adr-0025-project-standards-mcp-service-and-sdk-boundary.md#provider-execution-boundary` | stale/superseded | Historical path retained only to ground completed T1 identity metadata; replaced by the canonical short path and not an operational authority. | `23c0036f` | T1 terminal history |
| `repo:docs/adr/adr-0025-mcp-service-and-sdk-boundary.md#provider-execution-boundary` | decision | Existing 30-second bound, process-group termination/reaping, bounded result and diagnostics, third-descriptor result channel, and MCP failure-isolation obligations. | `4f4a3644` | §§3–7; T2–T3 |
| `repo:docs/adr/adr-0030-command-provider-execution-boundary.md#shared-bounded-runner` | decision | Direct control-plane and MCP calls use the one control-plane-owned runner exactly once; MCP delegates without a nested worker. | `2cf74539` | §§3–7, 9–12; T3 |
| `repo:docs/adr/adr-0027-adopt-go-alongside-python-with-neutral-tooling.md` | decision | Repository Go lane and neutral Python/Go tooling boundary. | `23c0036f` | §§3, 5, 7; T1, T4 |
| `repo:docs/adr/adr-0029-agents-root-allocation.md` | current-state evidence | Integrated release ADR that consumes number 0029 and makes 0030 the next free command-provider allocation. | `8dbdf9b1` | §§4–5, 9, 11; T1 |
| `repo:standards/adr/versions/1.5/README.md` | normative | Adopted ADR authoring, numbering, frontmatter, indexing, and relationship rules for the prerequisite decision record. | `23c0036f` | §§3, 7–9; T1 |
| `repo:src/project_standards/control_plane/providers.py::invoke_provider` | current-state evidence | V2 selection, payload integrity, resource loading, input/output schema validation, typed results, and current kind-aware Python/command execution. | `fdbbd85c` | §§4–5; T2–T5 |
| `repo:src/project_standards/control_plane/provider_subprocess.py::run_provider_subprocess` | current-state evidence | Shared bounded transport, caps, environment/spawn seam, termination, reaping, and deterministic diagnostic composition now owned by the control plane. | `78fb946b` | §§4–5; T2–T3 |
| `repo:src/project_standards/control_plane/provider_worker.py::run_request` | current-state evidence | Shared control-plane worker-side capsule dispatch and third-descriptor frame production. | `78fb946b` | §§4–5; T2 |
| `repo:src/project_standards/control_plane/provider_worker.py::authoritative_provider_input` | current-state evidence | Family-first then plan-bound composite input-authority helper. Its plan-bound fallback currently hard-codes the Python in-child runner, so a command-backed render contribution fails before the later verify command can receive its authoritative snapshots. | `0beee93e`; T4 real-route receipt | §§4–7, 9–12; T5 |
| `repo:src/project_standards/control_plane/planner.py::_contribution_content` | current-state evidence | Planning-time provider invocation seam that requires the fixture's `render-fixture` command result to produce the target content and completed reconciliation plan consumed by verification input authority. | `0beee93e`; T4 real-route receipt | §§4–7, 9–12; T5 |
| `repo:src/project_standards/control_plane/executor.py::_verify` | current-state evidence | Canonical post-apply verification path: derive VERIFY input from the completed plan, then invoke the declared verification provider exactly once. | `0beee93e` | §§4–7, 9–12; T5 |
| `repo:src/project_standards/control_plane/provider_inputs.py::_verification_input` | current-state evidence | Sole plan-bound snapshot projection from plan preconditions, referenced inputs, and owned managed units. | `0beee93e` | §§4–7, 9–12; T5 |
| `repo:src/project_standards/mcp_services/providers.py::_dispatch` | current-state evidence | T3's parent-side kind branch now preserves caller input and requests seam-authoritative input before invoking command bytes; its real plan-bound route exposed the hard-coded Python planning runner. | `fdbbd85c`; T4 real-route receipt | §§4–7, 9–12; T3, T5 |
| `repo:src/project_standards/package_contract/payload.py::ProviderDeclaration` | current-state evidence | V2 provider schema, payload-qualified Python entrypoint grammar, resource closure, operation/phase/effect contract, and declared `ProviderKind.COMMAND`. | `23c0036f` | §§4–6; T3–T4 |
| `repo:src/project_standards/control_plane/planner.py::_verification_requests` | current-state evidence | Current Python/documentation-only verification filter that must admit supported command declarations without package-specific dispatch. | `23c0036f` | §§4–6; T3–T4 |
| `repo:src/project_standards/provider_runner.py::_run_python_provider` | current-state evidence | Legacy V1 installed-bundle runner that deliberately rejects external-process provider kinds. | `23c0036f` | §§3–5; T3 |
| `repo:tests/mcp_services/test_providers.py` | current-state evidence | ADR 0025 boundary, timeout, cap, cancellation, descendant, cleanup, real-provider, and MCP composition regression surface. | `23c0036f` | §§4, 7; T2–T4 |
| `repo:tests/control_plane/test_providers.py` | current-state evidence | Direct-dispatch schema, resource, typed-effect, failure-content, and live-path integrity regression surface. | `23c0036f` | §§4, 7; T2–T4 |
| `repo:src/project_standards/control_plane/diagnostics.py::ControlPlaneError` | current-state evidence | Public control-plane failure wrapper whose safe built-in `__cause__` parity must be restored without changing its bounded public message. | `accce755` | §§3–7, 9–12; T2 |
| `repo:tests/mcp_services/security/test_consumer_boundaries.py::OpenAudit` | current-state evidence | Filesystem-access audit whose `open`/`io.open` wrappers currently stringify inherited integer file descriptors as though they were paths. | `accce755` | §§3–7, 9–12; T2 |
| `repo:tests/package_compatibility/test_catalog_matrix.py` | current-state evidence | Compatibility matrix and obsolete global `subprocess.Popen` denial that predates the required one-child direct boundary. | gate failure verified 2026-08-10 | §§4, 7, 9–12; T2 |
| `repo:tests/package_compatibility/test_performance.py` | current-state evidence | Existing performance contract and measured post-capsule conflict: three serial real-catalog runs were 5.416–5.460 seconds, with 101 required child starts consuming 4.745 seconds; the 100-order proof remained green at 24.68–24.88 seconds. | measured 2026-08-10 | §§7, 9–12; T2 |
| `repo:scripts/lib/go-reproducible-build.sh` | current-state evidence | Pinned `linux/amd64`, `CGO_ENABLED=0`, `-trimpath`, source-to-committed-byte rebuild-and-compare contract. | `23c0036f` | §§4–7; T4 |
| `repo:Makefile::go-verify-binary` | current-state evidence | Explicit owner list for every committed Go binary's reproducible-build verification. | `23c0036f` | §§4–7; T4 |
| `repo:tests/control_plane/test_end_to_end.py::test_every_adapter_converges_in_one_virtual_tree_and_second_apply_is_byte_noop` | current-state evidence | Real control-plane reconciliation flow and convergence oracle for the synthetic command fixture. | `23c0036f` | §§4, 7; T4 |
| `repo:tests/test_repository_hygiene.py::test_git_mode_policy__classifies_and_normalizes_the_complete_anchor_inventory` | current-state evidence | Git-index mode-policy census. Its adjacent comment requires every post-anchor executable to be classified in the same staged change because untracked executables are invisible to `git ls-files -s`. | `e033260a`; runner-label T3 fast-gate receipt | §§3–7, 9–12; T6 |
| `repo:scripts/build-command-provider-fixture.sh` | current-state evidence | Intentionally executable T4 build entrypoint, committed mode `100755`; it is repository tooling and belongs in the post-anchor tooling classification. | `e033260a` | §§3–7, 9–12; T6 |
| `repo:tests/fixtures/command-provider/standards/command-provider-fixture/versions/1.0/bin/command-provider-fixture` | current-state evidence | Intentionally executable committed native test fixture, mode `100755`, binary SHA-256 `83292a67bab10fb619d5d3be17210b8e056734efb15140562ee5b8741b63496a`; it requires a fixture-semantic post-anchor classification rather than a mode change. | `e033260a` | §§3–7, 9–12; T6 |
| `repo:docs/plans/2026-08-10-runner-label-advisory-plan.md#t3-extend-the-composed-project-specification-19-candidate` | operational evidence | External runner-label T3 integrated the T4 checkpoint and exposed the two indexed executables through its sole direct-local fast gate; its nine-path candidate remains exact and resumes only after T6. | integrated base `e033260a`; gate receipt 2026-08-11 | §§4, 7–13; T6 provenance and downstream handoff |
| `issue:L3DigitalNet/project-standards#156` | normative | Requires the family-agnostic schema-reference guard's T2 checkpoint before any production successor payload cut. | live through 2026-08-10T04:38:13Z | §§3, 8–13; deferred migration only |
| `repo:docs/plans/2026-08-10-schema-payload-reference-validation-plan.md#t2-document-the-successor-cut-guard-and-verify-the-repository` | informative | Names the currently approved T2/PV-T2-001 checkpoint that a later successor plan must re-resolve and consume. | revision 1 at `23c0036f` | §§3, 8–13; deferred migration only |
| `issue:L3DigitalNet/project-standards#162` | normative | Requires the release-wide ADR corpus sweep to run last; this plan's new ADR must exist before that sweep begins. | live through 2026-08-10T04:38:17Z | §§3, 8–13; T1 |

Conflict precedence: issue #142's owner decisions govern the target. ADR 0025 governs the properties retained from the MCP boundary; the new T1 ADR extends those properties to the generic control plane and records the accepted platform and command ABI. The gate receipt narrows T2 correction scope but does not authorize new caps, children, budgets, or error exposure. Current code and tests establish the starting behavior, not authority to retain MCP-only ownership or in-process CLI dispatch.

## 3. Scope, Boundaries, and Constraints

### 3.1 In Scope

- An accepted control-plane ADR recording the command-provider trust boundary, exact wire/platform contract, accepted portability narrowing, status-quo and Python-shim rejections, and the relationship to ADRs 0025 and 0027.
- One control-plane-owned bounded subprocess runner used by direct CLI/control-plane dispatch and MCP services, with the ADR 0025 timeout, per-stream/result caps, explicit truncation, third inherited result descriptor, `SIGTERM` then `SIGKILL`, whole-process-group cleanup, reaping, and content-safe diagnostics.
- Python exception parity across that boundary: the child supplies only sanitized `kind`/`detail`; the parent reconstructs an explicit safe built-in allowlist (at minimum `ValueError`) as `ControlPlaneError.__cause__`, while unknown kinds remain bounded generic and no arbitrary import or custom exception constructor is reachable.
- Additive V2 `ProviderDeclaration` semantics for `kind = "command"`: a payload-resource entrypoint, closed platform declaration, and executable mode, while preserving the Python declaration grammar.
- Exact command-provider input/resource/result transport, input and output schema validation at the same authoritative control-plane boundary as Python, platform preflight, temporary `0755` materialization, post-write digest verification, and cleanup.
- A Go command-provider fixture and source/committed-binary reproducibility gate that exercise real reconcile, validate, and drift-check entrypoints plus the material failure paths.
- A bounded correction to plan-bound MCP command input authority: the parent selects the planner runner by provider kind, planning-time command contributions run once through their direct control-plane command path, Python planning remains in the existing child, and the later target command still runs exactly once after authoritative input construction.
- A bounded post-checkpoint repository-hygiene correction that classifies the T4 build script and committed command fixture as intentional post-anchor executables without changing either file or any other mode-policy category.
- Generated package schema, focused tests, existing MCP/control-plane regression migration, and only the owner documentation required to keep the runtime and fixture contract discoverable.

### 3.2 Out of Scope and Deferred

- Do not migrate `standards/agent-handoff/versions/1.11/providers/agent_handoff.py`, cut an Agent Handoff successor, change its advertised/default version, or retire Python provider bytes. That migration receives its own later plan after T4 and external gate EG-156.
- Do not add another production package, payload family, catalog version, Catalog 5 selection, release, tag, publication, or GitHub mutation.
- Do not add a Python shim around the Go provider, execute an installed-wheel payload path, invoke a shell, search `PATH`, or inherit arbitrary environment variables into a command provider.
- Do not import an exception named by child data, execute a provider/custom exception constructor in the parent, expose raw child paths/secrets, or treat an inherited integer result descriptor as a filesystem path in the security audit.
- Do not make the legacy V1 `standard.toml` runner execute `command` declarations. Its installed-module path remains Python-only and fail-closed; V2 payload execution is the boundary changed here.
- Do not provide macOS, Windows, Linux architectures other than `amd64`, cross-platform fallback, a sandbox, privilege separation, network isolation, or aggregate rollback beyond the existing declared-live-path integrity check.
- Do not run #162's corpus-wide sweep or close #142/#156/#162. This plan only produces the prerequisite ADR checkpoint and core implementation checkpoints.
- This authoring task did not generate `.project-pipeline` state. During execution, only the orchestrator may generate or mutate that state; workers must not edit it directly.

### 3.3 Ownership and Preserved Behavior

| Boundary | Owner / Responsibility |
| --- | --- |
| T1 owns | The new ADR and its index entry; it produces the architecture checkpoint consumed by T2–T5 and externally by #162. |
| T2 owns | The shared subprocess transport/worker boundary, removal of MCP-local ownership, and Python-provider behavior parity through direct and MCP callers. |
| T3 owns | V2 command declaration semantics, platform/materialization/resource transport, command dispatch, deterministic failure taxonomy, and branch-point closure. |
| T4 owns | The Go fixture source and committed binary, its reproducible-build script/gate, the synthetic payload, end-to-end acceptance, and final integrated qualification. |
| T5 owns | The bounded plan-runner selection correction in T2-owned input authority plus focused T3-owned command-authority regressions; T4 remains owner of fixture and final acceptance. |
| T6 owns | The exact two-entry post-anchor Git-mode classification in `tests/test_repository_hygiene.py`; T4 remains owner of both executable artifacts and their accepted bytes/modes/digests. |
| Depends on | ADR 0025, Go ADR 0027, payload integrity, existing input/output schemas, repository snapshot integrity, bridge 3.5.0, and T1 before #162. The deferred production successor depends on EG-156. |
| Does not own | Agent Handoff migration behavior, package activation/publication, non-Linux platform support, organization work state, or #162's corpus rewrite. |
| Must preserve | All Python provider effects and typed results; current provider schema validation; selected-payload/resource integrity; public MCP DTO/service behavior; finding ordering; planner/executor genericity; existing V1 command refusal; no package-ID dispatch in shared control-plane modules; both T4 executable modes/bytes; fixture reproducible and payload digests; every unrelated mode-policy classification; and no production package, release, or GitHub change. |

### 3.4 Constraints and Authorization

- T1 is the first content checkpoint. No source, schema, fixture, generated schema, or build-gate implementation may begin before its accepted ADR commit validates.
- The command ABI is direct execution of a materialized payload resource. The runner passes no shell and resolves no executable from an installed directory or `PATH`.
- The first closed platform identifier is `linux/amd64`. A command declaration must name that target and mode `0755`; a mismatch is refused before materialization or spawn with a stable, path-free diagnostic.
- Python entrypoints remain `payload:{resource-id}#{symbol}`. Command entrypoints are `payload:{resource-id}` with no symbol fragment. `entrypoint_resource` resolves both forms to exactly one declared resource.
- The subprocess result travels only on the third inherited descriptor named by `argv[1]`. Provider `stdout` and `stderr` are diagnostics and cannot carry the result.
- A Python child failure frame carries sanitized `kind` and bounded `detail`. The parent uses a closed literal mapping of safe built-in exception types, including `ValueError`, to reconstruct `ControlPlaneError.__cause__`; an unknown kind maps to a fixed generic failure and cannot trigger import, attribute lookup, or custom code. The public control-plane message remains bounded/content-safe.
- Security audit wrappers distinguish transport handles from paths: integer targets passed to `builtins.open`/`io.open` are inherited file descriptors and are not recorded as filesystem access; every string, bytes, and `os.PathLike` target remains audited. This test correction does not weaken production path containment.
- Command provider environment construction is an explicit closed map containing only runner-required locale/runtime values; it does not copy the parent environment. Python worker environment compatibility is decided and recorded by T1 without weakening the command boundary.
- The command envelope and result are deterministic UTF-8 JSON. Resource bytes use standard padded Base64 keyed by sorted declared resource ID; input/resources/result reject malformed or duplicate data before use.
- Existing exact caps remain the initial runtime contract: 30 seconds per invocation, 8,192 bytes retained per diagnostic stream, 262,144 result bytes, and 16,384 composed diagnostic characters. Boundary tests cover the exact limit and `N + 1` where meaningful.
- The serial real-catalog planning proof has a 7.5-second ceiling, allowing the accepted one-child-per-invocation isolation cost plus bounded runner variance. The independent 100-order deterministic proof retains its 30-second ceiling.
- Temporary executable materialization uses a private per-invocation directory, writes the already integrity-checked bytes, sets exactly `0755`, rehashes the on-disk file against the manifest digest, executes that path, tears down the process group, and then removes the file/directory on success, timeout, cancellation, crash, schema failure, and spawn failure.
- Comments/docstrings added during implementation must record the cross-file ABI, why the third descriptor is authoritative, why installed paths are rejected, materialization/cleanup ordering, process-group teardown semantics, and environment boundary. Syntax narration and diff chatter are prohibited.
- Complete at local verified checkpoints. Do not publish, release, close issues, or begin the deferred payload migration under this plan.
- Plan-bound authority may invoke a declared planning-time provider needed to construct the reconciliation plan. Each invocation uses exactly one kind-appropriate control-plane route: Python stays in the already-running Python worker, while a command contribution is a direct command child with no wrapping worker. The target VERIFY command is not run during plan construction and runs exactly once afterward.
- T6 may modify only `tests/test_repository_hygiene.py`. It adds exactly the two committed `100755` T4 paths to semantically accurate post-anchor classifications: the build script as repository tooling and the committed binary as a native test fixture. It must not chmod, rewrite, regenerate, or rehash either artifact; change anchor/predecessor policy; reclassify another path; or weaken equality of the complete executable inventory.

## 4. Current State and Target State

### 4.1 Current State

`ProviderKind.COMMAND` is already legal in both manifest enums and the generated V2 payload schema. V2 `ProviderDeclaration` nevertheless accepts only the Python-shaped `payload:{resource}#{symbol}` executable entrypoint, and `invoke_provider` refuses every non-Python declaration. It loads digest-checked resource bytes, validates provider input, compiles Python bytes directly in the CLI process, captures only Python-level `stdout`/`stderr`, validates output, constructs a typed effect, and confirms declared live paths did not change. There is no timeout, cancellation point, process-group teardown, or fault isolation on this authoritative direct path.

MCP separately owns a hardened process boundary in `mcp_services/providers.py`. It starts a Python worker, pumps stdin/result/stdout/stderr without blocking, caps output and result data, returns results on a third descriptor, terminates and reaps the process group, filters failure text, and composes deterministic diagnostic sections. The child resolves the selected payload again and calls the same in-process `invoke_provider`. Thus MCP is bounded but the CLI is not, and extracting the boundary must avoid double-spawning MCP providers.

Revision 4 T2 reached a structurally green implementation but the direct-local fast gate exposed two bounded parity defects. Statics passed in 4:10, compatibility passed in 34:04, and performance passed in 0:39; the ordinary lane reported 17 failures and 4,858 passes in 13:41, for 34:45 total. The child error frame already contains sanitized `kind`/`detail`, but the parent does not restore the safe built-in cause expected by ordinary parity tests. Separately, `OpenAudit` records integer `open`/`io.open` result descriptors as path strings, causing transport-handle activity to look like forbidden filesystem access.

T2 is terminal at `78fb946b`, and T3 is terminal at `fdbbd85c` after implementing the nine-path command runtime and the MCP parent kind branch. T4's real-route proof then disproved one completed T3 assumption. Direct MCP command dispatch succeeds with exactly one child, and the six fixture tests are green, but composite `validate_repo` completes `validate-fixture` and fails `verify-fixture` before its target spawn. `verify-fixture` has no family input, so `authoritative_provider_input` builds a reconciliation plan; that plan needs the command-backed `render-fixture` contribution for `.standards/command-provider-fixture.txt`, yet the helper hard-codes Python-only `invoke_provider_in_child`. The observed result is `control-plane-unavailable`, no verify output or snapshots, and no verify spawn. T4 is blocked with its fixture/build/e2e work preserved until a permanent correction restores the real plan-bound route.

T1–T5 are now terminal, and T4's committed fixture at `e033260a` preserves binary SHA-256 `83292a67bab10fb619d5d3be17210b8e056734efb15140562ee5b8741b63496a` plus payload aggregate `66720e040b0c6eb6be3cf7397ee94955a996e8e9e28a17c81ebebd9c92df7986`. The external runner-label T3 then ran one direct-local fast gate on that integrated base. It exited 1 after 33:47: statics passed in 4:18 (Ruff, BasedPyright, Prettier, markdownlint 1,800/0, and pip-audit); ordinary returned 4,959 passes and one failure in 12:36; compatibility passed 141 in 33:06; performance passed 5 with 5,101 deselected in 38.19s; coverage remained 90%. The sole failure was `test_git_mode_policy__classifies_and_normalizes_the_complete_anchor_inventory`, reporting exactly the T4 build script and committed fixture binary as unexpected `100755` paths. T4's full gate missed them because both were untracked in its worker, exactly the staging hazard documented beside the policy classification. The runner-label candidate remains nine staged paths with no edit, rerun, or commit.

Payload resources carry path, media type, and digest but no executable semantics. Installed wheel modes are not a reliable execution contract. The reproducible Go build library proves the two current committed binaries only because their thin scripts are explicitly listed in `Makefile`; no command-provider binary or generic platform-declaration test exists.

### 4.2 Target State

The control plane owns one bounded execution implementation. `invoke_provider` resolves and validates the selected declaration and resources, validates input, snapshots declared live paths, and dispatches Python or command bytes through the one process boundary for that provider invocation. For Python, the parent prepares an integrity-checked execution capsule from the already-qualified `InstalledPayload`: exact provider/schema/resource bytes, declaration metadata, validated input, and effect contract. The child executes that capsule without rediscovering or revalidating the whole manifest from disk. The parent treats child output as untrusted: it parses one bounded result object, applies the declared output schema and typed-effect checks, verifies live-path integrity, and publishes only fixed failure categories plus bounded/sanitized diagnostics. For a child exception it reconstructs only an allowlisted safe built-in cause from sanitized frame data; unknown kinds use a fixed generic cause. MCP retains its operation/effect qualification and DTO mapping, routes Python through the existing child path, and routes each command declaration from the parent directly into the same control-plane invocation. A single-provider command receives the caller's validated input unchanged. Before a composite command VERIFY dispatch, the parent invokes `authoritative_provider_input` against the qualified selection with a parent-side planner runner that delegates each planning invocation through `invoke_provider`: the required `render-fixture` command is therefore one direct bounded child, while any Python planning provider retains its normal child path. The completed plan is projected only through `provider_dispatch_input`; the target `verify-fixture` command is never executed during construction and then runs exactly once through its own direct bounded child. A returned mapping replaces the seam directive, `None` retains the deliberately generic empty input, and any other authority failure retains the existing per-result failure behavior.

A V2 command declaration uses `entrypoint = "payload:{resource-id}"`, `platforms = ["linux/amd64"]`, and `mode = "0755"`. The entrypoint and declared provider resources are loaded from the verified payload inventory. A command receives canonical JSON on stdin containing the already schema-validated provider input plus Base64 resource bytes; it writes exactly one JSON result to the inherited descriptor. The control plane materializes executable bytes privately, verifies the materialized digest, executes the exact temporary path with a closed environment, and cleans up after group teardown. Unsupported hosts, wrong declarations, digest drift, malformed transport, oversize output, timeout, crash, nonzero exit, and schema violations fail deterministically without leaking installed or temporary paths.

The repository's Git-mode policy classifies every indexed executable exactly. The T4 build script remains `100755` in the post-anchor tooling class; the committed command-provider binary remains `100755` in an explicitly named native-fixture class. The complete-inventory equality stays closed, and neither artifact's bytes, reproducible digest, payload aggregate, nor any other classification changes.

### 4.3 Delta Summary

| Area | Current | Target | Must Preserve |
| --- | --- | --- | --- |
| Runner ownership | MCP-local bounded runner; direct control plane in-process. | One control-plane-owned runner used by CLI and MCP. | ADR 0025 bound, caps, third descriptor, group teardown, and service behavior. |
| Python dispatch | `compile`/`exec` in caller. | Parent prepares an integrity-checked capsule; Python bytes execute in a bounded child through the shared ABI without whole-manifest rediscovery; safe built-in causes are reconstructed from sanitized frames. | Qualified synthetic/monkeypatched `InstalledPayload` seams, input/resource immutability, all effects, schema checks, output notice, `ValueError` cause/detail parity, safe failure text, and generic routing. |
| MCP command dispatch | T3 routes command directly, but plan-bound input construction hard-codes the Python in-child planner runner and rejects a required command render before the target verify spawn. | Parent branches by provider kind and supplies a kind-appropriate planner runner to input authority: planning commands execute directly once; Python planning remains in-child; target verify is not executed during construction and runs once afterward. | One child per declared provider invocation and no nested worker; single-provider caller input, composite family/plan-bound/generic authority, exact snapshots, and per-result failures remain unchanged; no package-ID routing. |
| Filesystem security audit | Path-like and integer `open`/`io.open` targets are stringified together. | Ignore integer inherited-FD transport handles while continuing to record every path-like target. | Production path containment and all path-like audit assertions remain unchanged. |
| Command declaration | Enum legal but Python entrypoint shape and runtime refusal. | Payload resource with explicit `linux/amd64` and `0755` declaration. | Existing Python/documentation-only declarations and schema version remain valid. |
| Executable delivery | None. | Digest-verified temporary materialization; installed path never executed. | Payload integrity remains the source of trusted bytes. |
| Resource transport | In-process immutable `{id: bytes}` mapping. | Canonical Base64 map in the stdin envelope, with the input's digest map unchanged. | Only `provider.resources` bytes are exposed; order/encoding are deterministic. |
| Platform behavior | Python follows the CLI host. | Command providers fail closed outside declared `linux/amd64`; adopting packages knowingly narrow. | Python payload portability remains unchanged. |
| Reproducibility | Explicit gates for two unrelated committed Go binaries. | Fixture binary joins the same source-to-byte gate; future command binaries must add a thin script and gate entry. | Pinned Go toolchain/build flags and released bytes remain unchanged. |
| Agent Handoff | 1.11 provider remains Python. | Unchanged in this plan. | No successor cut, catalog activation, or consumer migration. |
| Git-mode inventory | Integrated T4 artifacts are intentionally `100755` but absent from post-anchor classifications. | Classify the build script as tooling and the committed binary as a native test fixture. | Exact modes/bytes/digests, closed executable equality, anchor inventory, and every unrelated classification. |

## 5. Change Surface and Architecture

### 5.1 Components and Ownership

| Component / Surface | Current Responsibility | Planned Responsibility | Paths / Contracts | Owning Task |
| --- | --- | --- | --- | --- |
| Architecture corpus | ADR 0025 owns MCP-only provider isolation; ADR 0027 owns Go coexistence; ADR 0029 allocates `.agents/`. | ADR 0030 owns generic provider execution, command ABI, materialization, and platform narrowing, with reciprocal links to the existing provider and Go decisions. | `docs/adr/adr-0030-command-provider-execution-boundary.md`, ADRs 0025/0027, `docs/adr/README.md` | T1 |
| Bounded transport | MCP module owns spawn, selector pump, caps, signals, frames, and cleanup. | Control-plane module owns a provider-neutral subprocess result/diagnostic contract. | `control_plane` new runner/worker modules; `mcp_services/providers.py` | T2 |
| Python error-frame parity | In-process exceptions retain native cause type/detail; revision-4 parent drops that safe cause across the child frame. | Closed safe built-in mapping reconstructs `ValueError` and approved peers as `ControlPlaneError.__cause__`; unknown kinds remain fixed generic. | control-plane runner/worker/providers; focused direct/MCP regressions | T2 |
| Consumer-boundary open audit | Integer inherited descriptors are recorded as apparent filesystem targets. | `open`/`io.open` integer handles are ignored; every path-like target remains audited. | `tests/mcp_services/security/test_consumer_boundaries.py::OpenAudit` | T2 |
| Direct provider dispatcher | Selects/validates and directly executes Python bytes. | Prepares/finishes all providers around the shared runner; Python uses a bounded child. | `control_plane/providers.py::invoke_provider` | T2 (shared owner), T3 contributor |
| MCP provider dispatcher | T3 branches on provider kind and requests seam-authoritative input in the parent, but the helper's plan-bound fallback assumes every planning provider is Python. | Preserve T3 dispatch; supply the helper a parent planner runner that routes command-backed plan contributions directly through `invoke_provider`, while the helper default keeps Python worker behavior unchanged. | `mcp_services/providers.py::_dispatch`; `control_plane/provider_worker.py::authoritative_provider_input`; planner/executor/provider-input seams | T5 contributor, shared owner T2 |
| V2 manifest/schema | Declares `command` enum but only Python-shaped executable contract. | Kind-specific entrypoint/platform/mode validation and generated schema. | `package_contract/payload.py::ProviderDeclaration`; generated standard-payload schema | T3 |
| Planner/public routing | Verify admits Python/documentation-only; legacy runner rejects command. | V2 generic routes admit supported command; V1 runner remains explicitly fail-closed. | `control_plane/planner.py::_verification_requests`; `provider_runner.py` | T3 |
| Materializer/command adapter | Absent. | Private file, `0755`, post-write digest check, closed environment, exact-path spawn, cleanup. | control-plane runner/provider modules | T3 |
| Go fixture/repro gate | No provider executable. | One minimal ABI fixture, committed bytes, thin build script, `Makefile` verification, payload/e2e tests. | `cmd/`, `scripts/`, `tests/fixtures/`, `tests/` | T4 |
| Repository Git-mode policy | Complete indexed executable equality excludes the two newly integrated T4 artifacts because they were untracked during T4's gate. | Add exactly two post-anchor role classifications while retaining the closed equality and all normalization/anchor/predecessor rules. | `tests/test_repository_hygiene.py::test_git_mode_policy__classifies_and_normalizes_the_complete_anchor_inventory` | T6 |

### 5.2 Target Control and Data Flow

```text
selected InstalledPayload + ProviderInvocation
        │
        ▼
control_plane.providers
  ├─ verify identity/declaration/resource closure and payload digests
  ├─ materialize referenced inputs and validate provider input schema
  ├─ snapshot declared live paths
  └─ build canonical execution capsule from checked provider/schema/resource bytes
        │
        ▼
control-plane bounded runner (single owner)
  ├─ Python: fixed interpreter/bootstrap argv
  └─ command: verified private 0755 entrypoint argv
        │ stdin JSON                    │ argv[1] result fd
        ▼                               ▼
  isolated process group          bounded JSON result
  stdout/stderr diagnostics       (never stdout)
        │                               │
        └──────────────┬────────────────┘
                       ▼
control_plane.providers
  ├─ validate frame + declared output schema + typed effect
  ├─ verify declared live paths unchanged
  ├─ reconstruct only allowlisted safe built-in cause from sanitized kind/detail
  ├─ sanitize/compose bounded public diagnostics
  └─ return ProviderResult
                       │
             CLI / planner / executor / MCP facade
```

The process boundary is fault isolation, not a sandbox. Payload bytes are catalog-trusted, but every byte and frame crossing the child boundary remains subject to integrity, size, shape, path-containment, and content-safety checks.

### 5.3 Change-Surface Matrix

| Surface | Applies? | Invariant / Required Change | Proof | Task |
| --- | --- | --- | --- | --- |
| Behavior | yes | Python becomes bounded without semantic drift; command providers execute across all declared effects/routes, including plan construction required by later command VERIFY input authority. | PV-T2-001, PV-T3-001, PV-T4-001, PV-T5-001 | T2–T5 |
| Architecture / dependency direction | yes | `control_plane` owns the process runner; MCP consumes it exactly once per declared provider invocation, including planning-time command contributions and the later target command, and never owns or creates a nested dispatch boundary. | PV-T1-001, PV-T2-001, PV-T3-001, PV-T5-001 | T1–T3, T5 |
| Public / cross-task interface | yes | Kind-specific V2 declaration and command wire ABI are frozen before code. | PV-T1-001, PV-T3-001 | T1, T3 |
| Data / state | yes | Entrypoint exists only as private temporary state during an invocation and is removed after teardown. | PV-T3-001 | T3 |
| Configuration | no | No consumer option/config key changes. | PV-T4-001 | T4 |
| Security / trust | yes | Integrity at read/materialization, no installed-path execution, no shell/PATH, closed command environment, bounded child/result/diagnostics, allowlisted cause reconstruction, generic unknown kinds, and path-like audit retention. | PV-T2-001, PV-T3-001 | T2–T3 |
| Compatibility / migration | yes | Existing Python manifests/results and V1 fail-closed behavior remain; command packages declare `linux/amd64`. | PV-T2-001, PV-T3-001 | T2–T3 |
| Operations / deployment | yes | No release or payload activation; final output is a local platform checkpoint and deferred migration input. | PV-T4-001 | T4 |
| Documentation | yes | New ADR/index and concise provider/repro owner truth. | PV-T1-001, PV-T4-001 | T1, T4 |
| Durable evidence | no | Committed tests, ADR, fixture source/binary, build script, and identity-bearing checkpoints are reproducible evidence. | PV-T4-001 | T4 |
| Repository policy | yes | Classify two intentional indexed executables by semantic role without mode, byte, digest, anchor, or unrelated-policy drift. | PV-T6-001 | T6 |

### 5.4 Binding Decisions

| ID | Decision | Rationale | Source | Affected Task(s) |
| --- | --- | --- | --- | --- |
| D-001 | Record and validate the control-plane trust/platform ADR before implementation; its T1 checkpoint is a prerequisite for #162. | The issue changes a repository-wide trust boundary and #162 must sweep the final ADR corpus once. | issue #142; issue #162 | T1–T5 |
| D-002 | Extract one control-plane-owned bounded runner; MCP delegates rather than wrapping it in another worker. | Duplicate or nested boundaries could drift in caps, cleanup, diagnostics, and schema behavior. | issue #142 owner decision | T2–T5 |
| D-003 | Preserve ADR 0025's 30-second bound, process-group `SIGTERM`/`SIGKILL`, reaping, per-stream/result caps, explicit truncation, third descriptor, and filtered failures for CLI and MCP. | These are accepted properties, not MCP implementation accidents. | ADR 0025; issue #142 | T1–T3 |
| D-004 | Use `payload:{resource-id}` plus `platforms = ["linux/amd64"]` and `mode = "0755"` for command declarations; Python retains `#symbol`. | A command has no Python symbol and needs an explicit executable/platform contract. | issue #142 owner decision | T1, T3–T4 |
| D-005 | Transport `{input, resources}` as canonical JSON on stdin; resources are standard padded Base64 keyed by resource ID, and the raw result uses only the inherited result descriptor. | JSON is the existing cross-boundary form; Base64 preserves arbitrary bytes; stdout/stderr remain untrusted diagnostics. | issue #142 owner decision; ADR 0025 | T1, T3–T4 |
| D-006 | Materialize the exact integrity-checked entrypoint bytes privately at `0755`, rehash the file, execute that path, teardown the group, then clean up. Never execute `payload.root/...`. | Wheel installers do not guarantee executable mode, and verification must remain coupled to use. | issue #142 implementation finding | T1, T3–T4 |
| D-007 | Command providers receive a closed environment and use no shell or `PATH`; unsupported platform refusal occurs before materialization/spawn. | Prevent ambient credentials/tooling and opaque `ENOEXEC` behavior from defining the ABI. | issue #142 scope/acceptance | T1, T3 |
| D-008 | Keep the V1 installed-bundle provider runner Python-only and fail-closed for command/workflow. | Its module-import contract is separate from V2 integrity-addressed payload execution; widening it would invite installed-path execution and exceed issue acceptance. | direct derivation from issue scope and current architecture | T3 |
| D-009 | Use a dedicated synthetic payload fixture, not a production family or Agent Handoff successor, for core end-to-end proof. | Core acceptance needs real routing proof without coupling platform implementation to the deferred package migration. | issue #142 acceptance; owner-approved migration boundary | T4 |
| D-010 | Require external #156 T2 before any later production successor cut, but do not misclassify the synthetic test fixture as a released family successor. | The guard must exist when a new package version is authored; it need not block core runtime proof. | issue #156 owner coordination | deferred migration boundary |
| D-011 | Reconstruct `ControlPlaneError.__cause__` only from a literal safe built-in mapping that includes `ValueError`, using the child's already-sanitized detail; unknown kinds use one fixed generic cause. | This restores observable Python parity without allowing child-controlled imports, arbitrary/custom exception execution, or unsafe public diagnostics. | revision-4 fast-gate ordinary failures; request | T2 |
| D-012 | Treat integer `builtins.open`/`io.open` targets in `OpenAudit` as inherited transport descriptors, not filesystem paths, while retaining every path-like audit entry. | The result channel is an FD by contract; stringifying it creates a false path-access finding and does not strengthen filesystem containment. | revision-4 fast-gate ordinary failures; result-fd contract | T2 |
| D-013 | In MCP parent dispatch, retain the existing Python worker route but send command declarations directly through `invoke_provider`; for composite commands, replace the seam directive only with the result of `authoritative_provider_input`, while single-provider caller input is unchanged. | A command launched inside the Python worker creates a forbidden second child, while forwarding the composite's empty directive skips the existing family/plan-bound input authority. Provider-kind routing satisfies both contracts without a package-ID branch. | ADR 0030 shared-runner decision; `_dispatch`; `authoritative_provider_input` | T3 |
| D-014 | Make plan-bound input construction accept an explicit provider runner. Its existing default remains `invoke_provider_in_child` for Python-worker callers; MCP parent command dispatch supplies `invoke_provider`, whose existing declaration-kind selection runs a required command-backed render directly through one bounded child before the separate target VERIFY invocation. | The T4 receipt proves the plan needs `render-fixture` content before `_verification_input` can project authoritative snapshots. A kind-aware existing runner seam preserves the planner and snapshot authorities, avoids a Python wrapper/nested command, does not execute the target verify during construction, and introduces no package-ID branch or second transport implementation. | T4 real-route receipt; ADR 0030; `_contribution_content`; `_verify`; `_verification_input` | T5 |
| D-015 | Preserve both T4 artifacts at `100755`; classify the build script in post-anchor tooling and the committed binary in a separately named post-anchor native-fixture set, then retain equality against the complete Git index. | The modes are intentional execution contracts and their bytes/digests are accepted T4 evidence. Role-specific classifications preserve the hygiene policy's meaning; chmod, a broad allowlist, or weakening equality would hide rather than correct the integration omission. | request; repository-hygiene comment and integrated fast-gate receipt | T6 |

## 6. Requirements and Acceptance

| ID | Requirement | Source | Priority | Owner Task | Task(s) | Proof(s) |
| --- | --- | --- | --- | --- | --- | --- |
| REQ-001 | An accepted ADR shall freeze the control-plane provider trust boundary, command ABI, `linux/amd64` consequence, materialization/integrity/cleanup order, and rejected status-quo/Python-shim alternatives before implementation. | `issue:L3DigitalNet/project-standards#142` | Must | T1 | T1 | PV-T1-001 |
| REQ-002 | The new ADR checkpoint shall precede T2–T4 and be provided to #162 before its final corpus-wide link/rename sweep. | `issue:L3DigitalNet/project-standards#162` | Must | T1 | T1 | PV-T1-001 |
| REQ-003 | Direct control-plane and MCP provider calls shall use one bounded runner that preserves the 30-second bound, process-group teardown/reaping, per-stream/result/diagnostic caps, explicit truncation, third-descriptor result transport, and sanitized deterministic failures. | `issue:L3DigitalNet/project-standards#142`; ADR 0025 | Must | T2 | T2 | PV-T2-001 |
| REQ-004 | All existing Python provider operations/effects, schema checks, resources, output notices, typed results, live-path integrity checks, CLI routing, and MCP DTO behavior shall remain equivalent after bounded extraction. | `issue:L3DigitalNet/project-standards#142` | Must | T2 | T2 | PV-T2-001 |
| REQ-005 | V2 command declarations shall require a payload-resource entrypoint, closed supported platform list containing `linux/amd64`, and mode `0755`, while existing Python/documentation-only manifests and the V1 command refusal remain valid. | `issue:L3DigitalNet/project-standards#142` | Must | T3 | T3 | PV-T3-001 |
| REQ-006 | A command provider shall receive the same schema-validated input and exactly its declared resource bytes over the defined JSON/Base64 transport, and its bounded JSON result shall pass the existing output schema and typed-effect validation before publication. | `issue:L3DigitalNet/project-standards#142` | Must | T3 | T3, T4, T5 | PV-T3-001, PV-T4-001, PV-T5-001 |
| REQ-007 | The runner shall refuse unsupported platforms before spawn, materialize verified executable bytes privately at `0755`, reverify the on-disk digest, execute no installed-wheel path, use no shell/PATH or inherited command environment, and clean up after every completion/failure path. | `issue:L3DigitalNet/project-standards#142` | Must | T3 | T3, T4 | PV-T3-001, PV-T4-001 |
| REQ-008 | Timeout, cancellation, crash, nonzero exit, malformed/oversize result, diagnostic overflow, digest mismatch, schema violation, descendant retention, and cleanup shall produce stable bounded behavior without raw installed/temp paths or secret environment values. | `issue:L3DigitalNet/project-standards#142`; ADR 0025 | Must | T3 | T2, T3, T5 | PV-T2-001, PV-T3-001, PV-T5-001 |
| REQ-009 | A Go-backed payload fixture shall pass real reconcile, validate, and drift-check flows, prove input/resource/output equivalence and materialized mode/digest/cleanup, and reject a wrong platform and plausible hollow transports. | `issue:L3DigitalNet/project-standards#142` | Must | T4 | T4 | PV-T4-001 |
| REQ-010 | The fixture's committed binary shall rebuild byte-identically under the shared pinned Go contract and be included in the repository Go verification gate; declaration tests shall fail when command platform/mode are missing or invalid. | `issue:L3DigitalNet/project-standards#142` | Must | T4 | T3, T4, T6 | PV-T3-001, PV-T4-001, PV-T6-001 |
| REQ-011 | No production successor payload shall be cut here; any future Agent Handoff successor shall start only from green T4 and #156 T2 checkpoints under a separate plan. | owner boundary; `issue:L3DigitalNet/project-standards#156` | Must | T4 | T4 | PV-T4-001 |
| REQ-012 | The bounded Python path shall complete the serial real-catalog planning proof in less than 7.5 seconds and the 100-order deterministic proof in less than 30 seconds. | `request`; measured T2 profiling | Must | T2 | T2 | PV-T2-001 |
| REQ-013 | The repository Git-mode inventory shall classify both intentionally executable T4 artifacts as post-anchor entries while preserving their `100755` modes, accepted bytes/digests, complete executable equality, anchor/predecessor rules, and every unrelated classification. | `request`; repository-hygiene policy; runner-label T3 gate receipt | Must | T6 | T6 | PV-T6-001 |

## 7. Verification and Evidence Strategy

- **Authoritative commands:** focused `tests/control_plane/test_providers.py`, shared-runner/command-provider tests including T5's real plan-bound runner-selection regression, `tests/mcp_services/test_providers.py`, payload execution/schema tests, command-provider end-to-end tests, and T6's focused/full repository-hygiene file; the five package validators; generated schema/projection checks; Git-tracked Prettier/markdownlint; Ruff, BasedPyright, and coverage; `make go-check`; `git diff --check`; fast `scripts/verify.sh` at intermediate engine and T6 correction checkpoints and `scripts/verify.sh --full` after resumed T4.
- **Runner placement:** Git-history/index/diff commands and the Git-dependent `scripts/verify.sh` lanes run locally. Synchronized-tree-compatible CPU-heavy pytest, BasedPyright, and Go gates run as `rexec -- command [args]`; shell syntax is used only through `rexec --shell` when an actual shell expression is required. Package/Markdown checks that depend on Git corpus selection remain local.
- **Prerequisites:** each new implementation worktree runs `scripts/bootstrap-worktree.sh` first and again after any `src/**` or payload-source change. Run `rexec config show`, `rexec doctor`, and `rexec setup --check` before classifying a remote failure as repository failure.
- **Oracles:** ADR 0025's bound/teardown/cap clauses; ADR 0030's exactly-once-per-provider-invocation MCP delegation rule; `authoritative_provider_input`, `_contribution_content`, `_verify`, `_verification_input`, and the existing caller-versus-seam authority contract; the T4 real-route render/verify receipt; V2 manifest/resource digests; existing Python direct/MCP outputs and `ControlPlaneError.__cause__` parity; the sanitized child `kind`/`detail` frame; path-like-only `OpenAudit` entries; declared schemas; independent on-disk hash/mode/liveness inspection; the shared Go rebuild-and-compare library; Git-index mode `100755`; the accepted binary/payload digests; and equality against the repository hygiene policy's complete classified executable set.
- **Negative controls:** old MCP-local runner retained, direct Python call still in-process, command dispatch nested inside the MCP Python worker, planning-time command forced through the Python-only runner, target VERIFY executed while constructing its input, required render result stubbed or skipped, completed plan snapshots reconstructed outside `provider_dispatch_input`, composite command receiving the empty seam directive instead of authority-built input, single-provider caller input rebuilt or replaced, package-ID routing, missing `ValueError` cause/detail, child-supplied unknown/custom kind imported or executed, path/secret canary surviving sanitization, integer result FD recorded as a filesystem path, path-like target ignored, result on stdout, missing/extra resource, Base64 corruption, installed resource forced non-executable, materialized byte mutation, unsupported platform, environment canary, exact/N+1 cap, stubborn descendants, malformed result, wrong schema, binary/source mismatch, chmod of either T4 artifact, one omitted/extraneous executable classification, fixture binary mislabeled as general tooling, changed anchor/predecessor policy, or weakened complete-inventory equality.
- **Test layers:** architecture/import-owner inspection, characterization, Python unit/contract, process/security/cleanup, payload schema generation, Go unit/race/build/audit, control-plane integration, MCP regression, extracted-wheel/materialization, and end-to-end reconcile/validate/drift-check.
- **Evidence:** command output is ephemeral. Retain the revision-4 failed-gate receipt in execution notes: statics green 4:10; ordinary 17 failed/4,858 passed 13:41; compatibility green 34:04; performance green 0:39; total 34:45. Retain T3's nine-path/299-focused-test green receipt and terminal checkpoint. Retain T4's correct real-route receipt and final accepted binary/payload digests. Retain the runner-label T3 discovery receipt: one direct-local fast gate on integrated base `e033260a`, exit 1 in 33:47; statics green 4:18; ordinary 4,959 passed/one failed in 12:36; compatibility 141 passed in 33:06; performance 5 passed/5,101 deselected in 38.19s; coverage 90%; sole failure the complete Git-mode classification with exactly two unexpected `100755` T4 paths. The external candidate remains nine staged paths with no edit/rerun/commit. The committed ADR, tests, fixture source/binary, build script, and checkpoint trailers retain the durable contract; no separate evidence file is required.
- **Late failure:** block the owning task. If a completed checkpoint is disproved, append a correction task with `corrects:` and `discovered_from:` and rerun the failed proof; never weaken a cap, skip a platform/materialization control, or reopen completed history.

## 8. Execution Summary

| Task | Title | Disposition | Work Type | Phase | Depends On | Requirement(s) | Primary Proof | Parallel / Conflict |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| T1 | Record the command-provider trust and platform decision | active | documentation | P1 | None | REQ-001, REQ-002 | PV-T1-001 | no / ADR corpus; external #162 |
| T2 | Extract the shared bounded runner and preserve Python behavior | active | brownfield-behavior | P2 | T1 | REQ-003, REQ-004, REQ-008, REQ-012 | PV-T2-001 | no / owns shared runner and dispatcher seam |
| T3 | Implement the V2 command declaration and runtime boundary | active | behavior | P3 | T2 | REQ-005, REQ-006, REQ-007, REQ-008, REQ-010 | PV-T3-001 | no / contributes to T2-owned runner/dispatcher |
| T4 | Prove the Go payload fixture and core platform checkpoint | active | behavior | P4 | T3, T5 | REQ-006, REQ-007, REQ-009, REQ-010, REQ-011 | PV-T4-001 | no / fixture, Makefile, final gate |
| T5 | Correct plan-bound command input authority | active | brownfield-behavior | P4 | T3 | REQ-006, REQ-008 | PV-T5-001 | no / corrects T3; discovered from blocked T4 |
| T6 | Classify the T4 fixture executables in repository mode policy | active | behavior | P5 | T4 | REQ-010, REQ-013 | PV-T6-001 | no / corrects T4; external runner-label T3 gate waits |

External gates:

- **EG-ADR:** T1's validated checkpoint is available to issue #162. The #162 final corpus sweep may begin only after that commit so it includes the new active ADR and all inbound links once.
- **EG-156 (deferred successor gate):** issue #156 plan T2/PV-T2-001 must be terminal at a bridge-validated checkpoint before any later production successor payload is authored or cut. It does not block T1–T5's synthetic-fixture platform work. If #156's plan is revised, the successor plan must use its approved replacement task/checkpoint rather than inferring equivalence.

## 9. Implementation Tasks

### Phase P1: Architecture Authority

#### T1: Record the command-provider trust and platform decision

- **disposition:** active
- **outcome:** The active ADR corpus contains one accepted record that unambiguously governs generic bounded provider execution, the command ABI, materialization, platform narrowing, environment/diagnostic policy, rejected alternatives, and its relationship to ADRs 0025/0027; #162 can consume its checkpoint before the final sweep.
- **work_type:** documentation
- **checkpoint:** one green `docs(adr)` commit with task, requirement, proof IDs, and the required `Plan-*` checkpoint trailers
- **boundary:** cross-task
- **depends_on:** []
- **dependency_reason:** none
- **requirements:** [REQ-001, REQ-002]
- **proof:** [PV-T1-001]
- **source_refs:** [issue:L3DigitalNet/project-standards#142, repo:docs/adr/adr-0025-project-standards-mcp-service-and-sdk-boundary.md#provider-execution-boundary, repo:docs/adr/adr-0027-adopt-go-alongside-python-with-neutral-tooling.md, repo:standards/adr/versions/1.5/README.md]
- **consumes:** [approved owner decision, ADR 1.5 authoring contract, existing MCP boundary and Go coexistence decisions]
- **produces:** [command-provider-execution-adr-v1, EG-ADR checkpoint]
- **preserves:** [ADR 0025's MCP SDK/service decisions, ADR 0027's neutral tooling scope, stable ADR numbering/links, #162's ownership of its later corpus sweep]
- **invariants:** [ADR 0030 is the exact next allocation after integrated ADR 0029; reciprocal related edges connect ADR 0030 with ADRs 0025 and 0027; decision boundary excludes Agent Handoff migration and future platforms; evidence and authority links follow the repo convention]
- **executor_discretion:** [concise prose arrangement, non-substantive index wording]
- **files:** [`docs/adr/adr-0030-command-provider-execution-boundary.md` (create; owner T1), `docs/adr/adr-0025-project-standards-mcp-service-and-sdk-boundary.md` (modify only reciprocal related edge; owner T1), `docs/adr/adr-0027-adopt-go-alongside-python-with-neutral-tooling.md` (modify only reciprocal related edge; owner T1), `docs/adr/README.md` (modify; owner T1)]
- **parallel_safe:** no
- **conflicts_with:** []
- **supersedes:** []
- **superseded_by:** []
- **evidence:** [ephemeral]
- **recovery:** if an ADR number/path was concurrently claimed, stop before writing and allocate the next number under ADR 1.5. If #162 has begun, pause this task and coordinate one serialized corpus owner; never land an ADR after the sweep and leave it unswept.
- **acceptance:** PV-T1-001 proves the new accepted ADR contains every binding D-002–D-008 boundary, names the portability consequence and both rejected alternatives, links ADRs 0025/0027 reciprocally as required by the ADR standard, appears once in the index, validates under frontmatter/reference/Markdown checks, and its checkpoint is explicitly handed to #162.
- **sub-tasks:**
  - **T1.1 INVENTORY** — confirm the next ADR number, ADR 1.5 filename/frontmatter rules, current inbound decision edges, and #162 execution state before editing.
  - **T1.2 UPDATE** — author the bounded record and index entry; do not modify implementation or perform #162's evidence-vs-authority sweep.
  - **T1.3 VERIFY REFERENCES** — search tracked paths for the new ADR ID/path and related ADR edges; verify no stale filename, duplicate number, unresolved placeholder, or missing reciprocal edge.
  - **T1.4 Verify Task** — run scoped frontmatter/reference, Prettier, markdownlint, `git diff --check`, and bridge validation; create the checkpoint and communicate EG-ADR to #162 without mutating GitHub state.

### Phase P2: Shared Bounded Python Baseline

#### T2: Extract the shared bounded runner and preserve Python behavior

- **disposition:** active
- **outcome:** Direct CLI/control-plane and MCP calls execute Python payload providers through one control-plane-owned bounded process implementation, with no MCP-local duplicate or nested worker and no observable typed-result regression.
- **work_type:** brownfield-behavior
- **checkpoint:** one green bounded-dispatch commit with task, requirement, proof IDs, and the required `Plan-*` checkpoint trailers
- **boundary:** cross-task
- **depends_on:** [T1]
- **dependency_reason:** consumes `command-provider-execution-adr-v1`, which fixes runner ownership, ABI direction, limits, environment split, and confirmation obligations before source movement
- **requirements:** [REQ-003, REQ-004, REQ-008, REQ-012]
- **proof:** [PV-T2-001]
- **source_refs:** [request, issue:L3DigitalNet/project-standards#142, repo:docs/adr/adr-0025-mcp-service-and-sdk-boundary.md#provider-execution-boundary, repo:src/project_standards/control_plane/providers.py::invoke_provider, repo:src/project_standards/control_plane/diagnostics.py::ControlPlaneError, repo:src/project_standards/control_plane/provider_subprocess.py::run_provider_subprocess, repo:src/project_standards/control_plane/provider_worker.py::run_request, repo:tests/mcp_services/test_providers.py::test_slow_provider_returns_bounded_diagnostic_and_worker_is_reaped, repo:tests/control_plane/test_providers.py::test_provider_returns_typed_findings_mutation_plan_and_migration_report, repo:tests/mcp_services/security/test_consumer_boundaries.py::OpenAudit, repo:tests/package_compatibility/test_catalog_matrix.py::deny_provider_child_processes, repo:tests/package_compatibility/test_performance.py::test_real_catalog_plans_inside_scale_and_time_boundary]
- **consumes:** [command-provider-execution-adr-v1, current Python provider input/resource/output and exception-cause contract, qualified InstalledPayload object seams, sanitized child error frame kind/detail, OpenAudit path-access oracle, revision-4 fast-gate receipt, ADR 0025 bounded transport behavior, approved 7.5-second and 30-second performance budgets]
- **produces:** [bounded-provider-runner-v1, python-provider-execution-capsule-v1, bounded-python-provider-dispatch-v1]
- **preserves:** [all V2 Python operations/effects, schemas, typed results, output notice semantics, live-path integrity, qualified synthetic and monkeypatched InstalledPayload behavior, public MCP DTO/service results, request qualification, finding ordering, content-safe public errors, safe ValueError cause/detail parity, path-like filesystem audit coverage, V1 runner behavior, one-child architecture, existing caps, 7.5-second real-catalog budget, 30-second deterministic-order budget]
- **invariants:** [one owner for selector pump/signals/caps/frame/diagnostics; direct and MCP callers do not wrap the shared runner in another provider process; child exception frames contain only sanitized bounded kind/detail; parent cause reconstruction uses a closed literal safe built-in allowlist including ValueError; unknown kinds cannot import/resolve/execute arbitrary exceptions and remain fixed generic; public diagnostics expose no raw paths/secrets; OpenAudit ignores only integer builtins.open/io.open transport handles and records every path-like target; parent verifies exact capsule bytes and child does not rediscover the whole payload manifest; compatibility permits the one required runner child but rejects nested/provider-created children; result fd is distinct from stdout/stderr; every exit path tears down/reaps the group; 7.5-second and 30-second ceilings remain exact]
- **executor_discretion:** [private module/type/helper names, exact extraction file split, test fixture factoring, behavior-preserving comment placement]
- **files:** [`src/project_standards/control_plane/provider_subprocess.py` (create; owner T2), `src/project_standards/control_plane/provider_worker.py` (create; owner T2), `src/project_standards/control_plane/providers.py` (modify; owner T2), `src/project_standards/mcp_services/providers.py` (modify; owner T2), `src/project_standards/mcp_services/provider_worker.py` (delete or reduce to compatibility-free removal; owner T2), `tests/control_plane/test_provider_subprocess.py` (create; owner T2), `tests/control_plane/test_providers.py` (modify; owner T2), `tests/mcp_services/test_providers.py` (modify; owner T2), `tests/mcp_services/test_provider_worker.py` (delete/migrate; owner T2), `tests/mcp_services/security/test_consumer_boundaries.py` (modify OpenAudit integer-FD distinction only; owner T2), `tests/package_compatibility/test_catalog_matrix.py` (modify only to permit the required shared-runner child while retaining nested/provider-created process controls; owner T2), `tests/package_compatibility/test_performance.py` (modify only the real-catalog ceiling from 5.0 to 7.5 seconds and explain the accepted process-isolation cost; owner T2)]
- **parallel_safe:** no
- **conflicts_with:** [T3, T4]
- **supersedes:** []
- **superseded_by:** []
- **evidence:** [ephemeral]
- **recovery:** preserve the green statics/compatibility/performance receipt and the 17-failure ordinary evidence before correction. If cause parity fails, correct only the closed built-in mapping and sanitized frame handling; never import a child-named module/type, call a custom constructor, publish raw detail, or replace unknown kinds with an attacker-selected class. If the audit still confuses handles and paths, ignore only integer builtins.open/io.open targets and retain all path-like recording. If either approved budget turns red, correct T2 without weakening isolation, caps, one-child ownership, audit scope, batching boundaries, or limits. Do not keep both runners or add a double-spawning wrapper.
- **acceptance:** PV-T2-001 observes one child for direct Python dispatch and the same single child for MCP dispatch; exact finding/mutation/migration objects, output notices, input/resource bytes, failure codes, timeout/caps/truncation, result-fd behavior, cancellation, crash, descendant cleanup, and sanitized diagnostics match their frozen characterization values; import/source inspection finds one transport implementation.
- **sub-tasks:**
  - **T2.1 CHARACTERIZE** — preserve the revision-4 fast-gate receipt and failing ordinary node IDs; confirm statics 4:10 green, ordinary 17 failed/4,858 passed in 13:41, compatibility 34:04 green, performance 0:39 green, total 34:45; inspect the child frame's sanitized kind/detail and OpenAudit wrapper behavior before edits.
  - **T2.2 Verify Baseline** — rerun focused unaffected runner/cap/process/performance controls and confirm the implementation still has one child, unchanged limits, green budgets, and no regression outside exception-cause/audit parity; do not reinterpret the 17 failures as architecture or budget defects.
  - **T2.3 RED** — add focused regressions requiring a provider `ValueError` to appear as `ControlPlaneError.__cause__` with the sanitized detail; requiring path/secret redaction and fixed generic behavior for an unknown/custom kind without import or constructor execution; and proving OpenAudit ignores integer inherited `open`/`io.open` handles but still records string/bytes/PathLike targets.
  - **T2.4 Verify RED** — run only those focused regressions and confirm correct-reason failure: the current parent drops the allowlisted ValueError cause/detail and current OpenAudit records integer result descriptors as paths, while redaction, unknown-kind nonexecution, and path-like controls prevent hollow or unsafe RED.
  - **T2.5 GREEN** — retain the sanitized child frame; add a literal parent-side safe built-in exception map including ValueError and a fixed generic unknown-kind path; chain the reconstructed safe cause from ControlPlaneError without changing its public bounded message; update OpenAudit to skip only integer builtins.open/io.open targets and continue recording every path-like target.
  - **T2.6 Verify GREEN** — run the new cause/detail/redaction/unknown-kind and FD-vs-path regressions, then direct/MCP parity, every ADR 0025 process-path assertion, qualified synthetic/monkeypatched payload regressions, exact typed effects, real-provider compositions, compatibility, import-boundary, and one-child assertions.
  - **T2.7 REFACTOR** — keep the safe exception map explicit/local and comments focused on the trust boundary; remove any dynamic resolution path; keep the OpenAudit integer check narrow; retain the single transport owner, fixed caps, diagnostic formatting, teardown, and ABI comments.
  - **T2.8 Verify Task** — rerun bootstrap; focused suites including all 17 prior ordinary failures; five package checks; Markdown/Ruff; `rexec -- uv run basedpyright`; `git diff --check`; run both serial performance proofs under the unchanged 7.5/30-second ceilings; rerun direct-local `scripts/verify.sh`, require all four lanes green, and create the checkpoint.

### Phase P3: Command Contract and Runtime

#### T3: Implement the V2 command declaration and runtime boundary

- **disposition:** active
- **outcome:** A valid V2 `command` declaration dispatches its integrity-addressed resource through the shared runner on `linux/amd64`, while every declaration, platform, transport, materialization, cleanup, schema, and diagnostic failure is deterministic and fail-closed.
- **work_type:** behavior
- **checkpoint:** one green command-runtime commit with task, requirement, proof IDs, and the required `Plan-*` checkpoint trailers
- **boundary:** public
- **depends_on:** [T2]
- **dependency_reason:** consumes `bounded-provider-runner-v1` and `bounded-python-provider-dispatch-v1`; command execution must extend the proven single runner rather than create another process implementation
- **requirements:** [REQ-005, REQ-006, REQ-007, REQ-008, REQ-010]
- **proof:** [PV-T3-001]
- **source_refs:** [issue:L3DigitalNet/project-standards#142, repo:docs/adr/adr-0025-mcp-service-and-sdk-boundary.md#provider-execution-boundary, repo:docs/adr/adr-0030-command-provider-execution-boundary.md#shared-bounded-runner, repo:src/project_standards/package_contract/payload.py::ProviderDeclaration, repo:src/project_standards/control_plane/providers.py::invoke_provider, repo:src/project_standards/control_plane/planner.py::_verification_requests, repo:src/project_standards/control_plane/provider_worker.py::authoritative_provider_input, repo:src/project_standards/mcp_services/providers.py::_dispatch, repo:src/project_standards/provider_runner.py::_run_python_provider]
- **consumes:** [bounded-provider-runner-v1, V2 provider/input/output/resource/integrity contracts, normalized host platform, ADR command ABI, MCP caller-versus-composite input-authority seam]
- **produces:** [v2-command-provider-declaration-v1, command-provider-wire-v1, command-provider-materialization-v1]
- **preserves:** [existing schema version, all existing manifests/generated schema compatibility, Python entrypoint grammar/behavior, documentation-only behavior, V1 command refusal, generic package-independent routing, single-provider MCP caller input, composite MCP family/plan-bound/generic input authority, MCP DTO and per-result failure behavior]
- **invariants:** [platform checked before disk/spawn; only declared resources cross; input validated before spawn; output validated after bounded parse; no shell/PATH/installed path; on-disk digest and 0755 checked before execution; cleanup follows group teardown; temp/installed paths and inherited secrets never enter diagnostics; MCP branches only on provider kind; Python retains the existing worker path; command calls the control-plane invocation directly from the parent so its process is the sole child; single-provider command dispatch passes caller input unchanged; composite command dispatch calls authoritative_provider_input against the qualified selection before invoking command bytes, uses its mapping when present, retains the declared empty generic input only when it returns None, and preserves content-safe per-result failure on any other authority error; no package identity selects the route]
- **executor_discretion:** [private Pydantic helper names, normalized platform helper placement, temporary-directory implementation satisfying the contract, internal diagnostic code representation consistent with existing `ControlPlaneError`]
- **files:** [`src/project_standards/package_contract/payload.py` (modify; owner T3), `src/project_standards/schemas/standard-payload.schema.json` (regenerate; owner T3), `src/project_standards/control_plane/providers.py` (modify; owner T2), `src/project_standards/control_plane/provider_subprocess.py` (modify; owner T2), `src/project_standards/control_plane/provider_worker.py` (modify; owner T2), `src/project_standards/control_plane/planner.py` (modify; owner T3), `src/project_standards/mcp_services/providers.py` (modify command kind-routing and input-authority delegation only; owner T2), `src/project_standards/provider_runner.py` (inspect and update only contract comments/tests if needed; owner T3), `tests/package_contract/test_payload_execution_contracts.py` (modify; owner T3), `tests/package_contract/test_schemas.py` (modify; owner T3), `tests/control_plane/test_command_providers.py` (create; owner T3), `tests/control_plane/test_providers.py` (command regression contribution; owner T2), `tests/mcp_services/test_providers.py` (command regression contribution; owner T2)]
- **parallel_safe:** no
- **conflicts_with:** [T2, T4]
- **supersedes:** []
- **superseded_by:** []
- **evidence:** [ephemeral]
- **recovery:** preserve the current nine-path direct-runtime candidate and its 299-focused-test green receipt while T3 remains blocked on the recorded MCP routing omission. Resume only after revision replacement. A declaration/schema/materialization or MCP authority failure leaves no extra executable or child and does not alter the repository; correct T3 without resetting the preserved candidate. Never route a command through the Python worker, forward a composite seam directive as provider input, rebuild single-provider caller input, fall back to Python/installed bytes/a shell/an undeclared platform/an unverified mode or digest/an unbounded result, or add a package-ID branch.
- **acceptance:** PV-T3-001 proves the exact declaration matrix and generated schema; correct stdin/result/resource equivalence; Python parity; supported/unsupported platform behavior; installed-path nonexecution; materialized mode/digest; closed environment; exact/N+1 caps; timeout/cancel/crash/nonzero/descendant cleanup; safe deterministic diagnostics; V1 refusal; and no package-ID branch.
- **sub-tasks:**
  - **T3.1 RED** — add declaration/schema and process-boundary tests first, including focused MCP single/composite command cases that count children and distinguish caller input, authority-built composite input, and the declared generic empty input. Expected failures are the current Python-only entrypoint grammar/runtime gate, missing platform/materialization/transport behavior, and `_dispatch` forcing command through the Python worker, not import, fixture, or environment failure.
  - **T3.2 Verify RED** — run the focused contract/runtime tests and prove each fails for the missing `command` behavior or nested MCP command route while T2's Python parity remains green; mutate the composite authority result and caller input independently so an empty-input or rebuilt-input false positive cannot pass.
  - **T3.3 GREEN** — implement kind-specific declaration/schema logic, platform preflight, canonical envelope, command environment/argv, private materialization and rehash, bounded result handling, and generic planner routing through T2's runner. In MCP `_dispatch`, leave Python on its existing path; route command directly to `invoke_provider`; preserve single-provider caller input; and, for the composite seam directive, call `authoritative_provider_input` against the qualified selection before command dispatch, retaining `{}` only for its explicit `None` result.
  - **T3.4 Verify GREEN** — run declaration/schema, direct runtime, MCP single/composite command routing, input-authority, one-child, transport, materialization, platform, diagnostic, and Python-parity matrices, including exact and `N + 1` caps; prove no package-ID branch and existing composite per-result failure behavior.
  - **T3.5 REFACTOR** — remove duplicated kind branches and stale MCP-worker/input-authority comments; ensure every new comment/docstring carries only ABI, invariant, failure, or cross-file contract information.
  - **T3.6 Verify Task** — rerun bootstrap; focused suites; five package checks including schema generation/projection; Markdown/Ruff; `rexec -- uv run basedpyright`; `git diff --check`; direct-local `scripts/verify.sh`; create the checkpoint.

### Phase P4: Go Fixture and Integrated Acceptance

#### T4: Prove the Go payload fixture and core platform checkpoint

- **disposition:** active
- **outcome:** A reproducibly built Go command-provider payload fixture passes real reconcile, validate, drift-check, wheel-install/materialization, platform, and failure-control paths, leaving one complete local platform checkpoint that a later Agent Handoff migration plan can consume.
- **work_type:** behavior
- **checkpoint:** one green fixture/integration commit with task, requirement, proof IDs, and the required `Plan-*` checkpoint trailers
- **boundary:** cross-task
- **depends_on:** [T3, T5]
- **dependency_reason:** consumes `v2-command-provider-declaration-v1`, `command-provider-wire-v1`, `command-provider-materialization-v1`, and T5's corrected `plan-bound-command-input-authority-v1`; the synthetic fixture is not a production successor cut, so EG-156 gates only the deferred package migration
- **requirements:** [REQ-006, REQ-007, REQ-009, REQ-010, REQ-011]
- **proof:** [PV-T4-001]
- **source_refs:** [issue:L3DigitalNet/project-standards#142, repo:scripts/lib/go-reproducible-build.sh, repo:Makefile::go-verify-binary, repo:tests/control_plane/test_end_to_end.py::test_every_adapter_converges_in_one_virtual_tree_and_second_apply_is_byte_noop, repo:tests/mcp_services/test_providers.py::test_drift_check_preserves_reconciliation_and_typed_provider_results]
- **consumes:** [v2-command-provider-declaration-v1, command-provider-wire-v1, command-provider-materialization-v1, plan-bound-command-input-authority-v1, repository Go toolchain/build library, real control-plane/MCP entrypoints]
- **produces:** [go-command-provider-fixture-v1, command-provider-core-checkpoint-v1]
- **preserves:** [no production family or successor payload, Catalog 5/default/self-host selections, released binary bytes, existing Go checks, no GitHub/release mutation]
- **invariants:** [fixture implements the ADR ABI rather than importing Python; build script is the sole output recipe and is listed in build/verify gates; e2e routes do not bypass public planner/executor/service owners; installed fixture resource may be 0644 because only the verified temporary copy executes]
- **executor_discretion:** [fixture package/internal organization, compact operation switch, test table factoring, fixture standard ID/version that does not collide with production identities]
- **files:** [`cmd/command-provider-fixture/` (create; owner T4), `internal/commandproviderfixture/` (create if needed; owner T4), `scripts/build-command-provider-fixture.sh` (create; owner T4), `Makefile` (modify; owner T4), `tests/fixtures/command-provider/` (create; owner T4), `tests/control_plane/test_command_provider_end_to_end.py` (create; owner T4), `tests/mcp_services/test_providers.py` (end-to-end contribution; owner T2), `tests/package_contract/test_command_provider_fixture.py` (create; owner T4), `scripts/README.md` (modify only if required; owner T4)]
- **parallel_safe:** no
- **conflicts_with:** [T2, T3, T5]
- **supersedes:** []
- **superseded_by:** []
- **evidence:** [ephemeral]
- **recovery:** preserve the blocked T4 worktree and its fixture/build/e2e edits while T5 corrects the real plan-bound route. Resume only after T5 is terminal, then rerun the exact failed composite route before broader T4 proof. If build/e2e proof still fails after T5, revert T4 only or append another correction task; do not cut a production payload, relax the ABI, regenerate from an unpinned Go toolchain, or treat helper-only invocation as end-to-end evidence. EG-156 remains mandatory before any later production successor cut.
- **acceptance:** PV-T4-001 proves the fixture binary matches a pinned rebuild, a command payload goes through real reconcile/validate/drift-check and preserves exact input/resource/output behavior, installed bytes are never executed, temporary mode/digest/cleanup and platform refusal are independently observed, hollow transports fail, all Python/MCP regressions remain green, and no production payload/catalog/release/GitHub path changed.
- **sub-tasks:**
  - **T4.1 RED** — validate T1–T3 checkpoints, then add the Go source/build/fixture/e2e tests with no committed binary. Expected failures are the missing built executable and absent end-to-end command result, while T3 unit contracts remain green.
  - **T4.2 Verify RED** — run focused Python and Go tests; confirm failure is caused by the absent fixture implementation, not toolchain mismatch, schema/projection drift, or a stale checkpoint.
  - **T4.3 GREEN** — implement the minimal Go ABI fixture with explicit JSON tags and error returns, build committed bytes through the shared script, wire both `go-binary` and `go-verify-binary`, and complete the synthetic payload/e2e routes.
  - **T4.4 Verify GREEN** — prove real route outputs plus materialization/platform controls; run Go race/unit/static/audit and byte comparison.
  - **T4.5 REFACTOR** — consolidate fixture helpers without changing the ABI and audit Go/Python comments for cross-file ABI and cleanup accuracy.
  - **T4.6 Verify Task** — rerun bootstrap; PV-T4-001; T2/T3 regressions; five package checks; Markdown/Ruff; `rexec -- uv run basedpyright`; `rexec -- make go-check`; `git diff --check`; direct-local `scripts/verify.sh --full`; validate plan/checkpoint; create the checkpoint.

#### T5: Correct plan-bound command input authority

- **disposition:** active
- **outcome:** MCP composite command VERIFY builds the executor-equivalent plan-bound input even when reconciliation needs a command-backed render contribution, without nesting a worker, executing the target verify during construction, changing Python authority, or adding package-specific routing.
- **work_type:** brownfield-behavior
- **checkpoint:** one green correction commit with task, requirement, proof IDs, correction provenance, and the required `Plan-*` checkpoint trailers
- **boundary:** cross-task
- **depends_on:** [T3]
- **dependency_reason:** corrects T3's completed plan-bound input-authority route before blocked T4 can resume; consumes the existing kind-aware `invoke_provider` route and the planner/executor/provider-input authorities rather than creating a new runner or snapshot projection
- **requirements:** [REQ-006, REQ-008]
- **proof:** [PV-T5-001]
- **source_refs:** [request, issue:L3DigitalNet/project-standards#142, repo:docs/adr/adr-0030-command-provider-execution-boundary.md#shared-bounded-runner, repo:src/project_standards/control_plane/provider_worker.py::authoritative_provider_input, repo:src/project_standards/control_plane/planner.py::_contribution_content, repo:src/project_standards/control_plane/executor.py::_verify, repo:src/project_standards/control_plane/provider_inputs.py::_verification_input, repo:src/project_standards/control_plane/providers.py::invoke_provider, repo:src/project_standards/mcp_services/providers.py::_dispatch, repo:tests/mcp_services/test_providers.py::test_mcp_command_parent_uses_authoritative_composite_input]
- **consumes:** [bounded-provider-runner-v1, v2-command-provider-declaration-v1, mcp-command-dispatch-v1, executor plan-bound verification authority, T4 real-route failure receipt]
- **produces:** [plan-bound-command-input-authority-v1]
- **preserves:** [T3 command declaration/wire/materialization outcome, family-first authority, plan-bound verification authority, explicit generic None outcome, single-provider caller input, Python MCP in-child construction and execution, one child per provider invocation, per-result service failure mapping, package-independent routing, all caps/budgets, every unrelated source byte and test]
- **invariants:** [authoritative_provider_input remains the routing owner and provider_dispatch_input remains the only snapshot-shape owner; its default planner runner stays invoke_provider_in_child for Python-worker callers; MCP parent command dispatch passes invoke_provider as the planner runner; planner invokes render-fixture exactly once through the existing direct command route when its content is required; no Python wrapper surrounds that command; verify-fixture is never invoked during its own input construction and is invoked exactly once afterward; required render output is not stubbed, cached, or reconstructed; no package ID or fixture identity selects a branch; the authoritative composite-input fake accepts keyword-only planner_runner and asserts identity with providers.invoke_provider; no other test double, parameter, assertion, or test changes]
- **executor_discretion:** [private callable type alias and parameter name, focused fixture factoring, whether the existing helper docstring or adjacent invariant comment best records the two caller contexts]
- **files:** [`src/project_standards/control_plane/provider_worker.py` (modify explicit planner-runner seam only; owner T2), `src/project_standards/mcp_services/providers.py` (modify `_dispatch_command` call to supply the parent kind-aware runner only; owner T2), `tests/control_plane/test_command_providers.py` (modify focused plan-bound runner-selection regressions only; owner T3), `tests/mcp_services/test_providers.py` (modify only the authoritative composite-input test double; owner T2)]
- **parallel_safe:** no
- **conflicts_with:** [T2, T3, T4]
- **supersedes:** []
- **superseded_by:** []
- **corrects:** [T3]
- **discovered_from:** [T4]
- **evidence:** [ephemeral]
- **recovery:** resume the candidate T5 worktree whose only edits are the original three T5-claimed paths; it has no unclaimed edit, gate, or commit. Preserve its correct-reason RED and 1/1 production-seam GREEN evidence, then add only the claimed `tests/mcp_services/test_providers.py` correction. If any unrelated byte changes, the focused failure has another cause, or the fake does not receive `providers.invoke_provider`, discard only that test correction and block without changing the three preserved implementation/test edits or T4. If the explicit runner seam changes Python construction, executes target VERIFY early, wraps a planning command in a Python child, requires a package-ID branch, or reconstructs snapshots outside provider_dispatch_input, block T5 and preserve T4 unchanged. After T5 checkpoints, resume the existing T4 worktree and rerun its failed real route before any full gate.
- **acceptance:** PV-T5-001 proves a real plan-bound command VERIFY receives the same snapshot object that the executor derives from the completed plan when that plan requires command-backed render content; the render command and later verify command each execute exactly once as separate non-nested provider invocations; the target verify does not execute during input construction; Python uses its existing in-child path; family-first and generic-None cases remain exact; no package-ID route, new transport, cap, budget, or fixture behavior is introduced.
- **sub-tasks:**
  - **T5.1 CHARACTERIZE** — preserve the T4 receipt and the focused control-test chain `provider_worker.py:119` → `planner._contribution_content` → `providers._qualified_python_provider`: plan construction calls command `render-fixture`, the Python-only worker rejects it with `provider kind is not executable in the Python worker`, and target verify is absent. Record the render declaration metadata and snapshot inputs so the regression cannot pass through a hollow provider.
  - **T5.2 RED** — retain the observed correct-reason RED from the new focused control test, not a missing fixture, import, schema, or platform failure. Require real render content to affect the completed plan, capture the input projected for verify, count provider IDs/operations and process boundaries, and retain Python, family-first, and generic-None controls.
  - **T5.3 Verify RED** — preserve evidence that the focused control failed through `provider_worker.py:119` → `planner._contribution_content` → `providers._qualified_python_provider` with `provider kind is not executable in the Python worker`; target verify was absent, and substituting fixed render bytes or empty snapshots would fail the oracle.
  - **T5.4 GREEN** — add an explicit planner-runner parameter to `authoritative_provider_input`, defaulting to `invoke_provider_in_child`; use it only for `PlannerRequest.provider_runner`. In MCP `_dispatch_command`, pass the existing kind-aware `invoke_provider`. Do not add a new dispatcher, execute the target verify from the helper, change `provider_dispatch_input`, or branch on standard/provider/package identity.
  - **T5.5 Verify GREEN** — preserve the observed 1/1 production-seam pass: render ran once as a direct child with precondition `sha256:77ed2d5d1b55425244f0247ac95488fe1891e9667d98ce4c003790151fed6988`, content `sha256:5b1d0f0d4222a93b9df84ded1558e0be742a6266b06b6829c04b8f1dfc5755bb`, mode `0644`, and executor-equivalent projection; target verify was absent during construction and ran later as the second direct child. Correct only the existing authoritative composite-input fake to accept keyword-only `planner_runner` and assert it is `providers.invoke_provider`. Its family/plan/generic parameters currently fail solely at that fake signature while adjacent failure-isolation and Python-path nodes pass (`2 passed, 3 failed`). Then prove all three parameters pass without changing any other test byte, and retain the family-first, generic-None, failure-isolation, caps, no-package-ID, and no-nested-worker controls.
  - **T5.6 Verify Task** — rerun the exact authoritative composite-input node first; rerun bootstrap after source edits; run the focused command/provider-worker/MCP routing regressions through `rexec -- uv run pytest ...`; run five package checks and scoped Markdown locally where Git corpus selection is required; run Ruff and `rexec -- uv run basedpyright`; run `git diff --check`; run direct-local `scripts/verify.sh`; validate the plan/checkpoint; create the correction checkpoint. Before any gate or commit, confirm the diff contains only the four claimed T5 paths and `tests/mcp_services/test_providers.py` differs only in that one fake signature/assertion. Do not run T4's final full gate here.

### Phase P5: Post-checkpoint Repository Policy Correction

#### T6: Classify the T4 fixture executables in repository mode policy

- **disposition:** active
- **outcome:** The repository's Git-mode inventory classifies both intentionally executable T4 artifacts without changing their modes, bytes, reproducible digest, payload aggregate, production packages, or any other mode-policy classification.
- **work_type:** behavior
- **checkpoint:** one green repository-policy correction commit with task, requirement, proof IDs, correction provenance, and the required `Plan-*` checkpoint trailers
- **boundary:** cross-task
- **depends_on:** [T4]
- **dependency_reason:** consumes the committed T4 build script and native fixture binary after Git made their intentional `100755` modes visible; the external runner-label T3 candidate remains paused until this classification checkpoint lands
- **requirements:** [REQ-010, REQ-013]
- **proof:** [PV-T6-001]
- **source_refs:** [request, repo:tests/test_repository_hygiene.py::test_git_mode_policy__classifies_and_normalizes_the_complete_anchor_inventory, repo:scripts/build-command-provider-fixture.sh, repo:tests/fixtures/command-provider/standards/command-provider-fixture/versions/1.0/bin/command-provider-fixture, repo:docs/plans/2026-08-10-runner-label-advisory-plan.md#t3-extend-the-composed-project-specification-19-candidate]
- **consumes:** [go-command-provider-fixture-v1, command-provider-core-checkpoint-v1, complete repository Git-mode inventory, runner-label T3 fast-gate receipt]
- **produces:** [command-provider-fixture-git-mode-classification-v1]
- **preserves:** [T4 checkpoint and acceptance, build script mode and bytes, fixture binary mode and bytes, binary SHA-256 `83292a67bab10fb619d5d3be17210b8e056734efb15140562ee5b8741b63496a`, payload aggregate `66720e040b0c6eb6be3cf7397ee94955a996e8e9e28a17c81ebebd9c92df7986`, complete executable equality, anchor/predecessor policy, every unrelated classification and assertion, production packages, release/GitHub state, external runner-label T3's exact nine-path candidate]
- **invariants:** [only the two named paths join the post-anchor inventory; the build script is classified as repository tooling; the committed binary is classified as a native test fixture rather than general tooling; both Git modes remain `100755`; complete-inventory equality remains closed; no artifact byte, digest, build rule, fixture payload, anchor/predecessor rule, or other classification changes]
- **executor_discretion:** [private name for the native-fixture set, concise durable comment placement that records semantic role and the staging visibility trap]
- **files:** [`tests/test_repository_hygiene.py` (modify only the two exact post-anchor classifications, their semantic naming/comments, and focused proof; owner T6)]
- **parallel_safe:** no
- **conflicts_with:** []
- **supersedes:** []
- **superseded_by:** []
- **corrects:** [T4]
- **discovered_from:** []
- **evidence:** [ephemeral]
- **recovery:** preserve the integrated T4 artifacts and external runner-label T3's exact nine-path candidate. If the current committed-index RED names any path besides the two accepted T4 executables, or either mode/digest differs, block before editing and reconcile the evidence. If the focused correction changes an artifact, adds a third classification, weakens complete equality, or changes anchor/predecessor policy, revert only T6 and block. After T6 checkpoints, rebase the preserved external runner-label T3 candidate onto the correction and rerun its sole direct-local fast gate; do not amend that candidate here.
- **acceptance:** PV-T6-001 proves the current committed index first fails for exactly the two unexpected `100755` T4 paths, then classifies only the build script as post-anchor tooling and only the committed binary as a post-anchor native fixture; the focused node and full hygiene file pass, both accepted digests remain exact, `git diff --check` and the direct-local fast gate pass, and no mode, artifact byte, production/release/GitHub path, or unrelated policy classification changes.
- **sub-tasks:**
  - **T6.1 RED** — on the current committed index, run the focused complete-inventory node and confirm it fails for exactly `scripts/build-command-provider-fixture.sh` and the committed `command-provider-fixture` binary as unexpected `100755` paths; reject a missing mode, digest drift, third path, or unrelated failure as wrong reason.
  - **T6.2 Verify RED** — inspect `git ls-files -s`, the accepted digest oracles, and the complete-inventory assertion to prove the failure comes only from post-checkpoint index visibility, not a mode, byte, digest, anchor, or normalization defect.
  - **T6.3 GREEN** — add exactly the build script to the existing post-anchor tooling classification and the committed binary to a narrowly named post-anchor native-test-fixture classification. Add only durable semantic naming/comments needed to distinguish their roles; retain the complete equality and all anchor/predecessor normalization logic.
  - **T6.4 Verify GREEN** — run the focused complete-inventory node and the full `tests/test_repository_hygiene.py`; inspect `git ls-files -s` for exact `100755` modes; recompute the binary and payload aggregate SHA-256 values; verify the diff contains only the claimed test file and two classifications; run `git diff --check`.
  - **T6.5 REFACTOR** — keep the existing tooling set for the build script and use one narrowly named native-fixture set for the binary; remove any redundant allowance or comment while retaining durable policy semantics and the staging-hazard explanation.
  - **T6.6 Verify Task** — rerun the focused/full hygiene proofs after any refactor, then run the sole direct-local fast `scripts/verify.sh` gate because it depends on the Git index/history and this is an intermediate test-policy correction; do not run `--full`. Validate the plan/checkpoint and create the T6 correction commit. Then hand the checkpoint to the external runner-label T3 owner for rebase and its own gate rerun.

## 10. Integration, Migration, and Recovery

### 10.1 Integration Sequence

1. T1 records the decision and produces EG-ADR. T2–T5 remain blocked until it validates; #162 consumes the same checkpoint before its final sweep.
2. T2 moves the existing bounded process contract into the control plane, restores safe built-in exception-cause parity from sanitized frames, corrects integer-FD audit classification, and proves Python/MCP parity before any command schema or executable is introduced.
3. T3 adds command declaration/runtime behavior to T2's single runner, closes MCP parent routing by sending commands directly through that boundary while leaving Python on its existing path, preserves caller input and composite seam authority, and proves all failure boundaries with synthetic executable controls.
4. T4 adds the synthetic Go payload fixture and reproducible binary, then blocks when its real composite route proves T3's plan-bound helper sends a required command render through the Python-only runner. Preserve its worktree and receipt.
5. T5 corrects the completed T3 route from the integrated checkpoint: the helper accepts the existing provider-runner seam, Python-worker callers retain the in-child default, and MCP parent command dispatch supplies the kind-aware direct runner. Prove the required planning render and later target verify are separate, exactly-once, non-nested invocations.
6. Resume T4's preserved worktree, rerun the failed real route first, then complete its final full gate. This is not a production successor cut.
7. The external runner-label T3 fast gate exposes exactly two newly indexed T4 executables that T4's untracked-worker gate could not see. T6 classifies those artifacts by semantic role without changing them, runs the focused/full hygiene proofs plus one direct-local fast gate, and checkpoints the repository-policy correction.
8. Rebase and resume the preserved external runner-label T3 candidate only after T6. Stop at the corrected `command-provider-core-checkpoint-v1`; a separately authored Agent Handoff migration plan may consume T4 only after verifying EG-156, and this plan cannot begin that cut.

### 10.2 Runtime Transition

- Required: yes, for Python provider execution ownership and the new command ABI; no persistent consumer data migration.
- Compatibility period: existing Python and documentation-only declarations remain valid; no production command declaration exists until a later package plan.
- Idempotency: repeated dispatch against identical payload/input bytes yields the same typed result and deterministic diagnostic ordering; repeated reconcile/validate/drift-check fixture runs leave no temporary executable or repository change beyond the owning operation's declared result.
- Point of no return: none. All changes are local source/contracts/fixtures; release and payload activation are external.
- Rollback / forward repair: revert the latest unreleased task checkpoint before downstream consumption or append a correction task after completion. Preserve T1's ADR history and published payload bytes.
- Recovery proof: T5 starts from the integrated T3 checkpoint and leaves the blocked T4 worktree untouched. Its focused proof counts the planning render and target verify separately and rejects target execution during construction; resumed T4 repeats the exact failed route before broader end-to-end flows and verifies a clean temporary namespace and unchanged production catalogs/payloads. T6 starts only after T4 is committed, reproduces the exact two-path Git-index RED, changes only the policy test, and retains both accepted artifact digests before the external runner-label candidate rebases.

### 10.3 External Coordination

- **#162:** its final sweep must observe T1's commit. If it has already started, serialize ownership and rerun its tracked-reference inventory after integrating T1; do not let both tasks edit `docs/adr/README.md` concurrently.
- **#156:** T1–T5 create no production successor and therefore do not consume this gate. The later Agent Handoff successor plan must verify the exact active-plan T2 checkpoint and its Plan trailers before authoring the new package version.
- **Agent Handoff migration:** the later plan owns its successor version, Go source parity, all package provider operations, documentation, catalog role, immutable predecessor proof, consumer behavior, and release classification. It may not treat this core fixture as package acceptance.
- **Runner-label T3:** preserve its exact nine staged paths while T6 corrects the integrated mode inventory. After T6 checkpoints, rebase that candidate and rerun its sole direct-local fast gate; T6 does not edit or commit any runner-label path.

### 10.4 Late Failure and Correction

A wrong-reason RED, ADR conflict, Python parity change, unsafe/dynamic exception reconstruction, path-like audit omission, duplicate runner, cap/cleanup failure, unsafe diagnostic, platform/materialization defect, planning-time target execution, skipped/fabricated render output, package-specific runner selection, missing external checkpoint, reproducibility mismatch, end-to-end route bypass, artifact mode/digest change, third executable classification, or weakened complete-inventory equality blocks the current task. T5 is the permanent correction to completed T3 and must checkpoint before blocked T4 resumes; T6 is the permanent classification correction to completed T4 and must checkpoint before external runner-label T3 resumes. If governing intent is incomplete, return the smallest amendment request to #142/the ADR owner. Otherwise append another permanent correction task with `corrects:` and `discovered_from:`, preserve completed definitions/checkpoints, rerun the owning proof, and then rerun every dependent proof.

## 11. Risks, Assumptions, and Open Questions

### 11.1 Risks

| ID | Risk | Likelihood | Impact | Treatment | Owner / Task |
| --- | --- | --- | --- | --- | --- |
| R-001 | Extraction leaves MCP double-spawning or preserves a second timeout/cap implementation. | medium | high | Import/source ownership and child-count tests reject duplicate/nested runners before T2 completes. | T2 |
| R-002 | Bounding all Python providers changes a formerly successful large-input or long-running CLI operation. | medium | high | Characterize real provider corpus, use deadline-bound nonblocking stdin, retain schema/resource behavior, and exercise real packaged providers; a limit conflict returns to ADR authority. | T1–T2 |
| R-003 | A temporary executable is verified before a later path/byte substitution. | low | high | Private directory, no installed-path execution, exact-path spawn, post-write digest/mode inspection, and race-focused negative tests. | T3 |
| R-004 | Result or diagnostic text exposes installed paths, temp paths, or inherited secrets. | medium | high | Closed command environment, fixed errors, double filtering at parent boundary, canary tests, and no raw exception/provider text publication. | T2–T3 |
| R-005 | A fixture-only happy path passes while reconcile, validate, or drift-check still filters command providers. | medium | high | T4 uses real route owners and cardinality/assertion controls; helper-only calls are insufficient. | T3–T4 |
| R-006 | The Go fixture binary diverges from source or a future provider omits the build gate. | medium | high | Thin build script plus shared rebuild comparison; list in both Makefile targets; tests pin platform/mode declaration and gate membership. | T4 |
| R-007 | #162 lands before the new ADR or #156 changes checkpoint identity before the deferred migration. | medium | medium | Deliver EG-ADR before #162; make the later successor plan re-resolve EG-156 rather than infer an equivalent checkpoint. | T1, deferred migration |
| R-008 | Core implementation silently expands into the 1,499-line Agent Handoff migration. | medium | high | File claims exclude Agent Handoff payload bytes; final diff asserts no production payload/catalog change; later plan is mandatory. | T4 |
| R-009 | Child-controlled exception kind triggers arbitrary import, lookup, or custom constructor execution in the parent. | medium | high | Literal safe built-in mapping with ValueError regression; unknown/custom kinds take one fixed generic path; import/constructor canaries must remain untouched. | T2 |
| R-010 | Ignoring integer inherited descriptors weakens the filesystem audit to miss a real path access. | medium | high | Narrow the exception to integer builtins.open/io.open targets and retain explicit string, bytes, and PathLike positive controls. | T2 |
| R-011 | MCP command dispatch either creates a nested child or sends the composite seam directive as empty provider input. | high | high | Branch only on provider kind in the parent; send command directly to the shared control-plane invocation; build composite input through `authoritative_provider_input`; preserve caller and generic-empty cases with independent mutation controls. | T3 |
| R-012 | Plan-bound command input construction executes a required command contribution through the Python-only runner, skips its content, or executes target VERIFY while deriving its own input. | high | high | T5 injects only the existing kind-aware provider runner at the planner seam; tests require real render output in the completed plan, executor-equivalent snapshot projection, absence of target VERIFY during construction, one later target execution, and one non-nested child per invocation. | T5, T4 |
| R-013 | Newly committed executable fixtures remain invisible to the repository mode inventory because their creating worktree gate ran before Git tracked them. | medium | high | T6 reproduces the committed-index census, adds exactly two semantic classifications while retaining closed equality, and hands a green fast-gate checkpoint to the waiting external runner-label task. | T6 |

### 11.2 Assumptions

| ID | Assumption | Impact if False |
| --- | --- | --- |
| A-001 | ADR number 0030 remains available after the integrated ADR 0029 checkpoint. | If another accepted record consumes 0030 before T1 writes, pause and revise the master to the next free exact path; do not overwrite it or silently exceed the file claim. |
| A-002 | The `linux/amd64` identifier and `0755` string can be added to schema version `1.0` as optional fields globally but required/prohibited by provider kind. | If schema-version policy requires a bump, pause T3 and obtain an owner amendment; do not silently create schema `1.1`. |
| A-003 | The shared runner can preserve all existing Python providers and meet the approved 7.5-second/30-second budgets by executing a parent-prepared integrity-checked capsule without retaining MCP's worker-side whole-manifest rediscovery. | If capsule parity or either approved budget fails, pause T2 and correct the implementation rather than retain a duplicate, normalize qualified synthetic payloads, batch across invocation boundaries, or raise bounds again. |
| A-004 | #156 revision 1 T2 remains the checkpoint that authorizes successor work. | If revised/superseded, use the bridge-validated active replacement named by #156's owner. |

### 11.3 Open Questions

None.

## 12. Final Verification

- Bridge 3.5.0 validates this active master and every T1–T6 checkpoint/trailer identity; EG-ADR was delivered before #162. EG-156 remains an explicit deferred-successor prerequisite, not a core-plan completion condition.
- Every Must requirement maps to completed task-owned Appendix B proof; no final broad gate excuses a failed task-level runner, command, materialization, platform, or fixture proof.
- The accepted ADR and index are frontmatter/reference/Prettier/markdownlint clean, and #162 has the T1 prerequisite before its final sweep.
- Source/import inspection finds one control-plane-owned selector/process/cap/teardown implementation, no nested MCP provider worker, a parent-side command branch keyed only by provider kind, and no package identity branch in shared routing.
- Existing Python providers preserve every effect, schema, resource, typed result, output notice, live-path integrity check, CLI route, MCP composition, and content-safe failure behavior under the bounded process.
- Focused parity proves an allowlisted provider `ValueError` is preserved as `ControlPlaneError.__cause__` with sanitized detail; unknown/custom kinds stay fixed generic, cannot import or execute code, and reveal no path/secret.
- Consumer-boundary security proof distinguishes inherited integer `open`/`io.open` handles from filesystem paths while continuing to record and enforce every string, bytes, and PathLike target.
- Serial performance proof retains the 30-second deterministic-order budget and passes the owner-approved 7.5-second real-catalog budget without weakening one-child isolation.
- Command declaration/generated-schema tests prove the `payload:{resource}`, `linux/amd64`, and `0755` contract, invalid kind-specific combinations, and unchanged Python/documentation-only/V1 behavior.
- Command process tests prove direct and MCP input/resource byte equivalence, single-provider caller-input preservation, composite family/plan-bound/generic input authority, exactly one child per command provider invocation, output schema/effect validation, result-fd isolation, exact/N+1 caps, timeout/cancellation/crash/nonzero/descendant cleanup, unsupported-platform pre-spawn refusal, closed environment, installed-path nonexecution, on-disk digest/mode verification, and cleanup after every path.
- T5's correction proof uses the real plan-bound shape: command `render-fixture` executes exactly once to determine the completed plan, executor-authoritative projection supplies `verify-fixture` input, the target verify is absent during construction and executes exactly once afterward, and neither invocation is wrapped in a Python worker. Python planning remains in its existing child and no package identity selects a runner.
- T6's committed-index proof classifies exactly the T4 build script as post-anchor tooling and the committed command fixture as a post-anchor native test fixture; both remain `100755`, the binary and payload aggregate SHA-256 values remain exact, and the closed complete-inventory equality plus all unrelated classifications remain unchanged.
- The committed Go fixture rebuilds byte-identically and `make go-check` passes remotely; extracted-wheel and real reconcile/validate/drift-check tests prove the integrated route without a Python shim.
- `scripts/bootstrap-worktree.sh` is fresh after the last `src/**`/fixture payload change. The five package checks, schema generation/projection, scoped Markdown gates, Ruff, remote BasedPyright, remote Go gate, `git diff --check`, T4's direct-local `scripts/verify.sh --full`, and T6's direct-local fast `scripts/verify.sh` pass.
- Tracked diff inspection shows no Agent Handoff provider migration, production payload version, catalog selection, release metadata, handoff state, GitHub state, or unrelated file change.
- No blocker, unapproved deviation, incomplete correction, publication action, or orphan external handoff remains.

## 13. Close-out

- **Completed:** record T1–T6 commits, including T5/T6 permanent correction provenance, corrected command-provider-core-checkpoint-v1, EG-ADR delivery to #162, and the unconsumed EG-156 requirement in the deferred-migration handoff.
- **Decisions / deviations harvested:** retain the accepted ADR and record only approved changes to ABI, limits, platform normalization, or runner ownership; do not rewrite completed task definitions.
- **Risks closed / accepted:** close R-001 through R-013 from proof or file a bounded follow-up owned outside core acceptance.
- **Deferred/discovered work filed:** the Agent Handoff provider migration remains a separately planned successor cut; any additional platform target, sandboxing, or environment capability requires explicit authority.
- **Source/ADR/handoff reconciliation:** T1 owns ADR/index truth; #162 owns its later corpus sweep. Agent Handoff updates occur only if core execution creates a durable repository fact not already owned by the ADR/plan and under a separately authorized closeout.
- **Scratch teardown:** only the orchestrator may remove execution state after all checkpoints and concise evidence pointers are committed and the later migration handoff can cite T4 plus EG-156.

## Appendix A. Interface and State Contracts

| Contract | Owner / Producer Task | Consumer(s) | Current | Planned / States | Errors / Limits | Compatibility / Invariant | Source |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `bounded-provider-runner-v1` | T2 | `invoke_provider`, CLI/planner/executor, MCP services, T3 command adapter | MCP-only Python-worker subprocess; direct dispatch in-process. | One control-plane runner accepts fixed argv/environment/request bytes and returns bounded result plus stdout/stderr capture; sanitized Python error frames carry kind/detail for closed parent reconstruction; teardown is unconditional. | 30 seconds; stdout/stderr 8,192 bytes each; result 262,144 bytes; diagnostics 16,384 chars; unknown exception kinds are fixed generic. | Third descriptor only; literal safe built-in cause map; no dynamic imports/custom constructors; `SIGTERM` then `SIGKILL`; group reaped; explicit truncation; no duplicate/nested runner. | ADR 0025; issue #142; T1 ADR; revision-4 gate receipt |
| `python-provider-execution-capsule-v1` | T2 | all existing V2 Python providers | Python bytes compile/execute in caller from an already-qualified `InstalledPayload`. | Parent verifies/serializes exact bytes and contract; child executes without whole-manifest rediscovery, writes one JSON object to the result fd, and reports sanitized exception kind/detail; parent restores allowlisted cause parity. | Same runner limits; pre-spawn mismatch fails in parent; `ValueError` cause retains sanitized detail; unknown kind uses a fixed generic cause; non-JSON/non-object fails bounded. | Qualified synthetic/monkeypatched seams, schemas, effects, notices, resource closure, public content-safe error, and V1 behavior remain exact. | current `invoke_provider`; T1 ADR; revision-4 gate correction |
| `consumer-open-audit-v1` | T2 | MCP consumer-boundary security regressions | OpenAudit records integer transport FDs as apparent paths. | Ignore integer builtins.open/io.open targets; record all string, bytes, and PathLike targets. | Any path-like access remains visible; only inherited integer transport handles are excluded. | Test-oracle correction only; production containment and result-fd isolation unchanged. | `OpenAudit`; result-fd contract; revision-4 gate receipt |
| `v2-command-provider-declaration-v1` | T3 | manifest loader/schema, planner, runtime, future payload authors | Enum exists; declaration/runtime reject it. | `kind = "command"`; `entrypoint = "payload:{resource-id}"`; `platforms = ["linux/amd64"]`; `mode = "0755"`; required/prohibited fields enforced by kind. | Unsupported/missing/duplicate platform, symbol fragment, mode other than `0755`, or undeclared entrypoint resource is invalid. | Schema `1.0` remains valid under A-002; Python uses `#symbol`; docs-only has no execution fields; V1 runner stays closed. | issue #142; T1 ADR |
| `command-provider-wire-v1` | T3 | Go fixture and future command providers | No command transport. | Stdin is canonical UTF-8 JSON object with schema version, validated provider input, and sorted `{resource_id: padded-base64}`; `argv[1]` is decimal result fd; result is one UTF-8 JSON object. | Malformed envelope/Base64/result, unknown/duplicate field, over-limit result, nonzero exit, and schema violation fail deterministically. | Input's resource digest map still names the same bytes; only declared resources are sent; stdout/stderr never carry result. | issue #142; T1 ADR |
| `command-provider-materialization-v1` | T3 | shared runner command adapter | Installed payload resource is data only. | Private per-call dir → write verified bytes → `chmod 0755` → rehash → exact-path spawn → process-group teardown → remove. | Unsupported platform or digest/mode failure occurs before spawn; cleanup covers every exit path. | Never execute installed-wheel path; no shell/PATH; closed environment; temp path not published. | issue #142 implementation finding; T1 ADR |
| `mcp-command-dispatch-v1` | T3, corrected by T5 | MCP single-provider and composite routes | T3's parent kind branch preserves caller/seam routing, but the plan-bound helper hard-codes Python in-child planning and rejects a required command render. | Python retains the existing route and helper default. Command invokes `control_plane.providers.invoke_provider` directly; caller input passes unchanged; composite input is built through `authoritative_provider_input` with the same kind-aware runner. Required planning providers execute through their normal one-child route, completed-plan snapshots come only from `provider_dispatch_input`, and target VERIFY executes once afterward. | Authority/selection failures preserve bounded service-error mapping and composite per-result isolation; command process/transport failures retain the shared runner taxonomy and caps. | One child per provider invocation; no nested Python worker, package-ID routing, target invocation during input construction, fabricated render result, caller-input rebuilding, or empty-directive substitution. | ADR 0030; `_dispatch`; `authoritative_provider_input`; `_contribution_content`; `_verify`; `_verification_input` |
| `go-command-provider-fixture-v1` | T4 | end-to-end and reproducibility tests | Absent. | Minimal Go implementation of wire v1, dedicated synthetic payload, committed `linux/amd64` bytes, thin build script, both Makefile gate entries. | Malformed input/resource/result controls return errors; no panic/log-and-return; build mismatch fails gate. | Test-only identity; no production package/catalog role; source and bytes are one checkpoint. | issue #142; Go ADR 0027; shared build library |
| `command-provider-core-checkpoint-v1` | T4 | later Agent Handoff migration plan | No platform checkpoint. | Plan-validated T4 commit with all core requirements/proofs; deferred EG-156 remains separately required. | Missing/mismatched checkpoint trailers or failed proof blocks successor work. | Authorizes planning input only, not a payload cut, activation, release, or issue closure. | this plan; #156 coordination |
| `command-provider-fixture-git-mode-classification-v1` | T6 | repository hygiene gate; external runner-label T3 | The two committed T4 artifacts are visible as unexpected `100755` paths because their creating worktree gate ran while they were untracked. | Build script joins post-anchor tooling; committed binary joins a separately named post-anchor native-test-fixture set; complete indexed equality remains closed. | Any third path, wrong role, changed mode/digest, broad allowlist, or weakened equality fails. | Classification only: T4 artifact bytes/modes/digests, all other policy categories, production packages, and external candidate remain exact. | repository-hygiene policy; runner-label T3 receipt; T4 checkpoint |

## Appendix B. Requirement-to-Proof Traceability

| Proof ID | Requirement(s) | Task | Method | Oracle | Command / Procedure | Expected Result | Negative Control | Environment | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PV-T1-001 | REQ-001, REQ-002 | T1 | ADR/frontmatter/reference inspection | issue #142 owner decision, ADR 0025, ADR 0027, ADR 1.5, #162 sequencing | Validate new ADR/index with local `project-standards validate`, tracked Prettier/markdownlint, `git grep`, bridge validation, and inspect the T1 checkpoint handoff. | One accepted bounded record covers D-002–D-008, rejected alternatives and portability consequence; metadata/links/index pass; #162 receives the commit before its sweep. | Omit installed-path prohibition, environment/platform rule, one ADR 0025 property, or #162 handoff; duplicate/skip ADR number. | local Git-aware docs checkout | ephemeral |
| PV-T2-001 | REQ-003, REQ-004, REQ-008, REQ-012 | T2 | characterization, refactor parity, subprocess/error-frame regression, and audit-oracle correction | existing direct Python outputs/MCP DTOs/exception causes; sanitized child kind/detail; ADR 0025 clauses; OpenAudit path corpus; independent child/fd/process inspection; revision-4 receipt; repeated timing evidence | Run focused ValueError cause/detail, redaction/unknown-kind, and FD-vs-path controls; focused control-plane/MCP suites; import/source ownership and no-dynamic-resolution checks; real provider compositions; both serial budgets; package/type/lint; then the direct-local fast gate after bootstrap. | Direct and MCP Python calls share one child, preserve exact effects/results and allowlisted safe cause parity, keep unknown kinds bounded generic, audit every path-like target but not integer transport FDs, satisfy all existing process/safety assertions, and remain below 7.5/30 seconds. | Drop ValueError cause/detail; leak path/secret; resolve or execute a child-named custom exception; record integer FD as path; ignore string/bytes/PathLike target; retain MCP runner; run direct in parent; spawn twice; write result to stdout; N+1 overflow; surviving child; raise either budget. | local and rexec synchronized Linux amd64; Git-dependent gate local; serial `-n 0` budget execution | ephemeral |
| PV-T3-001 | REQ-005, REQ-006, REQ-007, REQ-008, REQ-010 | T3 | schema, contract, process-boundary, MCP routing/input-authority, and filesystem regression | ADR 0030 exactly-once delegation; `authoritative_provider_input`; generated Pydantic schema; payload digest; declared input/output schemas; independent child-count/`stat`/hash/process/env inspection | Run payload declaration/schema tests; focused command direct and MCP single/composite matrices that distinguish caller, family, plan-bound, and generic input authority; one-child/source-routing controls; schema generation; package checks; type/lint; fast repository gate. | Valid command declaration dispatches on Linux amd64 with exact resources/result; MCP command uses one child, preserves caller/composite input authority and per-result failures, and never enters the Python worker; invalid declarations/platform/digest/mode/env/result/schema/cap/process cases fail before unsafe use and leave no temp/process/descriptor; Python/V1 remain green. | Force command through `python_worker_argv`; forward the empty composite directive instead of authority-built input; rebuild caller input; route by package ID; `payload:x#symbol`; missing platform/mode; 0644/modified temp; execute installed path; fake host; inherited secret; Base64 corruption; stdout-only result; N+1 result; nonzero/crash/fork. | local and rexec synchronized Linux amd64; host platform injected for refusal | ephemeral |
| PV-T4-001 | REQ-006, REQ-007, REQ-009, REQ-010, REQ-011 | T4 | Go build/repro, extracted-wheel, control-plane/MCP end-to-end | pinned shared build contract; synthetic payload manifest/digests/schemas; real reconcile/validate/drift-check results; independent filesystem/process inspection | Run Go unit/race/static/audit and rebuild compare; run command payload e2e across real routes; five package checks; full Python/Go/Markdown gates; full repository gate; inspect final diff and prove no production successor path changed. | Source and binary match; every real route returns expected typed output/findings; materialized file not installed resource executes at 0755 then disappears; wrong platform/hollow transports fail; no production payload/catalog/release/GitHub change. | Delete/alter build gate entry; mutate binary; force installed file 0644; bypass route with helper; remove one operation/provider; wrong platform; result on stdout; leave temp; alter production Agent Handoff/catalog path. | bootstrapped local Git-aware checkout; compatible CPU-heavy Python/Go lanes via rexec | ephemeral |
| PV-T5-001 | REQ-006, REQ-008 | T5 | correct-reason plan-bound runner-selection and call-order regression | ADR 0030 one child per provider invocation; T4 real-route receipt; `_contribution_content` required render result; `_verify` plus `_verification_input` snapshot authority; existing `invoke_provider` kind routing | Reproduce plan-bound command VERIFY with a real command render contribution; record provider IDs/operations, child boundaries, render-derived plan facts, and projected verify input; run Python/family/generic controls; then focused command/provider-worker/MCP routing regressions, package/static checks, and the direct-local fast gate. | `render-fixture/render` executes once through one direct bounded child and affects the completed plan; target `verify-fixture/verify` is absent during construction and executes once afterward through one direct bounded child with executor-equivalent snapshots; Python stays in-child; no package-ID branch, new runner, cap, or budget. | Keep Python-only planner runner; stub or skip render output; invoke target verify during construction; wrap render or verify in a Python worker; rebuild snapshot shape outside `provider_dispatch_input`; branch on fixture/package ID; alter family-first or generic-None behavior. | bootstrapped synchronized Linux amd64 for focused/static work via rexec; Git/index and fast gate direct-local | ephemeral |
| PV-T6-001 | REQ-010, REQ-013 | T6 | correct-reason Git-index inventory regression and exact artifact-preservation inspection | current `git ls-files -s`; repository-hygiene complete-inventory equality and adjacent staging comment; T4 checkpoint modes; accepted binary/payload SHA-256 values; runner-label T3 receipt | Direct-local: run the focused complete-inventory node on the committed index for the exact two-path RED; add exactly two role classifications; rerun that node and all of `tests/test_repository_hygiene.py`; inspect indexed modes; recompute binary and fixture payload aggregate SHA-256; inspect the one-file diff; run `git diff --check` and the fast `scripts/verify.sh`. | RED names only the two T4 paths; GREEN classifies the script as post-anchor tooling and binary as native test fixture; both test scopes and fast gate pass; both modes remain `100755`; accepted digests and every unrelated classification remain exact. | Chmod or rewrite either artifact; omit one path; classify a third path; label binary as general tooling; weaken complete equality; change anchor/predecessor or another category; mutate production/release/GitHub state. | bootstrapped local Git-aware checkout; all T6 checks direct-local because inventory and repository gate depend on Git/index/history | ephemeral |

## Appendix C. Durable Evidence

No separate evidence record is required. The accepted ADR, committed Python/Go tests, fixture source and binary, reproducible build script, and identity-bearing task checkpoints make every acceptance result inexpensive and reproducible.

## Appendix D. Deferred Work

| Item | Reason Deferred | Follow-up / Reopen Trigger |
| --- | --- | --- |
| Agent Handoff provider migration | Core issue acceptance does not require moving package logic, and the successor cut has distinct parity, package-version, catalog, documentation, and release obligations. | Author a separate format-3 plan after T4 and EG-156 validate; choose the successor version from live family state. |
| Additional command-provider platforms | Owner knowingly accepted Linux x86-64 for the internal consumer population; one binary/resource cannot imply unproved portability. | A real consumer/platform requirement supplies build, declaration, fixture, and release authority. |
| Stronger sandboxing or privilege isolation | The accepted boundary is bounded fault isolation for trusted catalog payloads, not an untrusted-code sandbox. | A separate threat model/owner decision changes the payload trust assumption. |
| Production payload release and issue closure | This plan stops at local core checkpoints and grants no publication or GitHub work-state mutation. | Parent release workflow authorizes package cut/activation, release verification, publication, and synchronized issue closure. |
