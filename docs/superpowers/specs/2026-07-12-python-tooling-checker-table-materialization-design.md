# Python Tooling Checker Table Materialization Design

**Date:** 2026-07-12 **Status:** owner-approved; contract audit reconciled through round 7 plus complete-release-evidence, release-bootstrap, and executable-proof reconciliation; implementation reconciliation records Pyright 1.1.411 packaging drift, the corrected oracle network boundary, complete root-materialization predecessor reconstruction, release-signature currency, preserved-container safety, the two guarded predecessor alignments, the pre/post-atomic root-gate boundary, bootstrap-safe Frontmatter endpoint preservation, and exact atomic release-content replay **Author:** Claude (Fable 5) with Chris Purcell / L3DigitalNet

## Problem and goal

Python Tooling 1.1 unconditionally declares both `table:/tool/basedpyright` and `table:/tool/pyright` contributions, and the provider renders the non-selected table with `typeCheckingMode = "off"`. BasedPyright refuses to parse a pyproject containing both tables. Verified against this branch's locked toolchain:

- basedpyright 1.39.6 exits 3 on the generated dual-table pyproject with `Pyproject file cannot have both 'pyright' and 'basedpyright' sections. pick one`, before any analysis runs.
- Removing `[tool.pyright]` alone returns the same project to exit 0.
- The rendered dev dependency is an unpinned `basedpyright`, so fresh consumers always install a current release with this behavior; no lock shields them.

Consequences: every fresh default consumer receives an invalid checker configuration; the generated `scripts/check.py` cannot pass its type-checking phase; the atomic v5 root migration would materialize the same fatal pair; parallel-coverage plan Task 9 (release replay) would fail. Selecting `pyright` repairs the CI gate — pyright ignores the unknown `[tool.basedpyright]` table — but not the editor surface, because the unconditionally recommended basedpyright extension parses the same pyproject.

The cause predates the parallel-coverage work. `MaterializationPredicate.option` accepts only a top-level option name and evaluates `config.get(option)`, while checker selection lives at `/type_checker/name`, so the payload cannot express conditional materialization for the two tables.

The goal is that exactly one checker table materializes for every schema-valid configuration, expressed declaratively in the payload, with transitions between checkers proven safe across plan, lock, migration, disable, and re-enable. No option is added or structurally changed; implementation reconciliation permits only the narrow `additional_dev_dependencies` item-pattern correction required to accept ordinary version comparators such as the already-approved `pytest-xdist>=3.8`. Completed ownership-relinquishment and coverage behavior remain unchanged.

## Approved approach

Extend conditional materialization to accept a canonical nested option pointer in the existing predicate field.

### Predicate grammar

- `MaterializationPredicate.option` accepts either spelling: an existing top-level `OptionName`, or an absolute option pointer.
- An option pointer starts with `/`, contains two or more segments, and every segment matches the OptionName grammar:

  ```text
  ^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$
  ```

- A single-segment pointer (`/foo`) is noncanonical and rejected; top-level options keep their bare spelling, so every predicate has exactly one valid spelling.
- The segment grammar excludes RFC 6901 escapes (`~0`, `~1`), array indices, percent forms, and non-ASCII spellings entirely; option schemas are closed objects keyed by option names, so nothing expressible is lost.
- Operator semantics are unchanged: exactly one of `equals`/`contains`, type-exact comparison at the leaf.

### Matching semantics

- Pointer evaluation walks nested mappings segment by segment.
- An absent key or a non-mapping intermediate value yields a non-match for that predicate.
- Runtime non-match is defense in depth only. Along statically validated pointer paths, the option-schema default contract (`_validate_default_contract` plus recursive `_apply_defaults`) applies at every traversed level — the intermediate rule below restricts traversal to exactly the shapes that contract covers — so a missing target is unreachable for schema-valid consumer input.

### Static predicate validation

- Pointer grammar is pure syntax and validates during manifest model validation, which has no option-schema access.
- `load_option_schema(payload_dir, manifest)` is the authoritative cross-contract validator: every `when_any` predicate — both spellings — must name a declared property path in the closed option schema, and every non-leaf segment must traverse a direct `properties` child whose declared `type` is exactly `object`. Nullable, composed (`anyOf`/`oneOf`/`allOf`), `$ref`, and missing-type intermediates are rejected until explicit resolution semantics are designed.
- Violations fail package loading as `PC-OPTIONS` before planning or writes. Without this check, an authoring typo such as `/type_checker/nmae` would silently drop both checker tables, and the generated gate would run the checker with no configuration at all — fail-open.
- Static validation closes undeclared-path and non-total-traversal errors only. A valid path with an impossible literal (for example `equals = "basedpyrite"`) or a type-incompatible operator is not statically rejected; the mandated exactly-one-table planner proofs catch that class deterministically for Python Tooling.
- Existing shipped predicates (Agent Handoff's `startup` and `harnesses`) already conform and must continue to load unchanged.

### Python Tooling payload and provider

- `basedpyright-config` gains `when_any = [{ option = "/type_checker/name", equals = "basedpyright" }]`; `pyright-config` gains the `pyright` counterpart.
- `_checker_table` raises when asked to render a table that does not match the selected checker, so payload/engine skew fails loudly instead of emitting the fatal pair.
- The two VS Code mode keys (`basedpyright.analysis.typeCheckingMode`, `python.analysis.typeCheckingMode`) remain unconditional. In `settings.json` the inactive-checker `"off"` value is correct and desirable — it silences the non-selected language server — and namespaced settings keys carry no dual-authority conflict, unlike the pyproject tables.

### Sequencing

- Separate implementation plan; same branch and release train as the parallel-coverage design.
- Must land before parallel-coverage plan Task 9 proves the migrated script/config/lock through installed rendering and the scratch complete-gate oracles, and before Task 11 executes that generated gate against the atomically migrated root. Coverage Tasks 7–8 do not execute the generated gate and may proceed independently.
- The parallel-coverage plan's Tasks 9 and 11 require matching amendments for the complete Release integration contract, plus a fresh audit of those amended tasks before Task 9 resumes; the prior plan audit covers only unchanged Tasks 1–8 and 10.

## Alternatives rejected

1. **Always render only `[tool.pyright]`.** Runtime-compatible with BasedPyright, but migration-unsafe: legacy consumers hold unclaimed `[tool.basedpyright]` bytes, so migration would add `[tool.pyright]` and recreate the fatal pair. It also abandons the basedpyright-specific configuration surface.
2. **Point BasedPyright at a separate `--project` configuration.** Duplicates configuration authority and breaks editor and language-server auto-discovery.
3. **Keep the narrowed coverage oracle and ship.** Hides a real shipped-package failure.
4. **Flatten `type_checker` into top-level scalar options.** Works with the existing flat predicate and avoids an engine change, but breaks the consumer-facing option schema — a versioned contract — and ripples through legacy migration mapping. The pointer extension is additive at every layer.
5. **Render empty content for the inactive table.** Requires new "empty content means absent unit" semantics across all adapters and leaves lock entries ambiguous.
6. **A distinct `option_pointer` predicate field.** Creates two valid spellings of the same predicate (`option = "startup"` versus `option_pointer = "/startup"`), violating the canonical-form discipline the contract enforces elsewhere; the overloaded field keeps the two syntaxes disjoint and unambiguous, and older engines reject pointer-bearing payloads loudly through pattern validation either way.

## Audit round 1 disposition

- **CTM-001:** Accepted. Grammar validation is syntax-only and stays in manifest model validation; `load_option_schema(payload_dir, manifest)` is the authoritative cross-contract validator, and an undeclared or structurally invalid predicate path fails package loading as `PC-OPTIONS` before planning or writes.
- **CTM-002:** Accepted. `_validate_default_contract` and `_apply_defaults` recurse only into children whose declared `type` is exactly `object`, so nullable, composed, or `$ref` intermediates escape the default contract. Every non-leaf pointer segment must now traverse a direct `properties` child with type exactly `object`; other intermediate shapes are rejected at schema load, which restores the totality argument along validated paths.
- **CTM-003:** Accepted. The Task 6 subprocess oracle hand-renders a fixed scope list and thereby omitted the second checker table. The complete-gate oracle now requires scratch control-plane initialization, actual reconciliation and apply, an exactly-one-checker-table assertion, and execution of the reconciled `scripts/check.py`.
- **CTM-004:** Accepted. `pyright` is absent from `pyproject.toml`, `uv.lock`, and the locked environment. It becomes a locked repository test dependency with a refreshed `uv.lock` so the offline oracle has a deterministic executable.
- **CTM-005:** Accepted. Verification and acceptance now require the V4-migration default proof, disable and re-enable proofs with lock assertions, second-plan convergence after each state, and at least one cycle beginning from a Pyright selection.
- **CTM-006:** Accepted. Static-validation claims are narrowed to undeclared paths and non-total traversal shapes. Literal-level validation against option enums and operator/type compatibility is an explicit non-goal; the exactly-one-table planner proofs deterministically catch an impossible literal for Python Tooling.
- **CTM-007:** Accepted. The payload projection is a path/symlink projection; the gate is `project-standards standards sync-payload-projection --check`, not content regeneration.
- **CTM-008:** Accepted. The open question is resolved as a non-goal: only regenerated schema descriptions document the widened grammar in this change; SBA prose remains untouched.

## Audit round 2 disposition

- **CTM-NEW-001:** Accepted. Python Tooling owns the whole `key:/dependency-groups/dev` unit, and the release-candidate legacy intent carries only `types-PyYAML` plus the coverage design's `pytest-xdist>=3.8`; a root-only direct Pyright dependency would conflict with or vanish from the provider-rendered dev group at migration. Pyright now flows through `additional_dev_dependencies` in both the disposable Task 9 intent and the transient Task 11 live intent, with survival assertions, a post-migration `uv.lock` refresh and check, and both oracle selections re-run after the atomic transition. The root selects BasedPyright and retains Pyright solely as the alternate-checker test dependency.
- **CTM-NEW-002:** Accepted. Verified in `_classify_desired`: with no lock entry, the planner adopts only a semantically equal pre-existing unit and otherwise emits `CP-CONSUMER-CONFLICT`; the live root dev array (`ruff>=0.14`, floorless `coverage[toml]`, different ordering, no Pyright) is not equal to the provider rendering. Tasks 9 and 11 must atomically pre-align `/dependency-groups/dev` to the exact provider-derived value before migration preview, never committing that pre-aligned legacy state separately, and prove preview applicability, migrated lock ownership, fixed-point convergence, and locked sync. A separate semantic-adoption mechanism is rejected as broader than this release needs.

## Audit round 3 disposition

- **CTM-NEW-003:** Accepted. The round-2 pre-alignment wording validated only the rewritten value and would have let an unguarded overwrite discard unexpected dependencies before preview, manufacturing adopt-equality and bypassing `CP-CONSUMER-CONFLICT`. Pre-alignment is now a guarded, bounded mutation: exact reviewed pre-write semantic value and source-byte digest preconditions, no write on any mismatch, a replacement derived through the installed provider from the pending migration config rather than a duplicated list, a rewrite of only `/dependency-groups/dev`, before/after and next-lock digest assertions, a negative drift-refusal test, and one shared guarded helper or equivalent contract for Tasks 9 and 11.
- **CTM-NEW-004:** Accepted. Ownership is now explicit: the checker implementation plan owns the predicate, schema, and provider implementation, the locked Pyright dependency, lifecycle tests, and the reconciliation-driven oracles; parallel-coverage Tasks 9 and 11 own the guarded disposable and live pre-alignment, migration intents, migrated-config assertions, lock refresh, and post-atomic oracle reruns. The parallel-coverage plan is amended before Task 9 and its amended Tasks 9 and 11 freshly audited; the prior plan audit remains valid only for unchanged Tasks 1–8 and 10.

## Audit round 4 disposition

- **CTM-NEW-005:** Accepted. Verified: the release replay starts from `copy_tracked_checkout` of the current unified root, no legacy overlay restores `pyproject.toml` or `uv.lock`, and `set_release_version` rewrites version strings in both files. A post-atomic replay would therefore hand the guard already-pre-aligned bytes — refusal breaks the replay, acceptance stops proving the guarded path. The Task 9 legacy overlay now carries frozen guarded-predecessor inputs (`pyproject.toml`, `uv.lock` — post-checker-implementation, pre-atomic, direct Pyright dependency included); post-atomic reconstruction restores those exact bytes before intent injection and pre-alignment; the proofs assert the guarded mutation actually occurs, so an already-aligned shortcut cannot pass; simulated pre-atomic and reconstructed post-atomic runs must produce equivalent release evidence; and the guard's source digest binds after `set_release_version` rewriting, so one canonical predecessor digest governs both runs (version-qualified dual digests are rejected as needless bookkeeping). The pre-alignment helper resolves the sparse migration configuration through the option schema to total effective options before invoking the render provider, since `_dependencies` requires the defaulted `type_checker`.

## Audit round 5 disposition

- **CTM-NEW-006:** Accepted. Verified: `declare_release_cut_intent` currently performs both intent injection and a hand-maintained before/after dev-group rewrite — the duplicated-list anti-pattern the round-3 contract already forbids — and the flow builds the installed distribution only afterward, so a provider-derived rewrite could not use the installed provider. The normative sequence is now: restore predecessor → set release version → build and extract the installed distribution → inject intent only → resolve options and render through that installed provider → guarded rewrite → preview/apply → refresh and check the lock. The proofs assert the rendering provider comes from the extracted installed tree, and the hand-maintained arrays are retired in favor of the guarded helper.
- **CTM-NEW-007:** Accepted. Verified: coverage-plan Task 9 Step 1 derives the overlay's required path set from every catalog-5 legacy-signature target plus `.project-standards.yml` and fails on a missing or extra entry, which would reject the new predecessor inputs. The authoritative overlay set is redefined as the exact union of the signature-target set, `.project-standards.yml`, and the guarded predecessor inputs `pyproject.toml` and `uv.lock`; the recomputation test enforces that union with no other extras.

## Audit round 6 disposition

- **CTM-NEW-008:** Accepted; supersedes CTM-NEW-007's exact-union definition. Implementation exposed that legacy signatures enumerate old authority, not every root target a selected v5 payload may materialize. A post-atomic copy therefore retained non-signature managed outputs and could not reconstruct the same predecessor or release patch. The authoritative overlay set is now the exact union of catalog-5 legacy-signature targets; non-`.standards/` artifact and contribution targets having at least one non-`create-only` declaration; and `.project-standards.yml`, `pyproject.toml`, and `uv.lock`. The live-preserved set is the set of non-`.standards/` targets whose declarations are all `create-only`, minus that complete overlay union; an all-`create-only` target that is also a legacy target therefore remains in the overlay. Live-preserved targets are excluded only when the proof establishes they already exist as regular files and remain byte-identical through migration. All `.standards/**` targets remain outside the overlay because reconstruction removes that directory as one unit. The current catalog adds six paths to the overlay: four pre-atomic files and two pre-atomic absences, all bound to the named pre-atomic Git tree. The post-atomic source shape is the completed migrated authority rather than a hand-built approximation: snapshots, mirroring, and replay use Git-known current paths, including non-ignored additions and tracked deletions, while excluding `.git/**` and ignored runtime artifacts such as `.venv/`, caches, bytecode, and coverage data. Reconstruction must make its predecessor authority tree byte-identical to the pre-atomic predecessor before evidence digests are compared.

## Audit round 7 disposition

- **CTM-NEW-009:** Accepted. Executing the first complete preview exposed a destructive classification defect: the frozen `CLAUDE.md` digest is recognized but is not in Python Tooling's preserved-container set, so migration can retire the whole file before adding only its managed block. The frozen root `AGENTS.md` digest is not yet recognized and would acquire the same defect if it were added only to signature history. Append `sha256:b7b00e3bf4a74e47a19418979925260f73098734c805148ee31384f3e6571b2b` to `legacy-agents`, add it to `_PRESERVED_CONTAINER_DIGESTS`, and add the already-known frozen `CLAUDE.md` digest `sha256:8c9ba6563c70ea051ad36f2054d41f36aa048ce61d813d100d4e7b25d5e05de0` to that preserved set. The release proof must reject Python Tooling whole-file remove/retirement claims against `AGENTS.md`, `CLAUDE.md`, `.vscode/settings.json`, or `.vscode/tasks.json`; it must not reject Agent Handoff's legitimate removal of the exact legacy bounded block. For each instruction file, require the planner's `retired_content` bytes to equal the frozen file after only that legacy block is stripped; require post-apply bytes to preserve that residual as an exact prefix whose suffix contains only one exact current Agent Handoff, Markdown Tooling, and Python Tooling provider envelope plus adapter newline separators. The proof does not use adapter removal as an equality oracle because removal deliberately consumes a separator newline. A static test proves every release-frozen preserved-container digest belongs to both its legacy signature history and the provider's preserved set.
- **CTM-NEW-010:** Accepted. Commit `f9d9b88` refreshed package-owned root release workflows without refreshing their migration signature histories. Append — never replace — the exact frozen digests to preserve recognition of earlier v4 bytes: Markdown Frontmatter `legacy-workflow` gains `sha256:09eb0cc6d6fe20a12b7c7cc5022f9a902ed29d0cdf35ef42c8685c5c93cea036`; Markdown Tooling `legacy-format-caller` gains `sha256:901639336cf3db411a0090c660d36036c2e8bc9bffd592bec3e4c064baf7cb7a` and `legacy-lint-caller` gains `sha256:3124debdc76f2c69dce5e24029de4defb424661835ce8ffad45084276782f656`; Project Spec `legacy-workflow` gains `sha256:0be22314a96e41f9861897e75baf7bfcf35b2f3ae51870db0f9cc6e982fa5525`; and Python Tooling `legacy-agents` gains the digest in CTM-NEW-009. Markdown Frontmatter's recognized legacy workflow is intentionally removed and replaced by contributions composed at `.github/workflows/validate-standards.yml` (this removal clause is superseded by CTM-NEW-017 for explicit local mode). The consumer-owned unknown `check.yml` is excluded from the membership rule: it remains intentionally unrecognized and preserved through its explicit relinquishment intent.
- **CTM-NEW-011:** Accepted with compatibility refinement. Migration classification must change with signature currency without reclassifying older recognized self-host bytes as caller mode. Markdown Tooling uses explicit complete self-host cohorts: the historical pair (`sha256:207b5463a64bc7a48e6af31620ebc5052c71118f350e18375a36435061a6e7a5`, `sha256:89ad3220574ce78a9628208d768344f300e5e1d701d7adaf16eb923f4cc8f772`) and the current frozen/resource pair (`sha256:901639336cf3db411a0090c660d36036c2e8bc9bffd592bec3e4c064baf7cb7a`, `sha256:3124debdc76f2e69dce5e24029de4defb424661835ce8ffad45084276782f656`). Only an exact complete cohort selects self-hosted mode; a partial or cross-generation mix fails migration. Project Spec accepts both the historical classifier `sha256:2e38ae698e0a45f9afdde997ce2fa58c827f4bdb518e108ca9d0a1f22f278cc8` and current frozen classifier `sha256:0be22314a96e41f9861897e75baf7bfcf35b2f3ae51870db0f9cc6e982fa5525` as self-hosted. The current Markdown Tooling frozen workflows are byte-identical to the selected self-host resources. Project Spec's current frozen workflow deliberately is not: it contains transitional `.project-standards.yml` triggers, so migration intentionally replaces it in place with the immutable v5 self-host resource. Tests cover both historical/current classifier generations, mixed/partial refusal, and caller-mode histories. The release preview, changed-path ledger, and retained evidence must distinguish Markdown Frontmatter's remove-and-compose transition (superseded by CTM-NEW-017 for explicit local mode), Markdown Tooling byte preservation, and Project Spec's in-place replacement rather than claim universal workflow byte identity.
- **CTM-NEW-012:** Accepted. The frozen `.vscode/tasks.json` `keyed-set:/tasks#label=check` unit conflicts with Python Tooling's parallel-aware provider output. A second guarded predecessor alignment is required. From the frozen whole-file digest `sha256:8dcb4880139bb708bf20819479bcb7898bb5d1dabd8d79e43b7d64bb3e4b3b08`, use the installed Python Tooling provider and `JsoncAdapter` to render and update only that keyed unit; require the exact reviewed legacy task value, full-file source digest, installed-provider provenance, parsed equality for the document version and every non-target task, a real mutation, and one final write. The installed-provider derivation produces task semantic digest `sha256:119597ceaea2647bae17e3261ad820bf1a7ffec997a33b431c9396797e03ff6d` and deterministic post-alignment whole-file digest `sha256:cf4aa30c3e2bfb1d69d0cfb7953d1e351db0e6ab06c5812f48cf9eface79a9f7`; record both through the overlay proof and append the whole-file digest to both `legacy-vscode-tasks` known history and `_PRESERVED_CONTAINER_DIGESTS`. Wrong digest, unexpected task semantics, or already-aligned input refuses without a write. The preview's next-lock unit must bind the same provider provenance and semantic digest reported by the guard.
- **CTM-NEW-013:** Accepted. The approved release intent already contains `pytest-xdist>=3.8`, but Python Tooling's `additional_dev_dependencies` item pattern rejects comparison operators. Widen only that item pattern from excluding comparator characters to excluding whitespace and backticks; retain all array, uniqueness, and closed-schema constraints and add positive comparator plus negative whitespace/backtick tests. This is an unpublished v5 payload correction, not an option addition or migration. Refresh every edited resource digest, family aggregate, catalog entry, and activation expectation across Markdown Frontmatter 1.2, Markdown Tooling 1.2, Project Spec 1.1, and Python Tooling 1.1; verify their source projections; then run package/schema/graph, source-wheel, activation, reconstruction, and evidence gates in that order, with retained evidence refreshed last.

## Complete-release-evidence reconciliation

- **CTM-NEW-014:** Accepted. Task 9's executable patch proves the package migration itself, but Task 11 subsequently changes release-only instruction, documentation, changelog, test, adoption, handoff, and metadata surfaces. Reusing the dirty post-finalization worktree as predecessor authority would either leak those bytes backward or let retained evidence describe only the migration subset rather than the atomic release commit. Task 11 must record the clean pre-atomic `HEAD` before its first live mutation and materialize that exact Git object tree as immutable predecessor authority. After every non-evidence release edit and verification cleanup, derive a second, canonical **complete release-content patch** from that recorded tree to the Git-known live tree with binary diffs and rename detection disabled; exclude only the retained evidence file to break self-reference. The scratch predecessor repository must start with an index equal to its committed tree and mark all non-ignored working-tree additions intent-to-add before diffing, so `.standards/**` and every other new release path appear in both patch and ledger without staging final content. Replay that patch against a fresh copy of the same pre-atomic tree and require exact mode-, symlink-, path-, and byte-aware Git-known equality with the live tree under the same one-file exclusion. A positive synthetic addition test proves the patch and ledger include new files. The retained changed-path ledger and release-content patch digest come from this complete patch, while Task 9's migration patch remains a separately named engine-proof fact. Refresh the release-input digest from the final live tree, again excluding only evidence. Bind the separately validated evidence bytes by SHA-256 to both the staged index blob and committed `HEAD` under clean-worktree checks, because that file is intentionally invisible to the canonical content diff. After committing, recompute the committed parent-to-release diff read-only with the same canonical routine and evidence exclusion and require the exact recorded paths and digest before tagging or publishing.

## Release-bootstrap reconciliation

- **CTM-NEW-017:** Accepted; supersedes only the Markdown Frontmatter removal clauses in CTM-NEW-010 and CTM-NEW-011. Catalog migration sets `workflow_mode = "local"` for this repository's release intent, preserves the recognized reusable `.github/workflows/validate-markdown-frontmatter.yml` endpoint byte-for-byte through the preliminary Task 9 migration, and composes `.github/workflows/validate-standards.yml` as a same-commit local caller. This avoids requiring the unpublished `v5` tag during the first `main` release run while keeping the canonical public `workflow_call` path available. The preliminary dependency scan classifies that one retained V4-shaped workflow as `release-bootstrap`; no other active migrated workflow may retain legacy authority. Task 11 rewrites the endpoint in place to unified v5 defaults and commands, retains `workflow_call`, and keeps the composed root workflow on the local call. Evidence distinguishes endpoint preservation plus local composition from Markdown Tooling byte preservation and Project Spec in-place replacement.

## Executable-proof reconciliation

- **CTM-NEW-015:** Accepted. Literal Task 9 execution of the migrated repository-root checker reached clean Ruff and BasedPyright phases and 2,586 passing tests, but correctly failed 96 root tests that remain bound to the pre-atomic `.project-standards.yml`, v4 version/changelog, workflow, dogfood, and legacy-CLI surfaces until Task 11 changes them in the same atomic release commit. Treating those failures as a checker defect would collapse the release boundary; weakening the nested test selection would not prove the real root gate. Task 9 therefore proves exact installed-provider script bytes and parallel command construction, executes the complete reconciled gate for both checker selections in isolated scratch consumers, and performs locked offline sync plus exact Pyright lock checks in both reconstructed migration paths. Task 11, after all release-only root tests/documents/workflows/version facts are final, executes the migrated repository gate and complete release checklist. This is an evidence-sequencing correction, not a checker or package behavior change.
- **CTM-NEW-016:** Accepted. Independent adversarial review and executable probes found five release-proof integrity weaknesses: ambient Git object/index/diff variables could redirect authority; canonical patch bytes depended on Git diff environment/config; the release-input stream used ambiguous NUL framing for arbitrary binary file content; instruction preservation trusted planner residual output as its own oracle; and retained prose bound only the input digest rather than the executed migration ledger/patch/control-plane/guard record. All release Git calls now use a minimal non-credential environment with system/global config disabled, self-contained overrides pass sanitized `fsck`, canonical diff formatting is fully pinned, release inputs use domain-separated length-prefixed fields, instruction residuals are independently sliced from frozen bytes, and one parsed machine-readable evidence record must exactly equal the slow proof result. Focused regressions cover ambient alternate borrowing, external indexes/diff options, the prior framing collision, and record mismatch.

## Implementation reconciliation

Implementation-time dependency resolution selected [`pyright==1.1.411`](https://pypi.org/project/pyright/1.1.411/). Unlike the wrapper behavior observed during plan audit, this release's 6.2 MB wheel contains the matching JavaScript runtime under `pyright/dist` and selects it before npm/cache installation. The cache-provisioning mechanics introduced for CR-001 are therefore superseded for this exact pin: the repository locks the complete wheel, adds no CI provisioning step, and proves `pyright --version` succeeds with uv/npm offline, an empty cache path that remains absent, `PYRIGHT_PYTHON_IGNORE_WARNINGS=true`, and bundled-runtime use required.

The generated complete gate still runs `pip-audit`. Its supported vulnerability services are network-backed, and [upstream offline vulnerability-service support remains open](https://github.com/pypa/pip-audit/issues/698). The oracle's boundary is consequently narrowed to offline locked-tool resolution plus complete gate execution; it does not claim that the `pip-audit` phase is network-isolated. This correction changes neither checker-table materialization nor the release-integration requirement to carry the exact Pyright pin.

The first complete predecessor preview then exposed release-signature drift introduced by the root workflow refresh at `f9d9b88`, unsafe preserved-container membership for the frozen instruction files, and one expected semantic conflict in the legacy VS Code `check` task. Those are package-contract inputs, not reasons to move the frozen predecessor baseline. Round 7 therefore keeps the named `26fb984` root authority, refreshes exact signature histories and self-host classifiers, proves container-preservation coherence, and adds an installed-provider-derived task-unit guard alongside the existing dev-group guard. It also records the narrow comparator-pattern correction required by the already-approved release intent.

## Contract changes

### Predicate model and generated schema

`MaterializationPredicate` in `src/project_standards/package_contract/payload.py` widens `option` to the two disjoint spellings with the grammar above, and `matches` gains nested traversal. The generated `src/project_standards/schemas/standard-payload.schema.json` is regenerated; the field description distinguishes bare top-level names from nested option pointers.

### Option-schema cross-contract validation

`load_option_schema` cross-checks every contribution and artifact `when_any` predicate against the loaded option schema's declared property paths, enforces the object-typed intermediate rule, and fails closed as `PC-OPTIONS` on any violation.

### Python Tooling 1.1 payload

`standards/python-tooling/versions/1.1/payload.toml` adds the two predicates and the release-current `AGENTS.md` and post-task-alignment `.vscode/tasks.json` signature digests. Its provider preserves the exact release-frozen shared containers named by the round-7 contract; older known histories retain their existing dispositions. `config.schema.json` changes only the `additional_dev_dependencies` item grammar so ordinary comparator-bearing requirements such as `pytest-xdist>=3.8` are valid; no option, default, or structural shape changes and no consumer migration is required. Payload resource digests plus catalog and family metadata are regenerated, and the payload path projection is verified with `project-standards standards sync-payload-projection --check`.

### Provider

`standards/python-tooling/versions/1.1/providers/python_tooling.py` guards `_checker_table`. Selected-table rendering stays byte-identical to current output; the non-selected table is never rendered.

### Repository test dependencies

`pyright==1.1.411` becomes a pinned, locked repository test dependency — `pyproject.toml` plus a refreshed `uv.lock`. Its wheel contains the matching bundled runtime, so `uv sync --locked --all-groups` installs the complete checker without a separate npm/cache provisioning phase. The oracle disables the wrapper's PyPI version-warning check, requires bundled-runtime use, sets uv and npm offline, points the wrapper at an empty cache, and asserts that cache remains absent after the version probe. The subsequent generated gate is complete but not wholly network-isolated because `pip-audit` queries its configured vulnerability service. Before the atomic migration the root declares the dependency directly; through migration it travels as the exact `pyright==1.1.411` `additional_dev_dependencies` entry so the provider-rendered dev group retains it — see Release integration.

### Release integration

Python Tooling owns the entire `key:/dependency-groups/dev` unit, and the planner adopts a pre-existing unlocked unit only when it is semantically equal to the provider rendering — otherwise it emits `CP-CONSUMER-CONFLICT`. Two obligations follow for the parallel-coverage plan's release tasks:

- **Pyright carry-through.** The Task 9 disposable legacy intent and the Task 11 transient live intent both add the exact pinned requirement recorded by the checker implementation (for example `pyright==X.Y.Z`) — never the bare name — to `additional_dev_dependencies`. The release proofs assert that exact requirement, equal to the pre-migration root pin, survives in `.standards/config.toml`, the provider-rendered dev group, and the refreshed `uv.lock`, and re-run both complete-gate oracle selections after the transition. The root records BasedPyright as its selected checker and retains Pyright solely as an alternate-checker test dependency.
- **Dev-group pre-alignment (guarded).** Both release preparations pre-align `/dependency-groups/dev` through one guarded helper or an equivalent shared contract that asserts the exact reviewed pre-write semantic value and the source file's byte digest and refuses without writing on any mismatch; derives the replacement by rendering the installed provider's `key:/dependency-groups/dev` contribution from the pending migration config — never a duplicated dependency list — so adopt-equality holds by construction; rewrites only that unit; and asserts before/after semantic digests plus the next-lock digest. Unexpected dependency drift therefore refuses pre-alignment without mutation instead of being silently discarded, preserving the planner's `CP-CONSUMER-CONFLICT` protection. The pre-aligned state exists only inside the atomic release preparation, never as a separately committed legacy state, and the proofs cover migration preview applicability, migrated dev-group and lock ownership, fixed-point convergence, and a locked offline sync.
- **VS Code check-task pre-alignment (guarded).** Both release preparations also pre-align only `.vscode/tasks.json` `keyed-set:/tasks#label=check` from the exact frozen container and reviewed task value. The replacement is rendered through the declared Python Tooling provider from the same extracted installed distribution and sparse migration config, inspected and updated through `JsoncAdapter`, and written only after the source digest, target semantics, installed-provider provenance, non-target parsed equality, and mutation requirements pass. Its deterministic post-alignment container digest is fixture authority and must appear in both the signature's known history and the provider's preserved-container set; its semantic digest and provider provenance must equal the previewed next-lock unit. The same no-write failures cover source drift, semantic drift, and already-aligned input.
- **Signature, classifier, and preserved-container currency.** The exact frozen package-owned root workflow and instruction digests are appended to their package histories without deleting older v4 signatures. Markdown Tooling's explicit paired cohorts and Project Spec's historical/current classifier set retain self-host semantics across recognized generations. Python Tooling's frozen `AGENTS.md` and `CLAUDE.md` digests, plus the deterministic post-alignment `.vscode/tasks.json` digest, are simultaneously recognized and preserved. Preview and apply may replace Agent Handoff's exact legacy instruction block with the three current provider blocks, preserve Markdown Frontmatter's recognized reusable endpoint in explicit local mode while composing `validate-standards.yml` as a same-commit caller, and replace Project Spec's transitional self-host workflow with its immutable v5 resource, but may not retire an instruction or shared VS Code container as a whole file. Consumer-owned `check.yml` remains deliberately unknown and unlocked.
- **Complete atomic release evidence.** Task 9 retains its migration-only patch and pre/post reconstruction equivalence proof. Task 11 records the clean pre-atomic Git commit before mutation, reruns the migration proof from a materialization of that commit rather than from the finalized live root, and separately derives the canonical complete release-content patch after all release-only edits. Only the retained evidence file is excluded from the complete patch and replay comparison. The final evidence distinguishes the migration patch from the complete release-content patch, binds the final release-input digest, and is verified against the committed parent-to-release diff before publication.

- **Guarded predecessor reconstruction.** The Task 9 legacy overlay carries frozen root-authority inputs from one named post-checker, pre-atomic Git tree, including `pyproject.toml` and `uv.lock` with the direct Pyright dependency. Its authoritative path set is the exact union of catalog-5 legacy-signature targets; non-`.standards/` artifact and contribution targets having at least one non-`create-only` declaration; and `.project-standards.yml`, `pyproject.toml`, and `uv.lock`. The live-preserved all-`create-only` set subtracts that overlay union, so create-only legacy targets still restore from the frozen tree; every remaining live-preserved target must be a regular file and remain byte-identical. `.standards/**` stays outside the overlay because reconstruction removes that directory wholesale. Post-atomic reconstruction restores every declared file or absence before intent injection and the two guarded alignments, so the replay exercises the true predecessor rather than leaked migrated outputs. The normative operation sequence is: restore predecessor → set release version → build and extract the installed distribution → inject intent only → resolve options and render both guarded units through that installed provider → guarded dev-group rewrite → guarded VS Code check-task rewrite → preview/apply → refresh and check the lock. The proof builds its post-atomic source from the completed migrated Git-known authority, reconstructs it, and requires byte-identical predecessor authority trees before comparing changed paths or release digests; repository metadata and ignored execution artifacts are never mirrored or compared as release content. It also asserts that the rendering provider comes from the extracted installed tree, that intent injection touches neither guarded unit, and that both guarded mutations actually occur — an already-aligned state cannot satisfy the test. The dev-group source digest binds after `set_release_version`; the task container digest binds the frozen bytes before its guard. The helper resolves the sparse migration configuration through the option schema to total effective options before invoking either contribution provider.

**Plan ownership.** The checker implementation plan owns the predicate, schema, and provider implementation, the locked Pyright dependency, the lifecycle tests, and the reconciliation-driven oracles. Parallel-coverage Tasks 9 and 11 own both guarded disposable/live alignments, signature/preserved-container/classifier currency required by the frozen predecessor, the narrow comparator schema correction and integrity refresh, the frozen predecessor overlay and its post-atomic restoration, migration intents, migrated-config assertions, lock refresh, and post-atomic oracle reruns. The parallel-coverage plan is amended before Task 9, and the amended Tasks 9 and 11 receive a fresh plan audit before execution; the prior audit remains valid only for unchanged Tasks 1–8 and 10.

### Lifecycle and migration interplay

No new engine machinery is required for transitions: the planner already builds desired intents only from materialized contributions and keeps previous-lock targets in diff scope, so a checker transition removes the stale table's unit and lock entry and creates the new one through the same path as package disable. The ownership-relinquishment predicate evaluates per-target materialization and composes unchanged. The migration `affected` list retains both checker contributions as metadata; the desired tree governs actual materialization. No shipped test currently flips a `when_any` predicate between applies, so the transition proofs below are new mandatory coverage, not redundancy — and the migration and disable/re-enable promises in this section carry matching mandatory proofs of their own.

## Non-goals

- Documenting the widened predicate grammar in Standard Bundle Authoring 2.0 prose. SBA's README and templates do not document `when_any` at all today, so there is no stale author-facing text to correct; only the regenerated schema descriptions change here.
- Static validation of predicate literals against option enums or operator/type compatibility; the exactly-one-table planner proofs cover the residual class for Python Tooling.
- Any new option, option-shape/default change, or change to top-level predicate spellings and behavior; the comparator item-pattern correction in CTM-NEW-013 is the only schema-byte exception.

## Files and ownership

| Surface | Responsibility |
| --- | --- |
| `src/project_standards/package_contract/payload.py` | Widened predicate grammar, nested matching, and `load_option_schema` cross-contract validation |
| `src/project_standards/schemas/standard-payload.schema.json` | Regenerated predicate contract and descriptions |
| Markdown Frontmatter 1.2, Markdown Tooling 1.2, Project Spec 1.1, and Python Tooling 1.1 payload/provider manifests plus catalog/family metadata | Frozen signature histories, historical/current self-host classifier cohorts, preserved-container safety, comparator grammar, conditional checker-table declarations, and regenerated integrity digests; projection gate via `sync-payload-projection --check` |
| `standards/python-tooling/versions/1.1/providers/python_tooling.py` | Selected-table-only rendering guard and recognized preserved-container set |
| `tests/package_contract/test_payload.py` and `tests/package_contract/test_payload_outputs.py` | Grammar, canonicality, matching, and static-validation coverage |
| `tests/control_plane/test_planner.py` and `tests/control_plane/test_lifecycle.py` | Exactly-one-table planning and checker-transition lifecycle proofs |
| `tests/package_contract/test_python_tooling_reconstruction.py` | Rendering, guard, and source/wheel reconstruction coverage |
| Reconciliation-driven complete-gate oracle beside the Task 6 subprocess oracle | Scratch control-plane apply, exactly-one-table assertion, and execution of the reconciled gate |
| `pyproject.toml` and `uv.lock` | Locked `pyright==1.1.411` dependency and bundled-runtime oracle |
| `tests/fixtures/package_compatibility/legacy/release-root/**`, `tests/package_compatibility/release_candidate.py`, and `tests/package_compatibility/test_release_candidate.py` | Complete root-authority predecessor overlay, Pyright carry-through, post-atomic reconstruction, both guarded alignments, preview dispositions, instruction-block preservation, recorded pre-atomic Git-tree materialization, and complete release-content replay coordinated with coverage-plan Tasks 9 and 11 |

## Failure behavior

- A malformed, noncanonical, single-segment, escaped, indexed, or non-NFC pointer fails manifest validation before load completes.
- A well-formed predicate naming an undeclared path, or traversing a nullable, composed, `$ref`, or non-object intermediate, fails option-schema loading as `PC-OPTIONS` before planning or writes.
- At runtime, a missing pointer target or non-mapping intermediate is a non-match; along statically validated paths this state is unreachable for schema-valid configurations.
- A valid-path predicate with an impossible literal is not statically rejected; the exactly-one-table planner proofs fail instead.
- A migration intent that omits Pyright from `additional_dev_dependencies`, or a live dev group left semantically unequal to the provider rendering at migration preview, fails the release proofs with `CP-CONSUMER-CONFLICT` instead of silent adoption or replacement.
- Pre-alignment refuses without mutation when the observed dev group or source digest differs from the reviewed pre-write state; unexpected dependency drift surfaces as a refused release preparation, not a silent overwrite.
- Check-task pre-alignment likewise refuses without mutation on a wrong container digest, unexpected semantic task value, non-target drift, or already-aligned input; its post-alignment whole-file and semantic digests must match payload history and next-lock state.
- A frozen container digest that is recognized but absent from the provider's preserved set, or preserved but absent from the corresponding signature history, fails the static coherence test. Any Python Tooling whole-file retirement of `AGENTS.md`, `CLAUDE.md`, `.vscode/settings.json`, or `.vscode/tasks.json` fails before apply; Agent Handoff's exact legacy bounded-block removal remains required.
- A post-atomic replay that fails to restore the frozen predecessor bytes fails its proofs — the guard refuses the migrated bytes, and the mutation-occurred assertion rejects an already-aligned shortcut — instead of silently passing on post-migration state.
- An overlay entry outside the authoritative union — legacy-signature targets, non-`.standards/` targets with any non-`create-only` declaration, `.project-standards.yml`, `pyproject.toml`, and `uv.lock` — or a missing entry fails the overlay recomputation test. An all-`create-only` target that is absent or changes bytes fails its separate preservation proof.
- A guarded rewrite rendered by anything other than the provider from the extracted installed distribution, or performed before that distribution exists, fails the provenance assertion.
- The provider refuses to render a checker table that does not match the resolved selection.
- A checker transition, V4 migration, or disable/re-enable cycle that leaves a stale table, drops or retains a wrong lock entry, or fails second-plan convergence fails its lifecycle proof.
- Payload byte changes without regenerated digests fail package, graph, and source/wheel reconstruction gates; projection drift fails `sync-payload-projection --check`.
- A Task 11 proof sourced from the finalized dirty root, a complete release-content patch that omits any non-evidence Git-known change, or a replay differing from the final live tree in path, mode, symlink, or bytes fails before commit. A committed parent-to-release diff that differs from the retained complete-patch digest or ledger blocks tagging and publication.
- An older engine reading a pointer-bearing payload fails loudly at predicate validation rather than mis-evaluating it.

## Verification

Follow test-driven development:

1. Add failing predicate syntax tests: canonical pointer acceptance; rejection of malformed, single-segment, escaped, indexed, and non-NFC spellings; type-sensitive `equals`/`contains` at nested leaves; missing-target and non-mapping-intermediate non-match.
2. Add failing cross-contract tests at `load_option_schema`: an undeclared path fails as `PC-OPTIONS`; nullable, composed, `$ref`, and missing-type intermediates fail; every existing shipped payload still loads.
3. Add failing planner tests: fresh apply materializes exactly one checker table for each checker selection.
4. Add failing lifecycle tests: `basedpyright → pyright → basedpyright` transitions remove the previously locked table, create and lock the newly selected table, and converge to an empty second plan after each step; disable removes the selected table and its lock unit; re-enable restores only the retained selection and converges; at least one full cycle begins from a Pyright selection.
5. Add a failing migration test: V4 migration produces only the default BasedPyright table and converges.
6. Add failing provider tests: `_checker_table` refuses the non-selected table; selected-table rendering is byte-identical to current output.
7. Implement the engine contract, cross-contract validation, payload predicates, and provider guard.
8. Add `pyright` as a locked repository test dependency and refresh `uv.lock`.
9. Regenerate the payload schema, package digests, and catalog metadata; run `sync-payload-projection --check`.
10. Add the reconciliation-driven complete-gate oracle: initialize a scratch control plane, reconcile and apply Python Tooling defaults, assert the composed pyproject contains exactly one checker table, prove checker execution from the locked environment with uv/npm offline and an unused Pyright cache, then execute the reconciled `scripts/check.py` and pass; repeat with the Pyright selection. The `pip-audit` phase retains its configured vulnerability-service network access.
11. Extend the release-candidate fixture and test in coordination with coverage-plan Tasks 9 and 11: derive the complete overlay, freeze each declared file or absence from the named pre-atomic Git tree, and prove excluded all-`create-only` targets remain byte-identical; carry exact Pyright through both intents; append the frozen package-owned workflow/instruction signatures, preserve explicit historical/current self-host classifier cohorts, and statically prove release-current preserved-container membership while retaining older standard-owned retirement behavior; keep consumer-owned `check.yml` intentionally unknown; add comparator-schema tests and refresh all four affected package integrity chains; follow the normative sequence — restore predecessor, set version, build/extract, inject intent only, provider-render and guard both `/dependency-groups/dev` and `.vscode/tasks.json`'s `check` task, preview/apply, refresh/check the lock — with exact source/semantic preconditions, bounded non-target equality, installed-provider provenance, mutation-required, and no-write negative cases for each guard; record the deterministic post-task container digest in the fixture and payload preservation contracts; reject Python Tooling whole-file retirement of the four shared containers through `retired_targets`, `legacy_removals`, and claims while allowing the exact Agent Handoff `retired_content` transition; require that residual as the exact post-apply prefix and prove its suffix contains only one byte-identical current envelope per provider plus adapter separators; prove Markdown Frontmatter's explicit-local endpoint preservation plus same-commit composition, Markdown Tooling byte preservation, and Project Spec's in-place self-host workflow replacement; then reconstruct from the completed migrated tree and prove byte-identical predecessors, equivalent migration evidence, preview applicability, next-lock semantic/provenance equality for both guarded units, fixed-point convergence, locked offline sync, and both oracle selections. In Task 11, additionally rewrite the Frontmatter endpoint to unified v5 authority, record and materialize the clean pre-atomic Git tree, derive and replay the complete final release-content patch with only retained evidence excluded, refresh final input/patch/ledger evidence, and verify the committed parent-to-release diff before publication.
12. Run focused package-contract, control-plane, and Python Tooling reconstruction suites, then the repository gate.

## Acceptance criteria

- Exactly one checker table materializes for every schema-valid Python Tooling 1.1 configuration; the dual-table pair is unreachable from any accepted configuration.
- Top-level predicate spellings and behavior are unchanged; existing payloads load and evaluate identically.
- Every `when_any` path is statically proven against the closed option schema at option-schema load, including the object-typed intermediate rule.
- Checker transitions, V4 migration, and disable/re-enable cycles preserve lock integrity and second-plan convergence, including a cycle beginning from a Pyright selection.
- A reconciliation-driven scratch consumer composes exactly one checker table, resolves both checkers from the locked toolchain without a Pyright cache or npm download, and passes the full reconciled gate end to end for both selections. `pip-audit` retains vulnerability-service network access; the narrowed subprocess oracle remains valid for its own subprocess-coverage claim.
- The VS Code mode keys remain unconditional; `config.schema.json` changes only to admit comparator-bearing `additional_dev_dependencies`, with whitespace/backtick rejection and every other option contract unchanged.
- All digests and generated schemas are regenerated, and `sync-payload-projection --check` passes.
- Both migration intents carry Pyright through `additional_dev_dependencies`; the migrated `.standards/config.toml`, dev group, and refreshed `uv.lock` retain it, and both oracle selections pass after the atomic transition.
- `/dependency-groups/dev` is pre-aligned through the guarded helper — provider-derived from schema-resolved total options, precondition-checked, bounded, digest-asserted — within the atomic release preparation; unexpected drift refuses without mutation; migration preview remains applicable with no `CP-CONSUMER-CONFLICT`, and the migrated unit converges to a fixed point.
- `.vscode/tasks.json`'s `check` keyed unit is pre-aligned through the installed provider under the same fail-closed discipline; the deterministic post-alignment container is recognized and preserved, its next-lock unit matches the guard, and all other tasks plus the document version remain equal.
- Every frozen package-owned release workflow is recognized; consumer-owned `check.yml` remains deliberately unknown and preserved without an action or lock. Historical and current self-host classifier cohorts select the intended mode, mixed/partial cohorts fail, no instruction or shared VS Code container is retired as a whole file, and instruction residual bytes remain identical across the exact legacy-block-to-three-current-block transition. Exactly one byte-identical block from each current provider is present. Workflow effects are explicit: Task 9 preserves Markdown Frontmatter's reusable endpoint in explicit local mode and composes `validate-standards.yml` as a same-commit caller, Task 11 rewrites that endpoint to unified v5 authority, Markdown Tooling preserves both files byte-for-byte, and Project Spec replaces its transitional workflow in place.
- Post-atomic reconstruction restores every frozen root-authority file and absence under the complete exact-union overlay set, excludes `.standards/**`, proves every live-copied all-`create-only` target is present and byte-identical, and makes the pre-atomic and reconstructed post-atomic predecessor Git-known authority trees byte-identical without `.git/**` or ignored runtime artifacts. Both guarded rewrites are rendered by the provider from the extracted installed distribution in the normative order, both guarded mutations provably occur in both runs, and both runs produce equivalent release evidence.
- Task 11's final evidence separately covers every Git-known atomic release-content change from the recorded pre-atomic commit, excluding only the self-referential retained evidence file; replay is exactly equal to the final live tree under that exclusion, and the committed diff is reverified before tag/publish.
- The parallel-coverage plan is amended and its amended Tasks 9 and 11 freshly audited before Task 9 resumes; the prior plan audit covers only unchanged Tasks 1–8 and 10.
