# Project Status

## Current snapshot

- MCP implementation T10 closed 2026-07-30 (`e30862f`): the cross-cutting proving suite closes protocol, safety, determinism, and CI acceptance over the T2–T9 implementation — 39 tests (32 new + 7 pre-existing import-boundary tests) across `tests/mcp_server/contract/` and `tests/mcp_server/integration/`, with the seven frozen TC-T10 node ids landed verbatim. The single product change is the authorized transport mapping: an unexpected handler exception now answers a structured `-32603` refusal carrying the newly ratified `internal-error` code in both eras, with raw exception text never reaching the wire and the detail proven present on stderr; the SDK's generic path (classic `code: 0` with exception text, modern `-32603` without `data`) is never served. ADR 0026 gained the error-taxonomy amendment queued since T3 (`2c75bea`), corrected to the full 32-code inventory after the engineer refuted the orchestrator's 22-code census (`0323bb1`). Both adversarial Codex reviews were vacuity-focused by design and dispositioned with recorded arbitration: RED returned 12 blocking + 2 should-fix (sorted-JSON masking the hash-seed canary, oracle-imports-implementation, recursive diagnostics masking, one-assignment-per-registry instructions sampling — all accepted; the engineer's six-vs-seven doc-gap claim was refuted against DR-002), and GREEN returned 3 blocking + 2 should-fix (internal-error literal pin, MRO-complete facade freeze, containing-package import resolution, stderr presence, census arithmetic — all accepted) with 10/14 RED dispositions judged faithful. The suite now proves cross-process hash-seed determinism, order-preserving wire comparison against independent projections for all eight stable surfaces, every instructions rendering across the full matrix, the literal `dev.project-standards/declaration` namespace with the seven DR-002 facts, reviewed absolute context ceilings that demonstrably trip on the recorded T6 scale defects (catalog 196,608 vs the 373,619-byte defect; listing 65,536 vs 587,143 — ceiling values flagged for owner review), a package-root-correct one-way SDK-isolated import graph, CI phase-union discovery without touching `check.yml`, the four-way `mcp==2.0.0` pin against the T1 evidence record, and whole-transcript notification absence. The battery passed 417 plan-mandated tests, all four §13 package-contract commands, 4104 ordinary tests at 90% coverage, 133 compatibility, 5 performance, pip-audit clean, and dogfood validation under the rebuilt candidate wheel; T11 (installed-wheel clients and documentation) is the ready task awaiting owner direction, and its OQ-005 real-consumer decision plus the codex `mcp_2026_07_28` flag re-check remain owner-facing inputs.
- MCP implementation T9 closed 2026-07-30 (`7b46a30`): the server registers `reconcile_preview`, `validate_repo`, and `drift_check` unconditionally on both eras — `reconcile_preview` returns the §5.5-frozen two-slot envelope (the exact `McpServiceFacade.reconcile` projection when the authoritative classification is initialized, otherwise the `RepoInspectionSnapshot` via `inspect_repo`, satisfying EC-005 at the tool layer; exactly-one-non-null is enforced by the advertised `oneOf` schema and proven by out-of-band falsification), the composite tools map the facade with no provider selection in the adapter, and client-advertised roots are fetched per repository-scoped call (classic era over the `roots/list` back-channel; the 2026-07-28 era raises `NoBackChannelError` and malformed successes degrade identically to unconstrained `None`, never refuse-everything `()`) narrowing but never widening the mandatory explicit `repo_root`, with `repo_inspect` joining the same resolver seam. The instructions surface became matrix-conditional under the 2026-07-30 ADR 0026 amendment (six-tool text when the session registers all six; the enumeration reduces to the actual registry otherwise) after the Codex RED review overturned the verbatim-always freeze, and the contract amendments at `223fcd5` also froze the envelope in plan §5.5. Both adversarial Codex reviews (RED 7 blocking + 2 should-fix; GREEN 3 blocking + 2 should-fix) were dispositioned with recorded arbitration — worker-interpreter instrumentation demands were rejected on the digest-verified trusted-payload trust model with bounded substitutes accepted (conservative relative-write rejection, `dir_fd`/exact-root controls, full-frame secret-sentinel sweep) — and TC-T9-003's registry and subprocess fixtures prove zero writes/apply reachability. The full battery passed 4072 ordinary + 133 compatibility + 5 performance tests at 90% coverage with pip-audit clean under the rebuilt candidate wheel; the signed commit carries the plan files-row amendment; T10 (protocol, safety, determinism, CI) is the ready task awaiting owner direction.
- MCP implementation T8 closed 2026-07-30 (`614e7e2`): the server registers the two generic discovery tools unconditionally on both eras — `standards_list` (no arguments) returns the FR-001 field-masked catalog projection as structured content, structurally equal to the `standards://catalog/5` document served in the same session through the single extracted `catalog_projection()` producer, and `repo_inspect` requires an explicit `repo_root` resolved through `resolve_effective_root` (launch boundary and advertised roots narrow, never widen) and returns the full ten-state `RepoInspectionSnapshot` with all 21 §5.5 `Finding` fields typed, required, and closed; every v1 tool declares a validated closed output schema and returns conforming structured content (`standard_read`'s is identity plus the complete seven-field DR-002 declaration, ruled defensible against duplicating payload bytes under NFR-012), tool metadata is a reviewed per-tool snapshot satisfying FR-023 with the invented numeric budgets removed, a `sys.addaudithook` oracle proves discovery facts flow only through the facade, DR-005 exclusion is canary-witnessed, and the all-direct counterfactual keeps both tools registered — closing the T7.4 carry-forward time bomb. The halted Codex RED review was rerun on a fresh seal (REWORK: 7 blocking + 3 should-fix, all accepted; freezes #3 mandatory-`readOnlyHint` and #4 numeric budgets overturned, so no ToolAnnotations anywhere) and the Codex GREEN review returned 2 blocking + 1 should-fix, all accepted and fixed with out-of-band schema-falsification proof (malformed declarations and findings now provably rejected); the full battery passed 4050 ordinary + 133 compatibility tests with 90% coverage under the rebuilt candidate wheel and the signed commit carries the plan amendment. T9 (reconciliation and provider tools) is the ready task awaiting owner direction.
- MCP implementation T7 closed 2026-07-29 (`572d84b`): the server derives prompts only from approved prompt-role resources — and the installed distribution's 22 declared resource roles include none, so `APPROVED_PROMPT_ROLES` is empty, a launch guard refuses any undeclared derivation, and the prompts capability is truthfully omitted; the FR-008 `standard_read` tool registers only for clients whose model-initiated direct resource access the T1 matrix records as not established (`CLIENT_DIRECT_RESOURCE_ACCESS` pinned whole to claude-code true / codex-cli false), delegates byte-for-byte to `ResourceRegistry.read`, and refuses NUL-bearing, non-canonical, and undeclared URIs with era-correct codes (legacy `-32002`, modern `-32602`); `PHASE_INSTRUCTIONS` was reworded to phase truth and `transport.py` remains the sole SDK importer. Two adversarial Codex reviews (RED 4 findings, GREEN 2) were dispositioned with recorded arbitration — RED froze the matrix seam as the named `tools.CLIENT_DIRECT_RESOURCE_ACCESS` constant and cut a speculative counterfactual-derivation test; GREEN added the refusal-parity cases test-only. The full battery passed 4036 ordinary + 133 compatibility tests under the rebuilt candidate wheel (first-pass compatibility/performance errors were self-inflicted `uv` cache contention during the xdist run; the clean rerun matched T6.6 byte-for-byte). The overnight session halted on /tmp inode exhaustion (1,048,576-inode tmpfs ceiling, reached by the file-count-heavy MCP fixtures) mid-way through T8's Codex RED review; the T8 closure bullet above records the resumed cadence.
- MCP implementation T6 closed 2026-07-29 (`92380e0`): the server registers the generation-qualified catalog resource and the two frozen `standards://` templates over the T2 facade — the catalog body is a mechanical FR-001 field-mask of `CatalogDescriptor` (83,764 bytes on real Catalog 5, down from the 373,619-byte full dump the GREEN review rejected against NFR-002), package resources serve the full exact `StandardDescriptor` as JSON structurally equal to the DTO projection, and payload reads return per-read digest/containment-rechecked bytes with the DR-002 declaration fields in the `dev.project-standards/declaration` `_meta` key; `resources/list` stays compact (catalog plus package versions) while every payload URI remains addressable through the templates, with launch aborted if any addressable URI disagrees with the adapter's own template; non-canonical URIs (trailing slash, uppercase, needless percent-encoding, the retired three-segment index form, the two-segment form, traversal, NUL) are refused from the registration index before any facade call, canonical not-found refusals speak each era's dialect (legacy `-32002`, modern `-32602`), instructions text was re-trimmed to phase truthfulness, and a corrupted distribution fails before stdio with empty stdout. Two adversarial Codex reviews (RED 8 findings, GREEN 3) were dispositioned with recorded arbitration and zero RED-oracle changes during GREEN; the real-catalog smoke that caught both scale defects is queued as a T10 byte-budget candidate; the full battery passed 4022 ordinary + 133 compatibility tests under the rebuilt candidate wheel; T7 (declared prompts and shared read fallback) is the ready task awaiting owner direction.
- MCP implementation T5 closed 2026-07-29 (`72794ea`): `project-standards mcp` launches a stdio server on the low-level `mcp==2.0.0` SDK server (the high-level `MCPServer` was proven unable to advertise truthfully for an empty registry), completing the eager installed-distribution integrity check before stdio starts, serving both protocol eras (classic 2025-06-18 initialize for Codex and 2026-07-28 per-request `_meta`) with byte-clean stdout and stderr-only logs, advertising capabilities exactly equivalent to registrations (nothing at T5) with ADR 0026's permanent negatives pinned (`subscribe` absent, `listChanged` false, no write/remote/logging capability), and resolving explicit roots through `resolve_effective_root` where the launch-time `--root-boundary` and client-advertised roots narrow but never replace, select, or widen; the adapter configuration is non-tunable (identity, version, and phase-truthful instructions are module constants), `transport.py` is the only SDK importer, and NUL-bearing path inputs are structured refusals. Two adversarial Codex reviews (RED 14 findings, GREEN 3) were dispositioned with recorded arbitration; contract amendments at `e071aa7` froze the resolver's boundary parameter names in plan §5.5 and phase-scoped ADR 0026's six-tool instructions text to T9/T10. The full battery passed 3995 ordinary + 133 compatibility tests under the rebuilt candidate wheel; the orchestrator executed T5.6 directly after transient API overloads killed the engineer mid-battery; T6 (expose exact resources) is the ready task awaiting owner direction.
- MCP implementation T4 closed 2026-07-29 (`b2b9964`): the facade adds `invoke_read_provider`/`validate_repo`/`drift_check`, dispatching the four approved non-mutating operations (validate, verify, lint, drift-check) through a session-isolated worker process under the ADR 0025 boundary — 30-second bound read at call time, SIGTERM→SIGKILL process-group termination with reaping, four-completion-path IPC cleanup, deadline-bound non-blocking request transmission, bounded truncation-marked frames with strict parent-side frame validation, recursive typed-input validation, root-contained finding paths, and canonical mapping-key order in `stable_json` (a confirmed hash-seed nondeterminism fix); allowlist and mutation-effect rejection precede worker creation, and worker results are parity-proven against authoritative direct dispatch with `output_notice` preserved. Two adversarial Codex reviews (RED 18 findings, GREEN 10) were dispositioned with recorded arbitration; the two rejected sandbox-architecture demands (payload filesystem rollback, supervising serializer process) are queued as T10 hardening candidates beside the T3 fd-root item. The full battery passed 3936 ordinary + 133 compatibility tests under the rebuilt candidate wheel; the master-plan T4 files row was amended for the four facade-host files; T5 is the ready task awaiting owner direction.
- MCP implementation T13 and T3 closed 2026-07-29 (`b8effb1`, `6e4e7e8`): `ProviderDescriptor` now carries the declared execution contract (entrypoint, schema references, resource set) with both nested DTO shapes frozen field-by-field in plan §5.5, and the facade adds `inspect_repo`/`reconcile` composing the authoritative control-plane state/planner/fingerprint APIs behind resolved-root containment, audited read allowlists, and structured refusals; degraded-state reconcile refuses at the facade and EC-005 satisfaction is recorded as a T9 tool-layer obligation; two adversarial Codex reviews (RED 13 findings, GREEN 8) were dispositioned with recorded arbitration and mutation-refutation evidence; the full battery passed 3888 ordinary + 133 compatibility tests under the rebuilt candidate wheel; T4 is the ready task awaiting owner direction.
- Owner decisions 2026-07-29: the four-segment `standards://` form is canonical everywhere — both producers aligned and `standards/catalog.md` regenerated (917 rows) at `e400f83`, shipping in standards v5.12.0 with the MCP server; standards packages stay locked until the MCP server is live except minor-level version changes; codex-cli 0.146.0 was assessed and dual-era serving remains mandatory (the `mcp_2026_07_28` flag ships disabled by default, openai/codex#34747; T11 re-check queued).
- MCP implementation T2 (exact package-resource services) closed 2026-07-28 at `e06da3b`: the SDK-free `McpServiceFacade` in `src/project_standards/mcp_services/` serves the generation-qualified catalog, exact V2-derived standard descriptors, and per-read digest/containment-verified resource bytes from either the installed distribution or a validated source repository, with fd-based no-follow reads, boundary-contained payload roots, strict frozen DTOs, and a four-code structured error taxonomy; two adversarial Codex reviews (RED: 8 blocking + 3 should-fix; GREEN: 3 blocking + 3 should-fix) were dispositioned with all accepted findings fixed pre-commit, and the full battery passed under the rebuilt candidate wheel (3869 ordinary + 133 compatibility tests, dogfood validate, strict static gates); the DTO-shape owner review it queued closed 2026-07-29 via T13.
- MCP implementation T1 (the Step 09 decision gate) closed 2026-07-28 at `d695d46`: the final 2026-07-28 MCP protocol and exact `mcp==2.0.0` (MIT, released same day, dual-era serving) are pinned under accepted ADRs 0025/0026 with recorded owner approvals; SPEC-RD01 is at revision 1.6 and SPEC-MS01 at 1.3 with OQ-001–004/006/007 resolved and OQ-005 assigned to T11; the evidence register is `docs/research/2026-07-28-project-standards-mcp-protocol-sdk-client-matrix.md`; commits are not yet pushed.
- Key client facts frozen for the server design: installed Codex CLI 0.145.0 negotiates protocol 2025-06-18 only (making SDK v2 dual-era serving mandatory and the `standard_read` fallback required), while Claude Code 2.1.220 supports resources, prompts, and roots natively; re-confirmed for 0.146.0 on 2026-07-29 (its `mcp_2026_07_28` flag ships disabled by default).
- Test-ledger repair `37a6486` split the node authority pins into the immutable v5.8.0 capture and new `current_*` working-tree fields after `96b21bd`'s js-yaml override diverged them and silently broke pytest collection; the ordinary suite passes 3856/3856 and the consumer-outcomes compatibility module 6/6 under the wheel-runtime PYTHONPATH.
- The `standards://` URI alignment disclosed in ADR 0026 was decided 2026-07-29: the four-segment SPEC-MS01 form is canonical, both producers are aligned at `e400f83`, and the served grammar keeps rejecting the retired three-segment form.
- Project Standards 5.11.0 is published at `ab7563585161a67ee9d8d219e752f947766a274b`; signed `v5.11.0` tag (GPG `9375AFEFA6F841B0`, verified GOODSIG), force-moved `v5` tag, and the Latest GitHub release are live.
- Wheel SHA-256 `53e93aa6…` and sdist SHA-256 `4c3b36b2…` match the qualified bytes.
- All nine release-commit workflow runs passed; `Check` completed in roughly 41 minutes.
- Issues #70–#74 are closed with release references: create-only deletion drift explained with apply convergence (#70, `b5f4014`/`f476c41`), engine `shape.defaults` fallback resolved (#71, `c8c192b`), masked-view parity for `max_rule_summary_chars`/`require_tables_or_bullets` (#73, `c8c192b`/`772d3a1`), `standards show` fallback-basis disclosure (#74, `8b2b80f`/`772d3a1`), and adr 1.3/cli-documentation 1.5/project-spec 1.5 dropping mutable release links (#72, `984acff`).
- Verified by two rounds of internal adversarial review (round 1 refuted `b5f4014`'s executor skip, round 2 confirmed the `f476c41` rework) plus an external Codex review of 4 findings (1 already-fixed, 3 accepted and fixed in `772d3a1`).
- Issues #55 and #62 remain open-deferred; new trackers #75 (enrichment FIFO mispairing), #76 (planner policy-transition asymmetry), and #77 (CREATE-side truthiness for empty managed artifacts) were filed from review discoveries.
- Consumer-facing behavior: `shape.defaults` now applies on the provider-less legacy route, and paragraphs over 360 characters in fatal-severity docs (e.g. `docs/TODO.md`) can now block `validate`.
- Every predecessor remains byte-identical and selectable.
- Post-release, `96b21bd` overrides `js-yaml` to `5.2.2` for a Dependabot high-severity DoS advisory, keeping `markdownlint-cli2` at `0.23.1` for action alignment until `markdownlint-cli2-action` ships `>=0.23.2`.
- Project Standards 5.10.0 was published at `4caeb104`; its signed tag and byte-verified wheel (`c51d1e78…936`) and sdist (`7b44fdb6…f80`) assets remain live.
- Issues #50–#54, #56–#61, and #63–#69 closed with release evidence; #64 was already fixed in Markdown Tooling 1.9 and is now regression-pinned; #67 closed as consumer-owned content with no code change.
- SPEC-VFCT rev 0.3 is implemented and released under the MCP-hold exception; it was implemented directly against the specification with no separate plan document.
- Project Standards 5.9.0 was published at `7e63055`; its signed tag and byte-verified wheel (`3c38ba98…f729`) and sdist (`91748772…59a1`) assets remain live.
- The 5.9.0 release-commit `Check` false negative was resolved by test-only `cb5ba6d` without changing release bytes; issues #32 and #35–#49 are closed.
- SPEC-VAIC rev 0.12 is implemented and released under owner-approved Option 1 and the MCP-hold exception; its completed plan was pruned.
- Project Standards 5.7.0 was published at `06162ec`. Its signed tag and byte-verified wheel (`4e3f2733…3e34`) and sdist (`9f967fa3…8e95`) assets remain live. All eight release-commit workflows passed, including `Check` run `29951568179`; issues #24 and #25 are closed with release evidence.
- The release adds Python Tooling 1.7 per-root coverage scoping — `additional_source_roots` entries may be strings (both-scope) or `{ path, coverage = false }` tables that join the checker `include` and Ruff `src` values but not `coverage.run.source` — and the TOML adapter's anchored comment preservation: managed-region rewrites consume their full source spans and re-emit consumer comments above the statement with the same key, ending the silent displacement and blank-line residue reported in issue #25. Exact verification passes 3,273 full-suite tests, CI-mirror lanes of 3,189 ordinary tests, 84 compatibility rows, and 5 performance tests, 90% coverage over the candidate wheel, and every local release gate.
- Project Standards 5.6.0 was published at `55ac756`. Its signed tag and byte-verified wheel and sdist assets remain live. All eight release-commit workflows passed, including `Check` run `29942273464`; issues #20–#23 are closed with release evidence.
- Released history: Project Standards 5.6.0 adds Python Tooling 1.6 `additional_source_roots`, governing-option contribution metadata, enriched `CP-CONSUMER-CONFLICT` diagnostics at reconciliation-plan schema 1.1, readiness-signal migration preview exit codes, corrected upgrade documentation, and internal Standard Bundle Authoring 2.5. A Codex cross-agent review gated the release; both accepted findings were fixed before publication. Exact verification passes 3,167 ordinary tests, 83 compatibility rows, 5 performance tests, 90% coverage, and every local release gate; downloaded wheel (`10f3d70b…eece5`) and sdist (`aab05134…7ab4`) assets match the qualified bytes exactly.
- Project Standards 5.5.0 was published at `8cbb306`. Its signed tag and byte-verified wheel and sdist assets remain live. All eight release-commit workflows passed, including `Check` run `29930356123`; issues #16–#19 are closed with release evidence.
- The release adds bounded semantic whole-file migration signatures, Markdown Tooling 1.7 disabled-formatter migration, Project Specification 1.4 configured-empty-corpus success, Agent Handoff 1.4 managed-envelope size accounting, and absolute Project Specification finding coordinates. All prior payloads remain byte-identical and selectable.
- Python Tooling 1.5 replaces whole-table checker/pytest ownership with canonical key ownership while preserving consumer `extraPaths` and `pythonpath`; its declared 1.4→1.5 transition safely retires historical table locks. Markdown Tooling 1.6 renders configured globs as inline code so strict MD037/MD049 policies do not parse them as emphasis. Released predecessors remain byte-immutable and selectable. `packages check-release --baseline v5.3.1` reports `minor` with no findings.
- The 5.3.0 release includes the complete adoption-mechanics audit remediation: all 3 blockers, 8 majors, minor/docs residue, and 25 singleton findings have implemented or explicit by-design dispositions in `docs/reviews/2026-07-20-adoption-mechanics-audit.md`. Compatible successor payloads are Agent Handoff 1.3, Markdown Frontmatter 1.4, and Project Specification 1.3; CLI Documentation 1.3, Markdown Tooling 1.5, and Python Tooling 1.4 carry their audit corrections. Engine and package re-reviews converged with no remaining findings.
- The consumer-documentation drift audit anchored to pre-remediation commit `88a6f88` is closed: all 17 documentation findings (1 high, 11 medium, 5 low) and all 3 implementation bugs have verified outcomes in `docs/reviews/2026-07-21-project-standards-drift.md`. Released payload bytes remain unchanged.
- Project Standards 5.3.1 publishes all 24 accepted code-simplification findings as a PATCH release: shared schema, package, control-plane, CLI, sync, Agent Handoff, adapter, and specification logic replace verified duplicate implementations without changing accepted inputs, consumer outcomes, package or catalog selections, schemas, workflows, or immutable payloads. `packages check-release --baseline v5.3.0` reports `patch` with no findings.
- Catalog 5 contains seven consumer packages plus reference-only Python Coding 0.6.
- Current defaults are ADR 1.3, Agent Handoff 1.6, CLI Documentation 1.5, Markdown Frontmatter 1.6, Markdown Tooling 1.10, Project Specification 1.5, and Python Tooling 1.10.
- Internal Standard Bundle Authoring 2.6 is advertised but not consumer-selectable. Every superseded payload remains advertised.
- Exact 5.5.0 verification passes 3,139 ordinary tests, all 83 catalog-derived compatibility rows, 5 performance tests, 90% branch coverage, strict Ruff/BasedPyright checks, package graph/schema/projection/catalog and MINOR-classification checks, 8 coherence tests, dogfood and Agent Handoff validation, and Python and Node dependency audits with no known vulnerabilities. Downloaded wheel (`bebc096f…c6e5a`) and sdist (`7e1f2792…96e9`) assets match the qualified bytes exactly.
- Exact 5.4.0 verification passes 3,066 ordinary tests, all 80 catalog-derived compatibility rows, 5 performance tests, 90% branch coverage, strict Ruff/BasedPyright checks, package graph/schema/projection/catalog and MINOR-classification checks, 8 coherence tests, dogfood and Agent Handoff validation, and Python and Node dependency audits with no known vulnerabilities. All eight release-commit workflows passed, including `Check` run `29883365570`; downloaded wheel (`2915f4ce…a2e39`) and sdist (`d9bcfe3e…bac87`) assets match exactly.
- The 5.2.0 correction unblocks V4→V5 migration for released v4 consumers (issues #9, #10, #11 plus an unfiled check-workflow digest gap): the platform gate accepts the `"v3"` wire tag, legacy signatures carry the released digest lineage, and the new `unknown_content_disposition = "preserve"` contract (bounded takeover) preserves consumer-modified instruction and shared-configuration files with a `CP-MIGRATION-BOUNDED-TAKEOVER` warning while reconciliation manages only the bounded units inside them.
- All 100 implementation-review findings have final dispositions: 96 accepted or adjusted corrections are implemented, four findings are closed with no change, and none is deferred or queued. All 14 consumer-documentation drift findings and both audit-discovered CLI defects are also corrected. Every consumer surface requires Python 3.14 or newer.
- The repository dogfoods the unified `.standards/` control plane; legacy `.project-standards.yml` authority is absent.
- The retained repository gate is direct: ordinary tests with coverage, the catalog-derived compatibility matrix on four xdist workers, performance tests, and a coverage report.
- Release-only replay, retained self-referential evidence, the custom test orchestrator, and their frozen predecessor fixture have been removed as unnecessary release-preparation machinery.
- Generic consumer-owned workflow support and optional parallel/subprocess coverage remain part of the immutable Python Tooling 1.2 package API; this repository simply no longer selects parallel coverage.
- Core control-plane, package, migration, source/wheel compatibility, composition, performance, formatting, typing, dependency, and documentation checks remain required.
- The 5.1.1 migration correction preserves partial Catalog 4 adoption: absent package namespaces are skipped, present empty namespaces adopt defaults, unknown settings remain blocking findings, and malformed selected values fail closed with a trusted provider coordinate.
- Verification claims are command-based; rerun the retained gate against current HEAD before claiming completion.
- MCP readiness Step 07 remains complete. The documentation reconciliation is complete, and SPEC-MT01 rev 1.4, SPEC-RD01 rev 1.5, and SPEC-MS01 rev 1.2 are approved and re-locked after separate Claude Opus high-effort specification and implementation-plan convergence reviews, including the 5.9.0 factual synchronization.
- The MCP implementation plan remains active and definition-only: 12 tasks, 72 subtasks, 61 governing requirements, and SHA-256 `51783be849d524e547e521e188475c9f1b11359a2fdd7ad2e3ee3ce40aeb00a8`. T1 is blocked before CHARACTERIZE on 2026-07-24 because final protocol and official stable SDK evidence are not yet available. T1 must freeze the approved protocol/SDK, client compatibility, SDK-independent boundary, and ADR/owner decisions before T2 begins.
- A temporary MCP project change hold is active until implementation-plan T12 closes. Avoid significant non-MCP features, architectural refactors, standards-package programs, release trains, and other broad repository changes; keep necessary maintenance narrow and obtain owner direction before an exception that could disturb the MCP baseline.
- Agent Handoff consumer retirement and the future `project-toolbox` and `agent-managed-repo` packages remain post-v5 work.
- The consumer-facing [agent adoption/update prompt](adoption-prompt.md) resolves GitHub's latest official release once, pins that immutable tag for the task, and provides preview/apply and preservation guidance, verification, and sanitized upstream irregularity reporting.
- Housekeeping 2026-07-19: `docs/superpowers/` retired (designs → `docs/specs/archive/`, research → `docs/research/`, active plan → `docs/plans/`); completed plans, review audits, and `docs/codex-reviews/` deleted.
- Durable implementation history remains in `docs/handoff/sessions/2026-07.md`.
