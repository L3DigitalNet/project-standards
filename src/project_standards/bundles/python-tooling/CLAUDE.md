# CLAUDE.md

This repository follows the Python Tooling SSOT Standard. The canonical agent instructions live in `AGENTS.md` — read it before editing code. Run the verification gate (`uv run ruff format --check . && uv run ruff check . && uv run basedpyright && uv run coverage run -m pytest && uv run coverage report && uv run pip-audit`) before claiming completion.
