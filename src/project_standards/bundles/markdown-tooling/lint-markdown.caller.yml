name: Lint Markdown

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  lint-markdown:
    uses: L3DigitalNet/project-standards/.github/workflows/lint-markdown.yml@{{ref}}
    with:
      globs: '**/*.md'
