name: Validate project specs

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate-specs:
    uses: L3DigitalNet/project-standards/.github/workflows/validate-specs.yml@{{ref}}
    with:
      config-path: ".project-standards.yml"
      standards-ref: "{{ref}}"
      strict-lint: false
