# TODO

## Purpose

This document is the user's visible task list alongside the v3 handoff system. Use it to track action items, follow-ups, and personal notes that should stay easy to find instead of living only in agent-facing handoff docs.

## Usage Instructions

- Write each actionable item as an unchecked Markdown task: `- [ ]`.
- When an item is completed during a session, change its marker to `- [x]`.
- During v3 handoff closeout, delete completed items from this document.
- Mirror any handoff task, todo, pending item, or follow-up here so the user can track it.
- Do not start or complete TODO items unless the user explicitly asks for that work.

<!-- LLM-EDIT-BOUNDARY: DO NOT EDIT ABOVE THIS LINE -->

## User Tracked Tasks

- [ ] Adopt the branch protection strategy and PR requirements from the `hw-radar` repo.

## Agent Tracked Tasks

- [ ] **(Informational, do not block) `spec new` symlinked-parent edge cases** (from Spec #2 final + security review, pre-existing, not regressions): (a) a deliberate above-cwd relative write like `spec new ../sibling/x.md` gets partial/arbitrary ancestor checking because pathlib does not normalize `..` before the `is_relative_to` bound; (b) a TOCTOU window exists between the parent-symlink check and `mkstemp`/`os.replace` (shared with `adopt/engine._atomic_write`). Both are acceptable for the Linux target; revisit only if `new` grows an engine-style `..`-rejecting pre-validation of `args.path`.
- [ ] **(Informational, do not block) OpenAPI is now 3.2.0 (2025-09-19).** The `project-spec` templates cite "OpenAPI Specification" unpinned, so no change is required; pin to 3.2.0 only if a spec needs a specific contract dialect. Recorded in the README §10 Source register (verified 2026-07-04).
- [ ] **Branch protection adoption plan** (research complete for the user task above; not yet applied — do not apply without explicit go-ahead). Gathered via `gh api repos/L3DigitalNet/hw-radar/branches/main/protection` + repo file inspection:
  - **Timing:** apply at the next major release, not now — enabling required-PR/status-check protection on `main` needs to be coordinated with that release's first PR merge (rather than sprung on a workflow that currently allows direct pushes to `main`).
  - **hw-radar's actual config** (classic branch protection, not a ruleset — `gh api .../rulesets` returned `[]`): protects `main` only, `dev` is unprotected (404). Required status checks `["check", "dependency-review"]` (`strict: true`, both `app_id: 15368` = GitHub Actions), `required_pull_request_reviews` present with `dismiss_stale_reviews: true`, `require_code_owner_reviews: false`, `required_approving_review_count: 0`, `required_signatures: true`, `enforce_admins: true`, `required_conversation_resolution: true`, `allow_force_pushes: false`, `allow_deletions: false`, `required_linear_history: false`.
  - **Why those two checks:** hw-radar's `check.yml` job is named `check` (fmt/lint/type/test/cov/audit gate) and runs on push-to-main/dev + all PRs. `dependency-review.yml` job is named `dependency-review`, **PR-only** (OQ20-ratified OSS-license allowlist gate over `uv.lock` via `dependency-review-action`) — deliberately excluded from `push` so it only gates PRs, not direct-to-branch commits (documented gap, spec §16 covers the manual fallback).
  - **project-standards current state (updated 2026-07-06 — PARTIALLY DONE out-of-band):** `main` now carries a **ruleset** `release-pipeline` (added between this research and the v4.2.0 release) enforcing `required_signatures` + `non_fast_forward` + `deletion` — but **NOT** required status checks or PR reviews. It is a _ruleset_, not classic protection (`gh api .../branches/main/protection` is still 404; inspect via `gh api .../rulesets`). A companion ruleset `release-pipeline-tags` makes `v*.*.*` tags immutable (blocks deletion + non-fast-forward). So steps (3) signatures and part of (4) force-push/deletion below are effectively satisfied via rulesets; the remaining gap is step (1) required status checks + step (2) PR reviews. (The v4.2.0 release hit this live: unsigned prior-session commits were rejected by `main` and had to be re-signed via rebase.) Five CI workflows exist, all on `push: main` + `pull_request`, with job names: `check` (check.yml), `prettier` (format.yml), `lint` (lint-markdown.yml), `validate` (validate-markdown-frontmatter.yml), `validate-specs` (validate-specs.yml). No `dependency-review.yml` equivalent exists yet — project-standards ships Python tooling scaffolds/validators, not a locked runtime dependency set, so a license-gate workflow isn't a direct port; decide during implementation whether it's needed or N/A.
  - **Adoption steps (not yet executed):** (1) enable classic protection on `main` requiring the 5 existing job-name contexts (`check`, `prettier`, `lint`, `validate`, `validate-specs`) with `strict: true`; (2) set `required_pull_request_reviews` (`dismiss_stale_reviews: true`, `required_approving_review_count: 0` to match hw-radar's solo-maintainer posture, `require_code_owner_reviews: false`); (3) set `required_signatures: true` (repo already commits under GPG key `9375AFEFA6F841B0` per global git policy); (4) set `enforce_admins: true`, `required_conversation_resolution: true`, `allow_force_pushes: false`, `allow_deletions: false`; (5) leave `testing` unprotected, mirroring hw-radar's unprotected `dev` (the working branch merges to `main` for release, per existing `docs/handoff/state.md` release flow); (6) apply via `gh api repos/L3DigitalNet/project-standards/branches/main/protection -X PUT --input -` with a JSON payload (mirrors `hw-radar`'s), or the repo Settings UI; (7) decide on a `dependency-review.yml` port — likely deferred/N/A absent locked runtime deps.

## CLI Documentation Standard — integrate as a fully adoptable standard

**Decision (2026-07-07):** the new CLI documentation standard (`standards/cli-framework/cli-documentation-standards.md`, currently untracked, research-prose draft) will be integrated as a **fully adoptable** standard — the sixth registered standard alongside Markdown Frontmatter, ADR, Python Tooling SSOT, Markdown Tooling, and Project Specification. This is a multi-phase feature (content rewrite + adopt-engine plumbing + tests + docs), **not** a reference-only drop like `python-coding`. Do not start until sequenced through the repo's spec→plan process (below). Phases are roughly ordered; the gate item in Phase 0 is time-sensitive.

- [ ] **Phase 0 — unblock the toolchain gate (time-sensitive).** The draft already breaks the green gate: it matches the frontmatter validator's `include: standards/**/*.md` glob and has no frontmatter, so `uv run validate-frontmatter --config .project-standards.yml` now reports `standards/cli-framework/cli-documentation-standards.md: no frontmatter found` (`✗ 1 error`). Until Phase 3 gives the governing doc canonical frontmatter, add an interim `standards/cli-framework/**` line to the `exclude:` block in `.project-standards.yml` (mirroring the existing `standards/python-coding/**` exclusion) so the corpus validates. (markdownlint + Prettier already pass on the file — only the frontmatter validator is red.)

- [ ] **Phase 1 — decide the canonical standard id and file layout.** The bundle contract (`standards/README.md` → "Bundle anatomy") requires the governing standard to live at `standards/<standard-id>/README.md`. Two decisions: **(a) standard-id** — the dir is `cli-framework/` but the content governs *CLI usage documentation*; pick the registry id (hyphenated), e.g. `cli-documentation` vs keeping `cli-framework`. This id is used everywhere (bundle dir, registry accessor, `.project-standards.yml` key, adopt CLI arg). **(b)** rename `cli-documentation-standards.md` → `README.md` inside that dir so it renders as a bundle.

- [ ] **Phase 2 — rewrite research prose into a normative standard.** The draft is a research report (Executive Summary / Bottom line / Observation-Inference-Recommendation), not a house-style standard. Bring it in line with `markdown-frontmatter/README.md` and `project-spec/README.md`: add requirement language (MUST/SHOULD/MAY), a Version assumptions section, an Evidence convention, and a Table of contents. **Editorial cleanup required regardless of house style:** (a) strip research-tool citation artifacts — the raw `citeturn…`/`fileciteturn…` tokens (e.g. lines ~314, ~318, ~455) and the stray trailing superscript digits (`… invocation. 1`, `… and README. 2 3`); convert to real numbered reference markers keyed to the Sources list, or remove; (b) **deduplicate the two `## Sources` sections** (~line 515 curated/annotated register + ~line 547 raw URL dump) into one; (c) fix the malformed "Bottom line" table (2 header cells, 3-column `| --- | --- | --- |` separator).

- [ ] **Phase 3 — bundle anatomy: author `adopt.md`, `templates/`, `examples/`.** An adoptable bundle needs more than `README.md`:
  - `standards/cli-framework/adopt.md` — adoption runbook **with canonical frontmatter** (`doc_type: runbook`), modeled on `standards/markdown-tooling/adopt.md` (the closest doc-heavy, copy-adopt analog).
  - `standards/cli-framework/templates/` — promote the in-doc scaffolds (canonical `usage.md` template, compact single-file README template, option-entry block, task-first example block, the CI-parity workflow) into real placeholder template files. `templates/**` is frontmatter-excluded (intentional placeholders), already covered by the existing `standards/**/templates/**` exclude.
  - `standards/cli-framework/examples/` (optional but preferred) — one validated, dogfooded worked example carrying **real** frontmatter (frontmatter-validated, not excluded).
  - Give `standards/cli-framework/README.md` canonical frontmatter, then **remove the Phase-0 interim exclude** so README + adopt.md are validated (templates excluded, examples validated) — matching how every other adoptable bundle is treated.

- [ ] **Phase 4 — adopt-engine plumbing (code; enforced invariant).** `cli.py:67 _assert_registry_bundle_parity` hard-fails (exit 2) unless a version-tracked standard has BOTH a registry contract AND a `bundles/<id>/` manifest. Wire all sides:
  - `src/project_standards/registry.py` — add a `cli_documentation_default` (or chosen-id) contract-version accessor alongside `frontmatter_default` / `adr_default` / `python_tooling_default` / `markdown_tooling_default`, plus its version source.
  - `src/project_standards/cli.py` — add the id to `_REGISTRY_STANDARD_IDS` (~line 48) and to the `_contract_version` dispatch map (~lines 36–39).
  - `src/project_standards/bundles/<id>/adopt.toml` — declarative manifest (`[standard] id = "<id>"` + `[[artifact]]` entries: template files as `kind = "file"`, the `.project-standards.yml` contract block as `kind = "fragment"`). `adopt/manifest.py:available_standards` auto-discovers the bundle dir, so no change there. Copy the artifact source files into `bundles/<id>/` (byte-identical dogfood expectation).
  - `.project-standards.yml` — add a `cli_documentation: version: "1.0"` (or chosen-id) contract block to dogfood the new standard, mirroring the `markdown_tooling` / `python_tooling` blocks.

- [ ] **Phase 5 — CI workflow decision.** The draft designs a doc↔parser parity / `--help` snapshot CI workflow. Decide whether to **ship it as a reusable workflow** (like markdown-tooling's `lint-markdown.caller.yml` / `format.caller.yml`, adding a `bundles/<id>/*.caller.yml`) or leave CI as adopter guidance only. Note the draft's own caution: normalize `NO_COLOR` + terminal width before snapshotting (Python 3.14 argparse emits colored help; Click rewraps by width), and don't assert on argparse section headings.

- [ ] **Phase 6 — tests.** Add coverage matching the existing gate: adopt-manifest test for the new bundle (`tests/test_adopt_manifest.py` — byte-identical materialization + manifest validation), frontmatter validation of the new `examples/` doc, and registry↔bundle parity coverage (the new id must appear on both sides). Keep `uv run coverage report` and the full gate green.

- [ ] **Phase 7 — register in the index + propagate across all six doc surfaces.** Each already carries a parallel `python-coding` row to mirror (but as *registered/adoptable*, not draft):
  - `standards/README.md` — add a table row (Bundle + **Adopt** link, not "—") and update the "Bundle anatomy" README-only note (which currently says "currently just python-coding/").
  - root `README.md` — TOC entry (~line 19), directory tree (~line 41), a dedicated `### CLI Documentation Standard` subsection (~line 97 area), the intro "single source of truth" enumeration (line 3), and the adoption map (~line 105).
  - root `CLAUDE.md` — the Purpose line (line 5) currently says "defines five"; update to six and add the standard to the enumerated list (out of the reference-only bucket).
  - `docs/handoff/architecture.md` — the component-graph standards list (line 9) and, if applicable, drop from Standing backlog.
  - `docs/handoff/specs-plans.md` — add the design/plan pointer rows once Phase 8 produces them.
  - `STATUS.md` — reflect the new standard in the at-a-glance state at release.

- [ ] **Phase 8 — versioning, changelog, release.** Bump per `meta/versioning.md` (new adoptable standard = minor at minimum), add a `CHANGELOG.md` entry and an `UPGRADING.md` note if adopters need action, then run the full gate (`ruff` + `basedpyright` + `pytest`/coverage + `pip-audit` + `tests/coherence`) before the release decision on `testing`→`main`. **Process note:** the repo ships every standard through a design spec (`docs/superpowers/specs/`) → codex spec-review → TDD plan (`docs/superpowers/plans/`) → executing-plans; sequence this feature the same way rather than editing in place — the phases above are the scope input to that spec, not a license to skip it.
